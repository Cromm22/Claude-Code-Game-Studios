# Tech Debt Register

Tracks advisory-level deviations and known gaps accepted during `/story-done` closure — not blocking at the time, but worth revisiting. Each entry links back to the story that surfaced it.

| ID | Source Story | Description | Owner/Target | Status |
|----|--------------|-------------|---------------|--------|
| TD-001 | `pc-2` (Lantern Axis, Light-to-Gather Gate & Tap-Hold Gather) | `_globalEventCounts` (ADR-0006's global RemoteEvent budget) is currently scoped to `PlayerControllerService` only, not truly project-wide — a client could combine calls across multiple services to exceed the intended 30/s cap once a second client-fired-event service exists. | Story 010 (RemoteEvent Trust Boundary Application) | Open |
| TD-002 | `pc-2` | Minor redundant window-reset duplication in `PlayerControllerService.luau`'s step-3 global-budget check — harmless (traced clean), but inconsistent with step 2's cleaner single-source-of-truth pattern. | `PlayerControllerService.luau` | Open |
| TD-003 | `pc-2` | 4 suggested (non-blocking) test cases never added: `no-player-state` drop path, double-`BeginGatherHold` overwrite semantics, `TryCommitGatherHold` nodeId-mismatch branch, H.10's literal 200ms debounce window (current implementation debounces unconditionally, functionally stricter than the AC but untested as literally specified). | `tests/unit/player-controller/lantern-gather-gate_test.luau` | Open |
| TD-004 | `ed-3` (Spatial Hash Grid, Live-Source List & Cap Eviction) | Once Story 004's real attribution archive exists, add a behavioral test (not just the current static/structural proof) confirming an evicted source's archive entry remains resolvable — currently only proven by construction (no archive parameter exists in the eviction functions). | Story 004 (Attribution Archive) | **Resolved** (`ed-4`) — `tests/unit/ecological-disturbance/attribution-archive-ttl-cap_test.luau::test_evicting_a_live_source_never_touches_a_separate_archive_table` now proves this behaviorally: a pre-populated archive entry survives a real `SpatialGridLogic.EvictOldestSource` call untouched. |
| TD-005 | `pc-3` (Disturbance Emission Publisher) | `platform` is hardcoded to `"pc"` at the emission-heartbeat call site (`PlayerControllerService.luau:663,668`) — no real per-player platform-classification signal exists in any RemoteEvent surface yet. This is not just an untested case: any real touch player with 1.0–2.0 studs of XZ drift (between `EMITTER_MOVEMENT_DELTA_FLOOR` and `MOBILE_ANALOG_DRIFT_THRESHOLD`) is misclassified as "moving" today and pays full pulse magnitude (0.10/0.08) instead of the intended attenuated/hold-floor value — the correct code path exists and is unit-tested in isolation (`test_H13_H14_...`), it's just unreachable in production. Gamepad is unaffected (never gets the drift-zeroing bonus by design). Flagged by pc-3+pc-11 code review, 2026-07-08. | A future story adding a real platform-classification signal (client platform detection + RemoteEvent or `UserInputService`-derived server-side inference) | Open — **do not trust mobile/touch playtesting results until this is resolved** |
| TD-006 | `pc-3` / `pc-11` (Player Controller epic) | `OnSprintPulse` (GDD C.9, the exertion audio-cue transport signal) has no owning story anywhere in the Player Controller epic. Story 010 (RemoteEvent Trust Boundary) and Story 011 (NetworkOwnership & Bandwidth, H.29) both assume it already exists as a wired `Client` signal, but it does not exist anywhere in `src/` (confirmed via grep across all PC files) and no PC story (001–015) claims authorship of declaring/firing it. Flagged by pc-3+pc-11 code review, 2026-07-08. | Epic-level gap — needs a new story (or an amendment to an existing one) to declare and wire `OnSprintPulse`, before Story 010's zero-registration audit or Story 011's H.29 bandwidth figure can be considered to actually include it | Open |
| TD-007 | `rc-2` code review (unrelated to rc-2's own diff) | `docs/architecture/control-manifest.md:118` still describes the older direct-Crafting→RunController caller model ("Crafting & Items reports BCT-DEFEAT to RunController via `RunEndConditionRaised`... the exact same single contract Player Controller uses") — but ADR-0002 already corrected this during `/architecture-review`: Crafting fires `OnBeaconWindowFailed`/`OnBeaconWindowSurvived` to Player Controller, which forwards to RunController. Pre-existing drift, not introduced by rc-2. Flagged by rc-2 code review, 2026-07-08. | `docs/architecture/control-manifest.md` | Open |
| TD-008 | `ed-8` (GetHottestHotspot) + `ed-14` (Streaming) code reviews | Two documentation-only ADR-0004 inaccuracies, both independently confirmed by code review (not implementation defects — both stories' code is correct and GDD-compliant): (a) ADR-0004's Decision text and `control-manifest.md:102`'s Performance Guardrail state `GetHottestHotspot`'s and `fieldValue()`'s cell-radius scan both "touch only ~4–9 grid cells per query" — true for `fieldValue()`/`topContributorsAt` (fixed 3×3 `QueryNeighborhood`, bounded by `INFLUENCE_RADIUS`), but false for `GetHottestHotspot`, whose caller-supplied `searchRadius` (GDD-documented range 32–512 studs) requires the new `QueryCellsInRadius` primitive and scales `O((searchRadius/SPATIAL_GRID_CELL_SIZE)²)`, not a fixed cell count. (b) ADR-0004's Key Interfaces code sample shows `self._floraChunkInitialSnapshot:Fire(player, snapshot)`, but N5 is explicit these are raw `RemoteEvent`s, whose real dispatch method is `:FireClient`, not `:Fire` (a Knit `RemoteSignal`/`BindableEvent` method) — the real implementation correctly uses `:FireClient`. | A short ADR-0004 addendum splitting the conflated guardrail sentence into two accurate statements (citing `ecological-disturbance.md` line 283's cost formula) + correcting the Key Interfaces `:Fire`→`:FireClient` sample + the matching `control-manifest.md:102` line fix | Open — documentation-only, does not need a full `/architecture-review` cycle |
| TD-009 | `ed-6` (Tier Classification) code review | `self._previousTierAtPosition` has no cleanup path (not on `PlayerRemoving`, not on any maintenance pass) — self-disclosed by the implementer as "tier-stuck-elevated," then sharpened by code review into a more concrete failure mode: if a position's sole live source expires and is culled, then LATER an unrelated, low-magnitude source lands at that exact canonical `%d_%d` cell, `RunClassificationPass` reads the stale elevated `previousTierAtPosition` entry instead of treating it as a first-call — e.g. `TierAt(0.15, "Hunt", ...)` returns `"Tense"` via the Hunt→Tense hysteresis branch instead of the correct first-call `"Calm"`, producing a spurious elevated tier and a needless `TierCrossedEvent`. Zero live consumer today (Predator AI does not yet subscribe to `TierCrossedEvent`), so non-blocking for `ed-6` itself. | A maintenance-pass prune for `self._previousTierAtPosition` (e.g. piggybacking on the existing `EXPIRY_MAINTENANCE_INTERVAL` cull) | Open — **should become an explicit prerequisite before Predator AI subscribes to `GetTierCrossedSignal()`, not just a someday-follow-up** |
