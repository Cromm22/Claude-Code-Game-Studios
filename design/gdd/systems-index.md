# Systems Index: Terranova (Roblox MVP)

> **Status**: Draft
> **Created**: 2026-04-22 (Unity scope) — Rewritten: 2026-04-29 (Roblox pivot)
> **Source Concept**: design/gdd/game-concept.md — **In Review** (round-2 revisions applied 2026-04-30; awaiting round-3 fresh-session re-review). Round-1 revision (2026-04-29) addressed 6 blockers; round-2 revision (2026-04-30) addressed 25 blockers across 7 themes (cosmetic-boundary regression, Fellowship pillar mechanics, predator FSM completeness, onboarding pre-death teaching, network/performance substrate, audio pillar coherence, AC independence). Review log: `design/gdd/reviews/game-concept-review-log.md`
> **Engine**: Roblox Studio + Luau + Knit + Rojo

---

## Overview

Terranova on Roblox is built around a single mechanical premise: the player squad's
survival actions disturb the alien ecology, and the ecology's apex predator hunts
along that disturbance. The Roblox-scoped MVP collapses the original 21-system
Unity vision down to **7 core systems** by (a) deferring resource breadth (oxygen
only), (b) cutting base-building, (c) consolidating crafting/inventory/items into
one GDD, and (d) leaning on Roblox built-ins for input, save/load, settings, world
streaming, and respawn. The systems below are designed for co-op-from-day-one with
server-authoritative architecture (Knit Services on the server, Knit Controllers
on the client).

---

## Systems Enumeration

| # | System Name | Category | Priority | Status | Design Doc | Depends On |
|---|-------------|----------|----------|--------|------------|------------|
| 1 | Player Controller | Player | MVP | **MAJOR REVISION NEEDED** (round-6 design-review complete 2026-05-05 — patch perimeter regressed; round-1 28 BLOCKING items across 4 themes: (1) transaction-integrity / Pillar-1 closure (grace-window first-pulse exploit, disconnect-before-death exploit inverts C.5.3, undefined SPRINT_CLIENT_TIMEOUT heartbeat signal, sprint-audio mental-model error); (2) unverified Roblox APIs (workspace:HasChunkLoaded() likely doesn't exist — entire C.5.6 spectator chunk-guard blocked; Humanoid.Died semantics under post-cutoff Character Controller Library GA unverified); (3) downstream-contract divergence (D.2 missing math.max(0, age) clock-skew pattern from ED.D.1; D.4 nil-platform silently skips drift floor; D.4 output range incorrect after Crafting modifier stack; F.4 perception input is position-only, missing velocity/facing/alive-status; no OnPlayerDied/Respawned signal flow to Predator); (4) mobile-floor regressions (lantern button unreachable mid-encounter on iPhone SE, quick-ping uses red/green/yellow/white with NO shape differentiation — direct accessibility-floor violation, captions queue undefined — WCAG 1.2.2/1.2.4, motion-reduction list omits vignettes + lantern ramp, walk-footstep Roblox-default is Pillar-3 violation, H.29 peak ~2.3KB/s violates <1KB/s claim). Section B player-fantasy is genuinely the strongest articulation in the project — round-2 must rewrite C–H to deliver B, not edit B. Scope L+ (foundational, ~1.5–2× round-1 effort). **Round-2 Theme 1 (Pillar 1 closure) APPLIED 2026-05-01** — G1 grace-cooldown (Sprint + Light) via `GRACE_REENTRY_COOLDOWN = 6.0 s`; N1 disconnect-before-death cost-finalization via composite imminent-death predicate; N2 ghost-sprint closure via `PlayerHeartbeat` RemoteEvent; Au2 sprint-audio mental model + OQ.1 closure. **Round-2 Theme 1 design-review complete 2026-05-02 — verdict NEEDS REVISION (Theme-1-round-2 patch required).** Theme 1 closed 4 of the 28 round-1 BLOCKING items (G1 grace-cycle exploit closed for Sprint + Light) but introduced 7 new BLOCKING items of equivalent severity: (1) `_deathCostPaid` check-and-set not atomic across Luau yield boundary (idempotency hole — entire load-bearing mechanism of N1); (2) `_lastHeartbeatTime` initialization on T1 unspecified — every legitimate sprint-after-walk-pause >3s would force-revert; (3) `lastPredatorDamageTimestamp` lifecycle/clearance unspecified — stale entries on rejoin trigger false Path B; (4) Path B fires on non-predator damage via health arm — Pillar 2 violation; (5) PredatorService init-order race causing `nil - number` runtime error in predicate; (6) OQ.1 closure contradicts V/A.3 walk footstep row in same document; (7) D.4 formula uses `t_entry` but variables table only defines `t_0`. CD synthesis: "Round-1 finding 'Pillar 1 not delivered at rule level' closed for grace-cycle exploit. New round-2 finding: 'Pillar 2 cost-attribution rule introduces two false-positive paths and one atomicity hole' — equivalent severity to what was closed." Theme-1-round-2 patch is M scope (single fresh session), all rule-level edits to existing sections. **Themes 2/3/4 + cross-cut testability + OQ.3 camera decision STILL PENDING in subsequent fresh sessions** (do not proceed to Theme 2 until Theme-1-round-2 patch closes per CD recommendation). **Re-reviewed 2026-05-02 (third pass, lean-equivalent, Phase 3b skipped per user choice — document unedited since prior review): all 7 BLOCKING items confirmed unaddressed via line-level grep; verdict unchanged.** **Theme-1-round-2 PATCH APPLIED 2026-05-02** — all 7 BLOCKING items closed at the rule level: (1) C.5.3 set-before-yield discipline added with mandatory 5-step ordering protecting `_deathCostPaid` atomicity across Luau yield boundaries; (2) E.D `_lastHeartbeatTime` lifecycle specified (init on T1, clear on T2/T5/PlayerRemoving, bootstrap-init table to `{}`, first-heartbeat grace via implicit T1 seed); (3) F.4 PredatorService row expanded to mandate Player-instance keying, bootstrap-empty-table init, no-write on PlayerAdded, clear on T6/PlayerRemoving; (4) C.5.3 predicate health arm now scoped to require predator damage (`lastPredatorDamageTimestamp ~= nil` on BOTH arms — closes Pillar 2 cost-attribution violation); (5) C.5.3 explicit nil-guard prose + F.4 init mandate close `nil - number` arithmetic init-order race; (6) V/A.3 walk row updated to "NOT a predator-perception input" closing OQ.1 contradiction; (7) `t_entry` renamed consistently to `t_0` across D.4 / C.8 / H.11b. File grew 906→935 lines. **Round-3 design-review complete 2026-05-02 — verdict NEEDS REVISION (round-3 patch required, scope S).** 6 specialists spawned in parallel adversarially (game-designer, systems-designer, network-programmer, ai-programmer, audio-director, qa-lead) + creative-director synthesis. Patch closure audit: 5 of 7 prior BLOCKING items closed at the rule level (PARTIAL on each — structurally correct with residual defects); 1 closure (item 7 `t_entry` rename) is GDD-side closed but registry-side OPEN (`design/registry/entities.yaml` lines 726-727 still reference `t_entry`); 1 closure (item 4 predicate predator-scoping) is NOT closed at the fantasy level — HP arm has no time bound, allowing 600s-old predator graze + oxygen-depletion to trigger Path B (same Pillar 2 defect class as round-1, narrowed but not eliminated; game-designer + ai-programmer convergent). 5 binding fixes (B1–B5) + 1 AC addition (H.22d): **B1** Add time bound to predicate HP arm — **USER DECISION LOCKED 2026-05-02: Option A** (mirror arm 2 with `(now - lastPredatorDamageTimestamp[player]) < HP_ARM_RECENT_WINDOW`, default **30 s** per CD recommendation; round-3 patch must update C.5.3 predicate + line 118 prose, register `HP_ARM_RECENT_WINDOW` in `entities.yaml`, and add G.6 knob row with range 15–60 s); **B2** Fix or annotate H.22b (setup contradicts patched predicate; tester would file false regression bug); **B3** Rewrite E.E line 503 predicate summary using patched predicate verbatim (edge case section currently describes pre-patch un-scoped OR); **B4** Update V/A.3 governing paragraph (line 704) to cover walk + sprint (currently sprint-only; rule statement must be self-defending vs table cell); **B5** Rename `t_entry` → `t_0` in `entities.yaml` lines 726-727 (registry-vs-GDD divergence); **H.22d** Add AC for Path B time-arm in isolation (arm 2 has zero coverage). CD synthesis: "Pillar 1 mostly delivered at the rule level. Pillar 2 NOT delivered — HP arm with no time bound is the same fantasy violation round-2 was meant to close, narrowed but not eliminated. Five binding fixes plus one AC. B1 is the only one requiring a user design decision; B2-B5 are mechanical (~30 min combined). Round-3-patch effort: B1 user-decision + ~60 min implementation; B2-B5 ~30 min combined." Forward-obligation list (NOT blocking Theme 1; defer to Themes 2/3/4) documented in review log including yield-point enumeration completeness, PlayerHeartbeat rate-limit timestamp-write ordering, F.4 nil-table-vs-nil-entry, T6 cross-ref, `_deathCostPaid` lifecycle clearance, AC additions H.33d/H.33e/H.22e, V/A.4 walk caption parity, OQ.1 closure text reconciliation. **Round-3 patch APPLIED 2026-05-02** — all 5 BLOCKING items + H.22d closed at the rule level: (B1) C.5.3 predicate HP arm now requires `(now - lastPredatorDamageTimestamp[player]) < HP_ARM_RECENT_WINDOW = 30 s`; line 118 prose rewritten to defend the 30 s window; G.6 row added with range 15–60 s; `HP_ARM_RECENT_WINDOW` registered in `entities.yaml` (Pillar 2 stale-graze defect closed); (B2) H.22b setup now includes `lastPredatorDamageTimestamp[A] = now - 5 s` precondition matching patched predicate; (B3) E.E line 503 predicate summary rewritten verbatim with both nil-checks + HP-arm time bound; (B4) V/A.3 line 704 governing prose extended to cover walk + sprint at the boundary statement (rule statement now self-defending vs table cell); (B5) `entities.yaml` lines 726-727 renamed `t_entry` → `t_0` (registry-vs-GDD divergence closed); (H.22d) new AC added for Path B time-arm in isolation (HP=100 + recent timestamp). File grew 935→938 lines. **Round-4 design-review complete 2026-05-02 — verdict MAJOR REVISION NEEDED (round-4 patch required, scope M).** 6 specialists spawned in parallel adversarially (game-designer, systems-designer, network-programmer, ai-programmer, audio-director, qa-lead) + creative-director synthesis. Round-3 closures verified at predicate level (B1-B5 + H.22d structurally consistent), but round-3 closure perimeter was wrong — round-4 surfaced 6 NEW BLOCKING items inside Theme 1 scope (rule-level cost attribution for Path B), not deferrable to Themes 2/3/4. CD: "Pillar 2 NOT delivered at the rule level" — three-way convergence on Path B downstream: (B6) `_deathCostPaid` never cleared on T6 — second death per session pays zero squad oxygen [ai-programmer F1]; (B7) PlayerRemoving teardown-order race between PredatorService and PC silently nulls the patched HP-arm predicate under non-deterministic Knit service connection ordering [network-programmer C2 + ai-programmer F2 — convergent]; (B8) no rollback path when `RequestSquadOxygenSpend` fails — `_deathCostPaid` is permanently set, oxygen never deducted [network-programmer C1]. Plus: (B9) D.4 grace-window jitter exploit — missed-grace-window pulses do not consume grace eligibility, G1 exploit class re-opens under server jitter realistic on thermally-throttled mobile [systems-designer F5]; (B10) clock-injection seam missing — `workspace:GetServerTimeNow()` cannot be mocked in TestEZ/Lemur, making H.22b/H.22c/H.22d/H.33a/H.33b non-executable as written, B2/H.22d closures are "conditionally paper" until DI seam mandated [qa-lead F1]; (B11) HP_IMMINENT_THRESHOLD `<` vs `<=` boundary undocumented — HP=25 exactly does NOT fire arm 1; same for `< HP_ARM_RECENT_WINDOW` [systems-designer F10 + ai-programmer F4 — convergent]. CD overruled 3 specialist disagreements: (1) game-designer F4/F5 (Path B legibility + emote-wheel HP/stamina) reclassified to IMPORTANT and deferred to HUD GDD as forward obligations; (2) B10 clock-injection escalated from Theme-4 lenient reading to Theme-1 BLOCKING; (3) B9 jitter exploit escalated from Theme-2 lenient reading to Theme-1 BLOCKING. Forward-obligation list documented in review log: HUD GDD (F4 Path B legibility, F5 emote-wheel HP coord); Theme 2 (yield-points, OnPlayerDied scoping, RequestPing tolerance, RequestEmote int-typed slot, dt guards, divide-by-zero); Theme 3 (Danger emote vs ping redundancy, LANTERN_VISIBILITY_RADIUS 12 vs 20 contradiction, V/A.6 cosmetic-boundary scope, audio F2/F5 carried, sprint button UX); Theme 4 (H.22e concurrent-path race, H.27/H.32 hardware fallback, H.31 level-design prereq, F.4 nil-table-vs-nil-entry). Round-4 patch effort: ~2-3 hours focused single-session (~6 surgical edits + ~6 IMPORTANT prose/AC additions + 1 new "Testability Contract" subsection). **Round-4 PATCH APPLIED 2026-05-02** — all 6 BLOCKING items + IMPORTANT cluster closed at the rule level: (B6) T6 transition table side-effects + C.5.3 line 123 prose + new AC `H.22e` close `_deathCostPaid` lifecycle clearance (Pillar 2 multi-life closure); (B7) C.5.3 Path B handler entry now mandates read-and-cache discipline as first statement before any yield + F.4 PredatorService row expanded with cross-service teardown-ordering contract closing the network-programmer + ai-programmer convergent finding; (B8) C.5.3 set-before-yield discipline updated with pcall-protected RemoteFunction + flag rollback path on failure + new AC `H.22f` verifies flag rollback on simulated failure; (B9) D.4 formula updated to anchor `t_lastGracePulse[axis] = t_0` on eligibility (not on grace-window pass), G.6 invariants block added, new AC `H.11h` verifies the missed-grace-window jitter case; (B10) new C.11 "Testability Contract" subsection mandates `getServerTime()` clock-injection seam as a binding rule for testability, H.22b/c/d/H.33a/b GIVEN clauses now reference the injectable clock; (B11) C.5.3 boundary semantics block + G.6 boundary annotation + entities.yaml notes for `HP_IMMINENT_THRESHOLD`, `HP_ARM_RECENT_WINDOW`, `DAMAGE_RECENT_WINDOW` document strict `<` semantics. IMPORTANT cluster: V/A.3 line 706 antecedent disambiguated; C.5.3 first-pass design-intent note + Scenario C false-negative class explicitly acknowledged; D.4 variable table updated for B9 anchor-on-`t_0` semantics; H.11f/H.11g Light-axis cooldown ACs added (parity with H.11b/c). File grew 938→~1010 lines. **Round-5 design-review complete 2026-05-04 — verdict NEEDS REVISION (round-5 patch required, scope M).** 6 specialists spawned in parallel adversarially (game-designer, systems-designer, network-programmer, ai-programmer, audio-director, qa-lead) + creative-director synthesis. Round-4 closures verified at GDD rule level (B6–B11 closed cleanly), but round-4 patch prose introduced/exposed defects at three seams — Path B execution chain, B8 rollback prose, and GDD-internal/registry consistency. CD: "Not the design is broken. The patch prose introduced ambiguity at three seams. Round-5 is the cleanup pass." 6 NEW BLOCKING (R5-B1–R5-B6): (R5-B1) Path B does not invoke T4 (lantern force-lower) — phantom Light emissions on destroyed character, Pillar 1 silent-emission break [game-designer + audio-director convergent]; (R5-B2) B8 rollback's "OTHER death path firing" retry-path prose factually wrong in concurrent-path race — squad oxygen never decrements, `OnPlayerDied` never broadcasts, Pillar 2 silent cost-loss [4-specialist convergence: ai-programmer + network-programmer + game-designer + audio-director]; (R5-B3) C.5.3 predicate inline `workspace:GetServerTimeNow()` violates C.11 forbid-list — undoes B10 closure, H.22b/c/d/e/f paper-only [qa-lead]; (R5-B4) entities.yaml `pulseEmissionMagnitude` line 734 encodes pre-B9 ordering — same divergence class as round-3 B5 [systems-designer]; (R5-B5) entities.yaml `GRACE_REENTRY_COOLDOWN` missing `>=` annotation — B11 incompletely applied [systems-designer]; (R5-B6) `PING_ORIGIN_TOLERANCE` undefined + camera-angle validation impossible — trust-boundary spec-completeness [network-programmer, CD-reframed Theme-1-spec/Theme-2-tuning]. CD overruled 5 disagreements: PING_ORIGIN_TOLERANCE BLOCKING for spec/Theme-2 for tuning; audio carried IMPORTANTs (F4-R4/F6-R4) held IMPORTANT pair-bound to R5-B1/B2; Scenario C player-experience prose IMPORTANT (HUD forward-obligation, not PC defect); T7 silence held IMPORTANT (S5-terminal invariant); qa-lead BLOCKING-R5-02 squad-oxygen call-spy held IMPORTANT (standard TestEZ idiom). Round-4 IMPORTANT cluster audit: F1-R4 + F3-R4 confirmed CLOSED; F4-R4 (lantern SFX per-pulse negative rule) and F6-R4 (Path B death SFX positional 3D nil anchor) NOT applied — review log overstated patch coverage by two items. R5-B2 requires user design decision on failure-recovery semantics (option a: deferred-Heartbeat reconciliation tick CD-recommended; option b: accepted-loss with operator log; option c: path-aware rollback) before patch prose can land. Round-5 patch effort: ~2-3 hours focused single-session, 5 textual fixes + 1 design decision (R5-B2) + ~3-4 IMPORTANT pair-bound prose additions. **Round-5 patch was PARTIALLY APPLIED 2026-05-04/05** — only the two design-decision-required items (R5-B1 Path B step 6 full T5 side-effects with lantern force-T4; R5-B2 user-confirmed option (a) deferred-Heartbeat reconciliation tick with `RECONCILIATION_TICK_INTERVAL = 5.0 s` + `deathDeductionCommitted` flag) were applied to the GDD body; the four trivial textual fixes (R5-B3/B4/B5/B6) were skipped. **Round-6 design-review complete 2026-05-05 — verdict MAJOR REVISION NEEDED.** 6 specialists spawned in parallel adversarially (game-designer, systems-designer, network-programmer, ai-programmer, audio-director, qa-lead) + creative-director senior synthesis. **12 BLOCKING (R6-B1–R6-B12) + 12 IMPORTANT (R6-I1–R6-I12).** R5-B3/B4/B5/B6 confirmed unapplied (carried as R6-B1/B2/B3/B4 with **5-specialist convergence on R6-B1 (predicate inline `workspace:GetServerTimeNow()` violates C.11)** and **4-specialist convergence on R6-B4 (`PING_ORIGIN_TOLERANCE` absent + camera-angle clause unenforceable)**). 8 NEW BLOCKING introduced by R5-B2's reconciliation-tick design surface: **R6-B5 `deathDeductionCommitted` not cleared on T6** (3-specialist convergence: systems-designer + ai-programmer + qa-lead — same defect class as round-4 B6 reproduced for the new flag introduced by R5-B2 patch itself; multi-life Pillar 2 silent break); R6-B6 `RECONCILIATION_TICK_INTERVAL` not registered in entities.yaml (registry divergence); R6-B7 reconciliation tick missing from C.11 seam scope table (testability — all tick ACs unwritable); R6-B8 reconciliation success path doesn't stop pulse timers — phantom Light/Sprint pulses on destroyed `HumanoidRootPart` for up to 10 s (R5-B1 closure regressed for the rollback-then-reconciliation path; Pillar 1 silent reopen); **R6-B9 `userId`-keyed reconciliation rows contradict F.4 `Player`-instance keying** (3-specialist convergence: game-designer + network-programmer + ai-programmer); R6-B10 step 5 `warn(... player.Name ...)` will runtime-crash for Path B retry attempts (`Player` instance destroyed by tick time); R6-B11 reconciliation tick has ZERO AC coverage in Section H (8 missing ACs); R6-B12 Death SFX fires 10s late on `reconciliation-recovery` broadcast — false predator cue violates horror audio-as-feedback contract. CD synthesis: "**Patch perimeter has regressed.** Pillar 1 holds at the rule level for the success path; reopens through R6-B8. **Pillar 2 does NOT hold honestly** — three new silent-loss sub-classes (T6-clearance, 10s OnPlayerDied gap, dual-table keying). Pillar 4 reopens through multi-life flag-clearance. **Meta-pattern: round-5's heavy R5-B2 design surface co-resident with the trivial R5-B1 fix ate the trivial fixes (R5-B3/B4/B5/B6 unapplied). The deepest design surface should have landed AFTER the trivial sweeps closed, not concurrent with them.**" CD-resolved 5 specialist disagreements: PredatorService deferral binding upgrade (IMPORTANT R6-I2); reconciliation success-path side-effects unified (R6-B8); audio carried-unfulfilled escalation rule (R6-I8/I9 mandatory close — process-integrity flag at 3 rounds carried); Lemur fidelity (IMPORTANT R6-I12 cluster); audio Death SFX BLOCKING despite single-specialist (R6-B12 — domain authority on horror audio-as-feedback contract). **Round-7 patch sequencing per CD recommendation: TWO SEQUENTIAL FRESH SESSIONS — Session 1 trivial-fix sweep (~30-45 min, scope S, R6-B1/B2/B3/B4/I6/I7/I8/I9) before Session 2 reconciliation-tick lifecycle and contract patch (~2-3 hours, scope M, R6-B5/B6/B7/B8/B9/B10/B11/B12 + IMPORTANT cluster). Two sessions, sequential. Do NOT attempt them in one session — that is exactly what produced the round-5 partial-patch state.** After both sessions land, run `/design-review design/gdd/player-controller.md` round-8 in another fresh session. If APPROVED at round-8, advance to Theme 2 (Roblox API verification). If NEEDS REVISION at round-8, the converging defect rate suggests the reconciliation-tick architecture itself may need reconsideration. **Round-7 Session 1 (trivial-fix sweep) APPLIED 2026-06-07** — R6-B1/B2/B3/B4 + R6-I6/I7/I8/I9 closed (predicate→`getServerTime()` seam; `PING_ORIGIN_TOLERANCE` registered; camera-angle clause deleted; audio Path-B rules). **Lean re-review 2026-06-08 re-affirmed MAJOR REVISION NEEDED** (no panel — verdict provably carried; 8 round-6 BLOCKING R6-B5–B12 + IMPORTANT cluster still open *by design*, pending the deferred Session 2 reconciliation-tick lifecycle patch; status unchanged). Review log: [player-controller-review-log.md](reviews/player-controller-review-log.md) | [player-controller.md](player-controller.md) | *(none — uses Roblox Humanoid + customization)* |
| 2 | Resource Management | Survival | MVP | Not Started | — | Crafting & Items (oxygen item def) |
| 3 | Resource Node | Survival | MVP | Not Started | — | Crafting & Items |
| 4 | Ecological Disturbance | Ecosystem | MVP | **NEEDS REVISION (light) — round-18 verdict review COMPLETE 2026-05-08 (this session, fresh `/clear`; full 7-spec panel — game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer, qa-lead — + creative-director synthesis ran per round-18 plan with round-15 four-layer protocol + round-17 two refinements applied). 3 BLOCKING + 18 IMPORTANT + ~9 NICE-TO-HAVE; specialist split 6 NEEDS REVISION / 1 APPROVED (network-programmer, no BLOCKING); CD-adjudicated 5 severity disagreements; per-canonical-name fanout grep PASS (5 of 5 canonical names land only on F.2a + F.4 + H.39c canonical surfaces; prior-form `getOnPredatorStateChangedSignal` survives only at line 2236 in clearly-marked deprecated annotation — B-CS-R17-1 closure verified intact); pillar fidelity intact (P1 STRENGTHENED with caveat reverting to NEUTRAL until B-CD18-1 closes; P2 NEUTRAL with concern; P3 STRENGTHENED; P4 NEUTRAL). **3 BLOCKING items (post-CD-adjudication)**: **B-CD18-1** (game-designer + qa-lead) `SQUAD_T_FAIL_FLOOR_THRESHOLD = 0.20` is below D.7 stationary-light asymptote `~0.272` — criterion (iv) safety net inert against permanent-Calm scenario it was designed to detect; published safe-range `0.05–0.29` actively misleads tuners (correct lower bound `~0.273`); **B-CD18-2** (systems-designer + performance-analyst) D.7 `~165 s` sprint-asymptote derivation arithmetically double-counts C.1.1 first-pulse exemption (formula `S(N) = 0.6644 − 0.5644 × r^(N-1)` already embeds S1 first-pulse-full-magnitude in the −0.5644 coefficient; formula-derived figure `~159–162 s`); **B-CD18-3** (3-specialist convergence: systems-designer + performance-analyst + qa-lead) round-17 committed H.39e + H.39f normative-YAML forward obligations to F.2b but NOT recorded — same-session-update rule violation. All 5 round-17 closures (B16-1 through B16-5) and 6 IMPORTANT closures (I16-1+I16-9, I16-2, I16-4, I16-5, I16-11, I16-14) verified INTACT. **Defect surface CONVERGING** — BLOCKING down 40% from round-16 (5→3); defect class shifted from cross-section drift to arithmetic + registry-hygiene; round-15 four-layer protocol + round-17 two refinements ARE working at the convergence level. CD recommends **round-19 patches in single fresh `/clear` session** (S-scope; ≤2 hours; trivial-sweep first then arithmetic cluster then four-layer protocol gates); DO NOT escalate to specialist-pair-author method shift OR test-spec-first authoring at this stage; do NOT re-decompose. **DO NOT predict APPROVED for round-19** — sixth-consecutive-falsified-APPROVED-prediction rule applies. CD authored 3 new pattern memory files this session: `feedback_defect_class_shift_overrides_ceiling.md`, `feedback_three_specialist_convergence_elevates.md`, `feedback_agent_coverage_gap_not_defect.md`. **Earlier round-17 entry preserved below for traceability**: round-17 patch session APPLIED 2026-05-08 (chrusht-direct authoring + red-team gameplay-programmer subagent + cross-section consistency qa-lead subagent with round-17 per-canonical-name fanout refinement + static-analysis-first protocol layer all ran per `feedback_canonical_surface_name_fanout_check.md` + `feedback_mechanical_translation_ceiling.md` recommendations). All 5 round-16 BLOCKING items closed inline at the rule level (B16-1 F.2b T7 row count `14 → 15`; B16-2 cross-section name canonicalisation `Signal → Bindable` at F.4 row + H.39c (b-iii); B16-3 H.30c sub-AC (i) normative YAML block authored parallel to H.39d's; B16-4 DisturbanceService:KnitStart slot-write ordering rule with H.39c (b-vi) grep verification; B16-5 H.39e Lint (b) visited-set keying `(module-path, function-name)` pair). 6 of 15 IMPORTANT items closed inline (I16-1+I16-9 `SQUAD_T_FAIL_FLOOR_THRESHOLD = 0.20` published in G.5 + threshold-deferral note rewrite + F.2b PARTIALLY-CLOSED status; I16-2 F.2b registry refresh — 4 PC-rows IN-FLIGHT + R11-I1 narrative two-field encoding back-edit + FAIL-iv-server AUTO-INTEGRATION row; I16-4 H.41l step 7 IEEE-754 cell `~3e-8 → ~2.7e-6` with cancellation-amplification derivation + cumulative-bound rewording; I16-5 D.7 sprint-asymptote canonical `~165 s` only; I16-11 D.6 GetHottestHotspot signature `callerPosition: Vector3` parameter — F.1 violation removed at all 11 call-site references; I16-14 F.2b ADR row for module-scope `DisturbanceServiceType` alias). Static-analysis-first pass caught 4 unfixed `GetHottestHotspot` call-site references; closed in-session. **Round-11 red-team gameplay-programmer subagent surfaced 3 BLOCKING + 4 IMPORTANT defects** in the round-17 patches (B-RT17-1 H.39c (b-vi) grep depended on local-variable name `handshake` — closed via method-name-suffix grep independence; B-RT17-2 H.39c (b-vi) failure-mode rationale mechanically incoherent — closed via correction to handshake-module-slot vs service-internal-field; B-RT17-3 H.30c YAML first-push portability defect — closed via BASE_SHA fallback + conservative-default rule; I-RT17-2 I16-11 implicit Predator AI nil-guard obligation — closed via new F.2b R17-I-RT17-2 forward-obligation row; I-RT17-3 H.39c (b-iii) closure note self-contradiction — closed via rewrite acknowledging round-15 partial-close vs round-17 close; I-RT17-4 H.PB1-BETA threshold-calibration F.2b row missing R-prefix ID — closed via R17-I16-1 ID assignment; I-RT17-1 IEEE-754 step 7 magnitude REJECTED as red-team math error — `0.65 - 0.6597 = -0.0097`, not `0.0003` as red-team claimed). **Round-13 cross-section consistency qa-lead subagent surfaced 2 BLOCKING + 2 IMPORTANT defects** in the round-17 patches (B-CS-R17-1 prior-form name `getOnPredatorStateChangedSignal` survived in F.4 row body — closed via row rewrite removing bare prior-form name; B-CS-R17-2 F.4 row omitted cross-reference to F.2a row 3 step 4 binding ordering rule — closed via cross-reference addition; I-CS-R17-1 BeaconCapEvalEntry field-name mapping prose vs type alias — closed via mapping note in F.2b R11-I1 row; I-CS-R17-2 H.39c (b-iii) stale line-1449 cross-reference — closed via row-name-anchored cross-reference replacing line-number-anchored). New CD pattern memory file authored: `feedback_canonical_surface_name_fanout_check.md` per round-16 CD pattern recommendation. GDD grew ~2229 → ~2400 lines (+171 net round-17 patches). T7 prereq count remains at 15. **Round-18 verdict review pending in separate fresh `/clear` session via `/design-review`. DO NOT predict APPROVED for round-18** — sixth-consecutive-falsified-APPROVED-prediction rule applies; empirically expected APPROVED OR NEEDS REVISION (very small tail). If round-18 returns APPROVED with the round-15 four-layer protocol + round-17 two refinements applied, the protocol is canonicalised and the GDD advances to architecture phase per F.2a-IMPORTANT N-cluster forward obligations. If round-18 returns NEEDS REVISION (very small tail), escalate to specialist-pair-author method shift OR test-spec-first authoring per CD pattern memory.** Round-16 narrative preserved below for traceability: round-16 verdict review COMPLETE 2026-05-07 (full 7-spec panel — game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer, qa-lead — + creative-director synthesis). Specialist vote: 4 NEEDS REVISION / 3 APPROVED. CD-adjudicated 4 severity disagreements; final 5 BLOCKING + 15 IMPORTANT + ~3 NICE-TO-HAVE. Round-15 four-layer protocol DID work — three-spec convergence dropped 4→0; total BLOCKING dropped 8→5; pillar fidelity intact (Pillars 1+3 STRENGTHENED, 2+4 NEUTRAL). Player Fantasy DELIVERED with one calibration caveat. All 10 round-14 BLOCKING items confirmed closed at the rule level. Round-16 BLOCKING set is entirely new-surface (not regression of round-15 closures). 5 BLOCKING (post-adjudication): B16-1 F.2b T7 row count drift (registry says "14"; T7 narrative + actual prereq table say "15" — round-15 R14-B9 same-session-update rule violated; single-word fix at line 1369); B16-2 cross-section name drift on canonical 5-export surface — F.4 line 1449 + H.39c (b-iii) line 2093 say `getOnPredatorStateChangedSignal` while F.2a row 3 says `getOnPredatorStateChangedBindable` (CD-elevated to BLOCKING per cross-section partial-close pattern; round-15 B-CS-2 closed COUNT but not NAME on same surface; drift is INSIDE the AC body authored to BE the canonical list); B16-3 H.30c sub-AC (i) Primary CI gate prose-only — no YAML normative block parallel to H.39d's lines 1998–2067 (CD-elevated to BLOCKING per severity-floor rule for primary server-authoritative write seams — `:ReleasePredatorLock` is the primary write seam for predator-lock registry purge); B16-4 handshake intra-KnitStart ordering race — `setOnPredatorStateChangedHandler` queue-drain firing `:Connect` callbacks may execute before DisturbanceService:KnitStart writes its `self._onPredatorStateChangedBindable` slot; B16-5 H.39e Lint (b) call-graph traversal rule 5 visited-set keying granularity unspecified — naive impl keying by module-path causes false-negatives on shared utility modules. CD adjudications (4): (1) H.30c sub-AC (i) YAML — qa-lead IMP vs network-programmer BLOCKING: ELEVATED to BLOCKING per severity-floor rule. (2) `SQUAD_T_FAIL_FLOOR_THRESHOLD` deferred — qa-lead BLOCK-2 vs game-designer IMP: KEPT as IMPORTANT (round-15 R14-B9 binding deferral; AC contract closed; only constant value uncommitted; CD-recommended interim 0.20 lands in trivial-sweep). (3) F.4 getter name drift — gameplay-programmer IMP: ELEVATED to BLOCKING per cross-section partial-close pattern. (4) `loadedChunks` safe-API gap — network-programmer BLOCKING: KEPT as DEFERRED-ADR-CONCERN, NOT a fresh round-16 BLOCKING (F.4 row 5 BLOCKED-on-ADR for 13 rounds with proper F.2b tracking; candidate API names belong in ADR's first revision). CD pattern memory (round-16): the round-15 four-layer protocol (mechanical-translation + self-stress + red-team + cross-section consistency + static-analysis-first) is empirically working at the convergence level (3-spec convergence dropped 4→0) but is NOT yet sufficient to reach APPROVED; two new defect classes emerged at round-16 — cross-section name-drift INSIDE the enforcement AC itself (B16-2), and YAML-vs-prose normative gap on primary write seams (B16-3); NO sixth protocol layer recommended; instead two refinements to existing layers — cross-section consistency pass adds per-name-fanout assertion (not just per-section count); static-analysis-first layer adds normative-block parallel-structure check; method-shift (specialist-pair-author or test-spec-first authoring) NOT yet warranted because empirical defect surface is shrinking; recommended new pattern memory file `feedback_canonical_surface_name_fanout_check.md`. Round-17 patch session pending in separate fresh `/clear` session — trivial-sweep first (B16-1 + I16-1 SQUAD_T 0.20 interim + I16-4 IEEE-754 cell + I16-5 D.7 reconcile + I16-2 registry refresh) then heavy patches (B16-2 name canonicalisation + B16-3 H.30c YAML block + B16-4 KnitStart ordering + B16-5 visited-set keying + I16-11 D.6 callerPosition signature + I16-14 module-scope type alias) + four-layer protocol with two refinements. ~1.5–2 hours estimated. **DO NOT predict APPROVED for round-17** — sixth-consecutive-falsified-APPROVED-prediction rule applies. If round-18 still returns NEEDS REVISION, escalate to specialist-pair-author method shift OR test-spec-first authoring per CD round-15 active.md plan.** Round-15 narrative preserved below for traceability: **In Review — round-15 heavy cluster patches APPLIED 2026-05-07 (this session, fresh `/clear`; chrusht-direct authoring + red-team gameplay-programmer subagent + cross-section consistency qa-lead subagent + NEW round-15 static-analysis-first protocol layer all ran per `feedback_mechanical_translation_ceiling.md` recommendation; all 10 round-14 BLOCKING heavy-cluster items + 6 red-team findings + 3 cross-section findings closed inline at the rule level). Round-16 verdict review pending in separate fresh `/clear` session via `/design-review`. DO NOT predict APPROVED for round-16 — fifth-consecutive-falsified-APPROVED-prediction rule applies; empirically expected APPROVED OR NEEDS REVISION (very small tail).** Round-15 heavy closures: B1 H.39e Lint (b) call-graph reachability extension + table-dispatch rule 2(d); B2 register-time-subscribe inside the handshake module's `RegisterPredatorStateChangedSignal` accessor body (folded into B4); B3 H.39f raw-RemoteEvent OnServerEvent lint alias-resolution forms (i)–(v); B4 module-private accessor architectural relocation to `src/server/services/DisturbanceService/RegistrationHandshake.luau` (5 canonical exports: `RegisterPredatorLockChangedSignal`, `RegisterPredatorStateChangedSignal`, `setOnPredatorStateChangedHandler`, `getOnPredatorLockChangedSignal`, `getOnPredatorStateChangedBindable`); B5 H.41l sub-test (a) ±0.001 IEEE-754 binary32 forward error propagation derivation with Goldberg/Higham/IEEE-754-2008 citation chain; B6 `BeaconCapEvalEntry` two-field encoding (`cap_engaged: boolean` + `scaleFactor: number`; round-13 `-1.0` sentinel REMOVED; field renamed `beaconScaleFactor` → `scaleFactor`); B9 H.PB1-BETA criterion (iv) server-side FAIL telemetry contract + new `_setExtendedStationarySegmentActive` test seam + cached `_cachedPredatorState` read-source contract; B10 H.30c sub-AC (i) Primary CI gate trigger path filter recursive; B11 H.36 PASS criterion explicit scope clause + F.6 Crafting-MAX_BEACON cross-GDD mandate; B12 new F.2b — Forward-Obligations Registry section (8 receiving-GDD tables). R13-I1, R13-I2, R13-I4 marked CLOSED inline. R11-I5 marked PARTIALLY CLOSED. New round-15 IMPORTANT cluster forward obligations: R15-I1, R15-I2, R15-I3 (AST-tooling + H.39c grep extension). Red-team subagent surfaced 3 BLOCKING + 3 IMPORTANT, all closed inline (B-RT-1 handshake-module export-list inconsistency between F.2a row 3 + H.39c (b-iii); B-RT-2 `setOnPredatorStateChangedHandler` unreachable code path; B-RT-3 `_setExtendedStationarySegmentActive` seam not added to H.39c + prereq table + T7 count; I-RT-1 call-graph table-dispatch rule; I-RT-2 read-site lexical-scope clarification; I-RT-3 predator_state cached-read source contract). Cross-section subagent surfaced 2 BLOCKING + 1 IMPORTANT, all closed inline (B-CS-1 `beaconScaleFactor`→`scaleFactor` rename across H.41h, H.41i, H.41f, H.41m, H.36a, D.7, H.41l; B-CS-2 H.39c (b) header + R14-B2 rationale tail count to "five exports"; I-CS-1 C.3.4 step 2 cache description rename). Static-analysis-first pass ran clean (no defects requiring fix before subagent gates). T7 count incremented 14 → 15 (per round-13 same-session-update rule applied to the round-15 R14-B9 `_setExtendedStationarySegmentActive` prereq addition). GDD grew ~1966 → ~2300 lines (+334 net). **Round-15 trivial-sweep entry preserved below for traceability:** Round-15 trivial-sweep APPLIED 2026-05-07 (prior session, fresh `/clear`). Round-15 trivial-sweep closed 2 of 12 round-14 BLOCKING items (B7 closure-`self` type annotation on `registerDeathHandler` helper inside `DisturbanceService:KnitInit` — explicit `local service: typeof(DisturbanceService) = self` capture so the inner `:_onHumanoidDied` invocation type-checks under `--!strict`; B8 explicit `cap_engaged` + `beaconScaleFactor` reads from per-beacon `BeaconCapEvalEntry` cache at the head of the C.3.4 step-3 pseudocode block — closes the round-14 undeclared-free-variable defect that broke implementer copy-paste), the metadata NICE-TO-HAVE (status header refresh from round-11 wording to round-15 wording), and the audit-trail directory closure (`.claude/agent-memory/creative-director/` directory created on disk with 5 files: `MEMORY.md` index + 4 pattern memory files — `feedback_cross_section_consistency_pass_partial_close.md` (round-13 protocol layer DID work for cross-section coherence; round-14 ceiling shifted to single-section implementability micro-defects); `feedback_mechanical_translation_ceiling.md` (L-scope-late-rounds GDD ceiling pattern + four-consecutive-falsified-APPROVED-prediction record + round-15+ static-analysis-first authoring layer recommendation); `feedback_severity_floor_for_server_authoritative_seams.md` (auto-promote IMPORTANT to BLOCKING when finding lands on primary write-side seam of server-authoritative system); `feedback_metadata_severity_demotion.md` (high specialist convergence on metadata defects ≠ inherited BLOCKING; severity assessed against load-bearing impact). The audit-trail closure addresses the defect that prior round logs from round-8 onward referenced these files as updated when the directory itself did not exist on disk. **Round-15 heavy cluster STILL PENDING (10 BLOCKING items)**: B1 H.39e Lint (b) call-graph scope (lint must operate on call graph reachable from `Humanoid.Died` handler chain, not just the callback body); B2 KnitStart inter-service ordering race for `OnPredatorStateChanged` BindableEvent; B3 H.39f raw-RemoteEvent OnServerEvent lint alias-resolution gap + R13-I tracking entry; B4 H.39c local-alias-to-Client-method accessor injection closure for `RegisterPredatorLockChangedSignal` / `RegisterPredatorStateChangedSignal`; B5 H.41l sub-test (a) ±0.001 tolerance IEEE-754 single-precision derivation; B6 `beaconScaleFactor = -1.0` sentinel read-site audit OR sum-typed `BeaconScale = Active(number) | Inert` replacement; B9 R13-I1 server-side FAIL discriminator for H.PB1-BETA criterion (iv); B10 R13-I4 path-filter for `:ReleasePredatorLock` primary write seam; B11 H.36 PASS scope-binding to bound the Crafting GDD `activeBeacons > 3` threat vector; B12 forward-obligation registry section. Heavy session protocol per `feedback_mechanical_translation_ceiling.md` and round-13 plan: red-team gameplay-programmer subagent (round-11 protocol) + cross-section consistency qa-lead subagent (round-13 protocol) + **NEW round-15 static-analysis-first authoring pass** (run lint/typecheck against pseudocode BEFORE submission to subagents). **Round-14 entry preserved below for traceability:** NEEDS REVISION (round-14 design-review complete 2026-05-07; verdict matches CD pattern memory's predicted NEEDS REVISION small tail — fourth consecutive non-APPROVED-prediction outcome that matched).** Full 7-spec panel (game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer, qa-lead) + creative-director synthesis. **12 BLOCKING (CD-adjudicated from raw 15 via 6 adjudications) + ~25 IMPORTANT + ~6 NICE-TO-HAVE.** Defect class is **single-section implementability micro-defects** (lint scope, type annotations, sentinel safety, undeclared free variables, alias-resolution gaps) — NOT cross-section coherence defects. Round-13's NEW cross-section consistency pass DID work for its target class structurally; the round-14 ceiling is a different ceiling. Protocol is converging, not failing — DO NOT yet escalate to specialist-pair-author method shift. **12 BLOCKING items**: B1 H.39e Lint (b) scope gap (ai-programmer + qa-lead — same defect class); B2 KnitStart inter-service ordering race for OnPredatorStateChanged (ai-programmer); B3 H.39f raw-RemoteEvent OnServerEvent lint alias-resolution gap, no R13-I tracking entry (network-programmer); B4 H.39c local-alias-to-Client-method accessor injection bypass for predator-lock signal accessors (network-programmer); B5 H.41l sub-test (a) ±0.001 tolerance unanchored (systems-designer); B6 `beaconScaleFactor = -1.0` sentinel unsound without exhaustive read-site audit (systems-designer); B7 `registerDeathHandler` closure `self` upvalue type unclear under `--!strict` (gameplay-programmer); B8 `cap_engaged` undeclared free variable in C.3.4 step 3 pseudocode (gameplay-programmer); **B9 R13-I1 server-side FAIL condition for H.PB1-BETA criterion (iv) deferred — CD-elevated to BLOCKING (game-designer + qa-lead convergent: existence vs threshold deferral; without server-side discriminator, Pillar 1 verification cannot discriminate honest pass from Hawthorne-effect false-pass)**; **B10 R13-I4 path filter gap covering `:ReleasePredatorLock` primary write seam — CD-elevated to BLOCKING (network-programmer + qa-lead convergent; honor-system on server-authoritative write path violates project Forbidden Patterns rule)**; B11 H.36 PASS criterion scope-unbound (R13-I2 deferral; Crafting GDD round-1 NEEDS REVISION is live threat vector for `activeBeacons > 3`) (performance-analyst); **B12 Forward-obligation registry required — CD-consolidated from game-designer B-1 + B-2 + qa-lead I-14-6 (27+ forward obligations across HUD/PC/Crafting with no commitment-tracking surface; T7 binds 14 prereqs but no parallel commitment for forward obligations)**. **CD adjudications**: status header staleness (7-of-7 specialists noticed; 3 BLOCKING vs 4 IMPORTANT/NICE-TO-HAVE) → demoted to NICE-TO-HAVE per NEW pattern `feedback_metadata_severity_demotion.md` (convergence on noticing ≠ convergence on severity); R13-I1 → BLOCKING; R13-I4 → BLOCKING per NEW pattern `feedback_severity_floor_for_server_authoritative_seams.md`; H.39e Lint (b) scope same defect class (kept BLOCKING). **Pillar fidelity (CD)**: Pillar 1 CONDITIONAL/WEAKENED (B9 closure restores STRENGTHENED); Pillar 2 NEUTRAL; Pillar 3 STRENGTHENED (ED rules-level mechanics intact); Pillar 4 NEUTRAL with concern (no mechanical expression). **CD pattern memory observations recommended for persist**: (1) NEW `feedback_cross_section_consistency_pass_partial_close.md` — round-13 protocol layer DID work for cross-section coherence; round-14 ceiling is implementability micro-defects (different ceiling); protocol converging; (2) UPDATE `feedback_mechanical_translation_ceiling.md` (file does NOT exist on disk despite prior round logs' claims — recommend creation); (3) NEW `feedback_severity_floor_for_server_authoritative_seams.md`; (4) NEW `feedback_metadata_severity_demotion.md`; (5) AUDIT-TRAIL FIX — create `.claude/agent-memory/creative-director/` directory and persist files. **Round-15 plan**: trivial-sweep FIRST in dedicated mini-session (B7, B8, status header, audit-trail integrity; ~30-45 min); heavy session second (9 remaining BLOCKING by cluster: lint/scope (B1, B3, B4); type/sentinel (B5, B6); structural (B9, B10, B11, B12); concurrency (B2); ~3-4 hours); self-stress + red-team subagent + cross-section consistency subagent + **NEW static-analysis-first authoring pass** (the round-15 protocol layer recommended by round-14 finding pattern). Round-16 verdict review in another fresh session. DO NOT predict APPROVED. If round-16 returns NEEDS REVISION again with implementability micro-defects, escalate to **test-spec-first authoring** (untried). **Round-13 entry preserved below for traceability:** **Round-13 PATCHES APPLIED 2026-05-07 — round-14 verdict review COMPLETED 2026-05-07; verdict NEEDS REVISION small tail.** All 8 round-12 BLOCKING items (R12-B1..R12-B8) closed in chrusht-direct authoring per CD-recommended sequence (B1 first to avoid cascade with B6; B6 second; B3+B4 paired; B5 split into static + runtime; B2 prereq-table seam + H.39c expansion + T7 count bumped 13 → 14; B7 symmetric IEEE-754 tolerances; B8 `--!strict` nil-guard + sentinel `-1.0` + `cap_engaged` definition). 10 of 15 IMPORTANT items folded inline (I2, I3, I4, I7, I8, I9, I10, I11, I13, I15); 5 preserved as F.2a-IMPORTANT R13-I1..R13-I5 forward obligations (I1 PB1-BETA Profile C floor, I5 H.36 scope clause, I6 DeathFrame PERF-DEVICE AC, I12 path filter expansion, I14 decay-trail UI obligation). **Red-team gameplay-programmer subagent ran post-patch per round-11 protocol** — caught 2 BLOCKING + 4 IMPORTANT additional defects (B1: (5-LC-a) re-subscription idiom produced 2 source-level `humanoid.Died:Connect` call sites breaking H.39e Lint (a) cardinality — closed via single-helper refactor; B2: H.30c sub-AC (i) retained stale "(except as the tail-call exception)" parenthetical referencing an exception R12-B1 abolished — closed via removal; I1: State A nil-guard rationale was misleading — closed via comment rewrite; I2: F.6 PC-T5 mandate cross-reference to F.2a row 3 naming-collision note added; I3: H.41l sub-test (c) `beacon_native_at_B1` disambiguation added; I4: T7 enumeration name standardisation across prereq table). **NEW for round-13: cross-section consistency pass (qa-lead subagent) ran AFTER red-team self-review** per the round-12 plan addition — caught 0 BLOCKING + 2 IMPORTANT (I-CS-1: E.23 same-frame ordering MUST without H.39h verification path — closed via explicit forward-obligation deferral annotation; I-CS-2: GetOnPlayerDiedSignal return-type ambiguity — closed via explicit Luau type signature annotation in F.4 row). All red-team + cross-section findings closed in-session. GDD grew ~1823 → ~2050 lines (+227 net round-13 patches). **Round-12 entry preserved below for traceability:** **MAJOR REVISION NEEDED (round-12 design-review complete 2026-05-07)** — full 7-specialist adversarial panel + creative-director synthesis. All 7 specialists returned NEEDS REVISION. **8 BLOCKING (R12-B1..R12-B8) + 15 IMPORTANT.** 4 of 8 BLOCKING are 3-spec convergent: R12-B1 C.1.11 step 5c `task.spawn` MUST contradicts H.39e Lint (b) `task.spawn` ZERO-occurrences ban (3-spec: systems-designer + network-programmer + gameplay-programmer); R12-B2 `_resetCapCueDecayPassesRemaining` test seam mandated by H.41m sub-AC (d) but not added to prereq table or H.39c grep — T7 count drift (3-spec: systems-designer + qa-lead + performance-analyst); R12-B5 H.30c sub-AC (h) reframed to lint-only static check is structurally tautological — non-trivial runtime ordering invariant lost (3-spec: ai-programmer + qa-lead + network-programmer); R12-B6 `_deathLockSnapshot` double-death/respawn semantics + re-subscription on CharacterAdded unspecified in C.1.11 step 5 (3-spec: network-programmer + qa-lead + ai-programmer). 2-spec: R12-B3 OnPredatorLockChanged acquisition path ambiguous + F.1 contradiction (ai-programmer + network-programmer). 1-spec: R12-B4 DR-4 monopoly violated by Player Controller GDD T5 + Crafting GDD C.8 cross-GDD propagation gap; R12-B7 H.41l sub-test (a) and (b) one-sided IEEE-754 tolerance windows; R12-B8 State C `beaconScaleFactor = nil` violates `--!strict` in C.3.4 step 3. **CD synthesis (persisted as agent memory update at `feedback_mechanical_translation_ceiling.md`)**: round-11's red-team-intercept innovation correctly closed the section-local authoring-fidelity defect class that broke round-10 (intercept caught 3 BLOCKING + 5 IMPORTANT in-session). However round-12 surfaced a NEW defect class the section-scoped red-team did not catch: **cross-section coherence defects** — co-authored prose-and-AC pairs (e.g., C.1.11 step 5c prose vs H.39e Lint (b) AC; H.41m sub-AC (d) seam mandate vs prereq table omission) that each pass section-local review independently while contradicting each other when read in sequence. THIS IS THE THIRD CONSECUTIVE FALSIFIED APPROVED-OR-NEAR-APPROVED PREDICTION (round-7c→8, round-9→10, round-11→12). The mechanical-translation method has a structural ceiling that progressively-stronger interventions partially close but do not eliminate. **CD verdict: MAJOR REVISION NEEDED (scope M).** Round-13 plan: separate fresh `/clear` session; chrusht-direct authors all 8 R12-B patches in CD-recommended sequence (B1 first to avoid cascade with B6 respawn-semantics; B3+B4 paired; B5 split sub-AC into static + runtime; B2 prereq table + H.39c update; B7+B8 last); after authoring, run red-team gameplay-programmer subagent (per round-11 protocol); **then run a SECOND subagent (qa-lead recommended) for cross-section consistency pass** — single instruction: "for every newly-authored prose passage and every newly-authored AC, identify any pair where reading both in sequence produces a contradiction or an unenforceable rule." Round-14 verdict review pending after round-13 patches land. If round-14 fails, escalate to specialist-pair-author method shift. **Earlier round-11 entry preserved**: Round-11 heavy patches APPLIED 2026-05-07 (fresh `/clear` per round-10 CD authoring-method-shift recommendation). All 6 round-10 BLOCKING items closed; 3 multi-spec convergent IMPORTANT clusters absorbed (covering 9 of 17 round-10 IMPORTANT findings); 8 single-spec IMPORTANTs preserved as F.2a-IMPORTANT R11-I1..R11-I8 forward obligations. Two batched design rulings landed at session start on CD-recommended options: **DR-4 = Option B** (BindableEvent-mediated dispatch — DisturbanceService is sole `Humanoid.Died` subscriber + owns `OnPlayerDied` BindableEvent dispatch; supersedes round-9 KnitInit-vs-KnitStart connection-order rule that rested on undocumented Roblox `:Connect` ordering); **DR-5 = Option A** (author H.39e CI lint AC — closes round-10 cross-reference orphan; H.39e enforces three lints: Humanoid.Died subscription monopoly, OnPlayerDied:Connect callback yield-primitive ban, OnPredatorLockChanged server-side fire-path observer ban). 6 BLOCKING patches: B-R10-1 C.1.11 fire-ordering family with three sub-findings via DR-4 = Option B BindableEvent monopoly architecture + sub-step 4b sync-callback prohibition + E.23 same-frame Fire(true)+Fire(false) ordering contract; B-R10-2 C.3.4 State C discriminant strict if/elseif/else + nil-on-State-C populations unless persistence rule fires; B-R10-3 `_capCueDecayPassesRemaining` cadence-derived `math.ceil(1.0 × FIELD_UPDATE_HZ)`; B-R10-4 H.41l worked-example clean-up with three sub-tests at d=4/23.9/24.1 studs; B-R10-5 author H.39e CI lint AC; B-R10-6 sub-stud-tolerance source-placement helper added to prereq table + T7 count bumped 8 → 13. **Red-team gameplay-programmer subagent ran post-patch per CD authoring-method-shift recommendation** — caught 3 BLOCKING + 5 IMPORTANT defects (stale KnitInit-vs-KnitStart reference in DeathAttributionPayload prose; H.30c sub-AC (e) ordering predicate ambiguity vs synchronous BindableEvent semantics; H.39e lint (b) tail-task.spawn AST-recognition AC-testability defect; OnPlayerDiedPayload type alias missing; H.41l sub-test (a) no explicit ± tolerance; H.41m sub-AC (d) test-isolation reset; H.30c sub-AC (h) AUTO-UNIT-grep mislabel; H.30c sub-AC (j) no negative failure-case sub-test) — ALL 8 fixed in same session BEFORE round-12 panel submission. GDD grew ~1660 → 1823 lines (+163 net round-11 patches). Per round-10 CD pattern memory at `.claude/agent-memory/creative-director/feedback_mechanical_translation_ceiling.md`: **DO NOT predict APPROVED for round-12.** The mechanical-translation method has documented ceiling for L-scope GDDs at round 7+; round-12 may surface NEEDS REVISION (small tail) and that outcome is empirically expected. The 8 single-spec IMPORTANT forward obligations (R11-I1..R11-I8) may surface as round-12 BLOCKING if their underlying defect classes prove load-bearing on the round-11 architectural pivot. Earlier round-10 entry preserved below for traceability: round-10 design-review complete 2026-05-06 — verdict NEEDS REVISION (round-9's prediction of round-10 APPROVED falsified). Full 7-specialist panel (game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer, qa-lead) + creative-director synthesis. 6 of 7 specialists returned NEEDS REVISION; only performance-analyst returned APPROVED (PA confirmed budget arithmetic ~140× headroom — domain-correct but did not look for the defect classes that broke this round; CD adjudicated PA's verdict as scope-correct but rejected the document-overall signal). 6 BLOCKING items (3 multi-specialist convergent) + 17 IMPORTANT + 5 NICE-TO-HAVE. Defect surface shifted from integration/implementability to authoring-fidelity defects — editorial residue, cross-reference orphans, pseudocode evaluation-order ambiguity, cadence-hardcoded constants, unverified Roblox API guarantees, missing prereq table entries. Most consequential: 3-spec convergent fire-ordering family on `Humanoid.Died` — Roblox `:Connect` fire-order on a HIGH-risk auto-updating live platform is unverified and is load-bearing for C.1.11 step 5 / H.30c sub-AC (h). Other BLOCKING: C.3.4 State C discriminant under-specified two ways (populates `nearby`/`stationary_nearby` when cap never engaged → HUD misreads "we're capping it"; pseudocode evaluation order not enforced); `_capCueDecayPassesRemaining` hardcoded to 5 instead of `math.ceil(1.0 × FIELD_UPDATE_HZ)` — breaks F.2a row 7 persistence at non-default cadence; H.41l contains literal authoring residue ("wait this is too low; let me revise") in normative AC body; H.39e referenced from C.1.11 step 5 but AC body absent from Section H (cross-reference orphan); H.41l implicitly requires R9-I6 sub-stud placement helper which has no story filed and is missing from prereq table. CD pattern observations #1 (rounds 7+ require full 7-spec panel) and #2 (mechanical-translation needs self-stress) BOTH validated again — qa-lead "wait this is too low; let me revise" smoking-gun proves round-9's self-stress was not actually run on H.41l. **NEW pattern persisted as agent memory** at `.claude/agent-memory/creative-director/feedback_mechanical_translation_ceiling.md`: TWO consecutive mechanical-translation patch sessions on this GDD have falsified APPROVED predictions (round-7c → 8 fail; round-9 → 10 fail). Mechanical-translation method has a ceiling for L-scope GDDs at round 7+; defect surface has shifted to authoring-fidelity which requires an out-of-author reviewer. CD verdict: NEEDS REVISION (not MAJOR REVISION; 6 BLOCKING items individually narrow-scope with clear local fixes; structural problem is the *authoring method* not the design). **Round-11 plan: authoring method shift** — fresh `/clear` session; batched AskUserQuestion design rulings up-front (DR-4 Roblox `:Connect` strategy — Option B BindableEvent-mediated dispatch recommended; DR-5 H.39e — Option A author-in-Section-H recommended); chrusht-direct authors 6 BLOCKING patches per focused punch list; **before submitting to round-12 panel** spawn ONE fresh subagent (gameplay-programmer recommended) for red-team review — "What did the author miss? Look for editorial residue, cross-reference orphans, arithmetic incoherence, AC wording defects." DO NOT predict APPROVED for round-12. Earlier round-9 entry preserved below for traceability: round-9 heavy patches APPLIED 2026-05-06 closed all 21 round-8 BLOCKING items across 4 convergence clusters + 9 novel singletons via mechanical translation (3 batched design rulings DR-1/DR-2/DR-3 all landed on CD-recommended options). Round-9 closed all 21 round-8 BLOCKING items across 4 convergence clusters + 9 novel singletons via mechanical translation (3 batched design rulings DR-1/DR-2/DR-3 all landed on CD-recommended options): DR-1=A extended `MeterUpdatePayload` with `beaconCapState`/`nearby`/`stationary_nearby` optional fields + new C.3.4 cap-state discriminant compute rules + new H.41m AC; DR-2=B added co-location amendment to C.3.6 step 6 (when 2+ beacons within INFLUENCE_RADIUS, other-beacon contribution INCLUDED in `non_beacon_contribution_at_B` denominator) + new H.41l AC; DR-3=B moved `OnPredatorLockChanged:Fire(false)` ownership from Predator AI into `DisturbanceService:ReleasePredatorLock` body with sub-steps (4a)/(4b)/(4c) yield-free contract + new C.1.11 step 5 codifying `Humanoid.Died` subscription ownership (DisturbanceService MUST subscribe in KnitInit) + new H.30c sub-ACs (e)–(i) covering during-purge ordering + connection-order determinism (100-trial harness) + concurrent task.spawn invariant. 4 new test seams added to Test Infrastructure Prerequisites table (`MockOrderingRecorder`, `_runFieldUpdatePass`, `_setRingBufferState`, characterless-player harness mechanism); H.36a profile-fence scope expanded to enumerate 9 distinct workloads; H.36 PERF-DEVICE expanded to TWO fixture variants (Beacon-inactive baseline + Beacon-active 3-beacon fixture). 9 novel singletons closed: B-NS1 co-location amendment; B-NS2 solo-with-under-filled-buffer accept-as-designed + new H.41n AC; B-NS3 grep-pattern expansion to four syntactic forms in H.39/H.39b; B-NS4 H.39d CI YAML trigger expansion (`pull_request` + `push: branches: [main]`); B-NS5 INVESTIGATE_FALLBACK_DURATION = 8.0s in G.6; B-NS6 `--!strict` nil-guard idiom for Character.PrimaryPart in C.3.6 step 4 + new H.41o AC; B-NS7 new H.PB1-BETA criterion (iv) for session-arc closure against permanent-Calm attractor; B-NS8 E.25 discontinuity-detection cadence at field-update (5Hz) folded into H.36a workload 8; B-NS9 covered by Cluster 3 / H.41m. 30 IMPORTANT items consolidated as F.2a-IMPORTANT R9-I1..R9-I10 forward obligations (with 20 absorbed into BLOCKING patches). Round-9 grep-sweeps verified clean: `GetLockedTargetedPlayers` only in negation contexts; `beaconCapState` consistent across C.3.4/F.2a row 7/H.41m; `INVESTIGATE_FALLBACK_DURATION` consistent across F.2a row 1/F.2a-IMPORTANT F6/G.6; co-location rule consistent across C.3.6 step 6/H.41i/H.41l; nil-guard idiom consistent at C.3.6 step 4. Earlier round-8 review entry preserved below for traceability. ALL 17 of 17 round-7 BLOCKING items closed (4 trivial-sweep + 13 heavy-patches). 4 design rulings landed: D1 = Option B `> 0.50 of nearby` (denominator `connected` → `nearby` at C.3.6 step 5; H.41g regression-closure AC); D2 = cumulative-distance redesign (C.3.6 step 4 sampled-boolean → `cumulativeXZ` integral; new tunable `BEACON_CUMULATIVE_DISTANCE_THRESHOLD = 6.0 studs`); D3 = split-channel (`OnPredatorStateChanged` BindableEvent server-internal owned by Predator AI for defense-in-depth purge + `OnPredatorLockChanged` Knit RemoteSignal client-facing for HUD; circular-dep eliminated at C.1.11 + E.23 + F.2a row 3); D4 = pair-check (PA + game-designer parallel re-derivation produced ~40 lower-bound, NOT round-5's ~103 NOR PA-F1's ~51 — both prior figures shown to be arithmetic errors). 26 IMPORTANT items consolidated as binding forward obligations in new F.2a-IMPORTANT section + 3 directly applied (HotspotResult prose "all four" → "all five"; F.2a row 7 HUD MAY → MUST cap-cue; Section B 4th-bullet (c) chunk-stream-in pre-disturbed zone fantasy-edge). Other significant closures: R7-B5 beaconScaleFactor 3 sub-modes (typical / ambient>cap / beacon-near-floor) documented in D.7 + E.25 + new H.41f/h ACs; R7-B7 RegisterPredatorLock merge+grace-window semantics replaces replace-on-re-register at C.1.11 + E.23 + new G.4 knob `PREDATOR_LOCK_RELEASE_GRACE_WINDOW = 5.0 s`; R7-B10 topContributorsAt empty-vs-non-empty contract codified at D.6 + new H.25b AC; R7-B11 NaN/Infinity canonical idiom `n == n and n ~= math.huge and n ~= -math.huge` codified at E.16 + H.3b; R7-B12 `_injectSyntheticEmission` upper bound (0, 1.05] → (0, 1.00]; R7-B13 H.31c three-independent-invocations rewrite; R7-B14 H.32d server-observable membership-set transitions only; R7-B15 H.41e facilitator-directed second session; R7-B16 H.39d CI grep gate (full GitHub Actions YAML embedded) replaces honor-system PR-template-checklist; R7-B17 H.36 lower-bound 103 → ~40 with full pair-check derivation. GDD grew 1366 → 1522 lines (+156 net). Mandatory whole-document grep-sweeps clean before close-out (EmissionType 5-value enum at all 13+ sites consistent; (0, 1.05] zero matches; cell-key encoding aligned at D.4 + D.6; split-channel documented at all 3 architecture sites; MAGNITUDE_BEACON / RETREAT_THRESHOLD / reserved-enum-slot consistent). Round-7c heavy patches APPLIED 2026-05-06 (separate fresh session) closed all 5 round-7b BLOCKING + 3 NC + 4 IMPORTANT directly + 5 IMPORTANT consolidated + 6 grep-sweeps verified clean + 2 new ACs (H.41j, H.41k) + 1 expanded AC (H.30c sub-AC e/f/g). GDD 1522 → 1557 lines (+35 net). Round-7c predicted round-8 APPROVED. **Round-8 design-review complete 2026-05-06 — verdict MAJOR REVISION NEEDED (round-7c prediction falsified).** Full 7-specialist panel (game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer, qa-lead) + creative-director synthesis. **All 7 specialists returned NEEDS REVISION.** ~21 BLOCKING items across 4 convergence clusters + 9 novel singletons. **Convergence Cluster 1 (5-way: ai-programmer + gameplay-programmer + systems-designer + network-programmer + qa-lead, with game-designer N8-2 echoing) — C.1.11 ordering rule / missing API contract**: step 4 mandates "Predator AI computes the to-be-cleared targetedPlayer set from the registry's pre-purge state" but no `GetLockedTargetedPlayers(predatorId)` accessor declared on DisturbanceService — step 4 contractually unimplementable; `Humanoid.Died` connection-order race (Knit init non-deterministic, same pattern PC GDD round-4 was BLOCKING for); concurrent-task race against step 1 sampling (Predator AI re-queries from `task.spawn` between Heartbeats); BindableEvent inbound race from Predator AI side (`OnPredatorStateChanged` synchronous on server; ED only constrains its own code path); H.30c sub-AC (g) not deterministically reproducible (yield-free guarantee not codified). **Convergence Cluster 2 (3-way: gameplay-programmer + qa-lead) — H.41j / H.41k test-seam gaps**: no `_runFieldUpdatePass()` seam to drive deterministic Heartbeat advance for sub-test (e); MockOrderingRecorder for H.30c sub-AC (e) named but not added to prereq table (T7 sprint-entry gate breaks silently); ring-buffer manipulation seam missing; H.41j sub-test (a) harness mechanism unspecified. **Convergence Cluster 3 (2-way: game-designer + performance-analyst) — HUD data-surface gap**: F.2a row 7 mandates HUD render 3 visually-distinct cap states but `OnMeterUpdate` carries only `playerT` and `squadT`; State B diagnosis requires `beaconScaleFactor` clamp-to-zero + `non_beacon_contribution_at_B > BEACON_STATIONARY_CAP_FIELD_VALUE` neither of which is in any pushed payload — GDD mandates HUD behaviour without mandating the data contract that enables it. **Convergence Cluster 4 (2-way: performance-analyst + qa-lead) — H.36/H.36a profilebegin scope stale**: round-7c added per-player ring-buffer scan + per-beacon cap-state evaluation; H.36a's scope wording (line 1342) was NOT updated; H.36 PERF-DEVICE never measures the cap path because the test fixture has no Beacon-active variant. **9 Novel singletons**: B-NS1 co-located beacons within INFLUENCE_RADIUS defeat cap-Hunt-floor intent (H.41i avoids testing this); B-NS2 solo-nearby player with under-filled buffer makes cap unreachable; B-NS3 H.39/H.39b grep misses assignment-form RemoteFunction declarations; B-NS4 H.39d CI gate bypassable on direct push to main (branch-protection assumption non-normative); B-NS5 H.30c bootstrap path cannot be verified without `INVESTIGATE_FALLBACK_DURATION` (not in G.6); B-NS6 `--!strict` nil-guard on `Character.PrimaryPart` missing from C.3.6 step 4 ring-buffer write contract; B-NS7 R7c-I1 permanent-Calm attractor unenforced (fully-stationary squad settles at ~0.272, just below Tense 0.30; H.PB1-BETA was NOT amended despite forward obligation); B-NS8 E.25 discontinuity-detection cadence unspecified; B-NS9 new AC needed for three-state cap-cue server-side compute/push. **CD synthesis (persisted as agent memory)**: (1) **The held-out 2-specialist protocol drifts by GDD review-round maturity** — rounds 1–3 game-designer + systems-designer covers design-surface; rounds 4–6 need 3-spec ai-programmer + network-programmer + qa-lead for integration; **rounds 7+ require full 7-spec panel — no held-out**. Round-7b's 2-spec held-out passed cleanly because defects had shifted to integration / implementability INVISIBLE to the design-surface roster by construction. (2) **Mechanical-translation patch sessions are self-validating, not self-stressing** — round-7c authored mechanically-correct prose patches that satisfied round-7b's prescriptive fix directions but did NOT test their own edge cases (H.41i avoided beacon co-location; H.41k didn't test solo-nearby-with-under-filled-buffer; F.2a row 7 mandated 3-state HUD without authoring the data contract). Mechanical-translation rounds need an explicit self-stress pass before predicting next-round APPROVED. (3) Round-7c's prediction of round-8 APPROVED was structurally optimistic — not author error, category blind spot. **CD verdict: MAJOR REVISION NEEDED. Scope L.** Path forward: round-9 fresh-session patch via batched AskUserQuestion design rulings up-front (HUD data-contract path A vs B; co-location accept-as-designed vs new cap rule; step-4 ownership) followed by chrusht-direct mechanical translation OR specialist subagents for Cluster 1 (ai-programmer + network-programmer pair-check). After round-9 patches land, round-10 `/design-review` for verdict. — round-7 fresh-session re-review with 7 specialists (game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer, qa-lead) + creative-director senior synthesis. **17 BLOCKING + 26 IMPORTANT + 11 NICE-TO-HAVE.** GDD was unchanged since round-5 revisions, so all round-6 unresolved items remain live AND round-7 surfaced new defects. Multi-specialist convergences (binding evidence): **5-specialist on EmissionType enum stale at 6 locations** (game-designer found a 6th location at line 321 that round-6 missed: "up to 4 future Crafting types"); **4-specialist on `OnPredatorLockChanged` circular dependency** (DisturbanceService cannot subscribe server-side to its own client-facing Knit RemoteSignal — purge silently never fires, archive TTL leak, 95% MVP gate at risk); **3-specialist on `beaconScaleFactor` failure modes** (3 distinct sub-modes: ambient>cap clamps to 0/field stays above cap; denominator-zero on decayed beacon; non_beacon value source underspecified); **2-specialist on F.2a row 3 line 881 Knit `:FireClient` residue** (round-5 missed 4th location). Single-specialist BLOCKING items of high severity: ai-programmer F2 `RegisterPredatorLock` re-registration silent data-loss window (Pillar 2 silent break of 95% MVP death-attribution gate); gameplay-programmer F1 D.4 vs D.6 divergent cell-key encoding (`%d` vs `..` produce different keys for same XZ position); gameplay-programmer F4 `tierAt()` `--!strict` missing return path (blocks compilation); gameplay-programmer F6 E.16 NaN detection idiom never specified (load-bearing gate under-specified); systems-designer F6 H.31c upper bound 1.05 admits invalid magnitudes; qa-lead F1 H.31c plural-but-not-three-invocations escape hatch; qa-lead F3 H.41e denominator undefined when no participant attempts degenerate strategy (working cap → INCONCLUSIVE/FAIL inversion); qa-lead F5 H.39d evidence-file enforcement is honor system; performance-analyst F1 H.36 lower-bound ~103 vs ~51 sources arithmetic error. **4 specialist disagreements adjudicated by CD as design rulings for round-7a session-start AskUserQuestion**: D1 cap 2-of-4 fix (`>=` vs `>0.50 of nearby` vs replace cap mechanism); D2 shuffle-gaming exploit (cumulative-distance redesign — CD recommends — vs velocity check vs tighten window); D3 OnPredatorLockChanged fix (split into two channels — CD recommends — vs drop purge); D4 PA-F1 arithmetic verify (game-designer + PA pair-check before patching). **CD verdict: MAJOR REVISION NEEDED.** Three of three pillars have ≥1 round-7 BLOCKING that compromises them at the rule level (Pillar 1 Quiet-Is-Power broken by shuffle-gaming; Pillar 2 Fellowship + 95% MVP gate broken by RegisterPredatorLock data-loss + OnPredatorLockChanged purge-never-fires; Pillar 3 World-Watches/Section B traceability broken by chunk stream-in into pre-disturbed zone). **Pattern observation (CD)**: round-7 has THREE distinct signals — (1) revision-residue refusing to settle, (2) prior fixes introducing design regressions (round-6's `>` → `>=` cap fix breaks legitimate active-defender play), (3) AC harness escape hatches mirroring rule-level revision-residue. **Revision velocity is now producing as many problems as it solves.** This GDD will not converge under round-by-round narrow-scope patches. Scope L. **CD round-7 protocol: TWO sequential fresh sessions — round-7a (~3-4 hours) trivial sweep BEFORE design-decision patches with mandatory whole-document grep-sweeps before close-out; round-7b (~1-2 hours) held-out adversarial design pass by game-designer + systems-designer only on every round-7a fix to catch round-7-introduced regressions before round-8.** Round-5 blocker handoff: [ecological-disturbance-round-5-blockers.md](reviews/ecological-disturbance-round-5-blockers.md). Full round-7 review summary in [review log](reviews/ecological-disturbance-review-log.md) | [ecological-disturbance.md](ecological-disturbance.md) | *(none — bus + signal definitions)* |
| 5 | Predator AI | Predator | MVP | Not Started | — | Player Controller, Ecological Disturbance |
| 6 | Crafting & Items | Crafting | MVP | **APPROVED (accepted) — round-23 full-panel `/design-review` + doc-integrity reconciliation COMPLETE 2026-06-08; status unchanged (still APPROVED-by-acceptance).** Round-23 → **NEEDS REVISION (19th consecutive non-APPROVED full panel)**, again NOT overturning the round-20 user acceptance. The recurring reconciliation class recurred a **23rd** time — 2 instances self-inflicted by round-22's own same-day edits + 1 pre-existing stale sibling the prior 22 rounds never reached. Split panel: game/ux/network/build APPROVED; systems/qa NEEDS REVISION. **Network AUTHORITY/ATTRIBUTION CHAIN SOUND a 15th round (PROTECT); systems formulas degenerate-free; AC 122/32 exact.** 3 BLOCKING doc-integrity fixed in-session: (1) **[qa, decisive — only live-path defect]** H.52 + C.8 BT3 in-window CRAFT emit selector `beaconActivated and not beaconWindowSurvived` → `beaconActivated == true and not runOutcomeResolved` + new H.52 variant (c) (sibling of the round-16/22-fixed use/H.109 paths; would have emitted at a stale `beaconWorldPosition` into an ended run after a defeat); (2) H.105 THEN prose band `[35,70]` → `[35.0, 70.1]` (self-inflicted by round-22); (3) determinism preamble appended H.97 (bit-exact) + H.89 (window/grace) — the round-22 forward-maintenance rule's first miss. **Build lens under-scoped a 3rd consecutive round (called the predicate "extinct" while qa found it live at H.52) — CD ruling: reconciliation-clearance now lives PERMANENTLY on the content axis (systems/qa); build is a wiring/lifecycle confirmation only, never a closure gate; a CI grep hook is logged as a TD/qa task to retire the author-discipline dependence.** Counts unchanged (122 ACs / 32 edge cases). Branch `crafting-round2-patch` (rounds 3–23); NOT committed. _(Round-22 + prior history preserved below.)_ **APPROVED (accepted) — round-22 full-panel `/design-review` + doc-integrity reconciliation COMPLETE 2026-06-08; status unchanged (still APPROVED-by-acceptance).** Round-22 (a fresh full 7-lens panel against the post-round-21 GDD) → **NEEDS REVISION (18th consecutive non-APPROVED full panel)** but again did NOT overturn the round-20 user acceptance: the recurring reconciliation class recurred a **22nd** time on NEW surfaces — this round **self-inflicted by round-21's own same-day edits** (H.105's closed-form pin → a boundary degeneracy; the new preamble forward-maintenance rule → a list still violating it; the arity fix elsewhere → stale siblings at H.109 + surface-13). 5 of 7 lenses found new instances; network (**AUTHORITY/ATTRIBUTION CHAIN SOUND a 14th round — PROTECT**) + build (BUILDABLE) clean. **6 BLOCKING doc-integrity items fixed in-session:** H.105 predicate `<=70.0`→`<=70.1` (the mandated `BEACON_HALF_LIFE=128 s`→`70.08 s` edge crashed startup at a legal config) + G.6 closed-form alignment; H.109 stale `beaconActivated && !beaconWindowSurvived` suppression gate → `beaconActivated == true` alone (it would have certified the post-victory defeat-race); surface-13 stale `ceil(#aliveMembers/2)` → `min(requiredHoldersBaseline,#aliveMembers)`; determinism preamble extended to H.22/H.37/H.92/H.102/H.125/H.129 + the forward-maintenance rule; H.106 index label → "closes BOTH lines"; F.4 "16 BIOMASS→8 Canisters" arithmetic (→18). Counts unchanged (122 ACs / 32 edge cases). Advisory recommended items deferred to the implementation/polish phase. Branch `crafting-round2-patch` (rounds 3–22); NOT committed. _(Round-21 + prior history preserved below.)_ **APPROVED (accepted) — round-21 full-panel `/design-review` + doc-integrity reconciliation COMPLETE 2026-06-08; status unchanged (still APPROVED-by-acceptance).** Round-21 (the first fresh full panel against the post-acceptance GDD) → **NEEDS REVISION (17th consecutive non-APPROVED)** but did NOT overturn the round-20 user acceptance: every finding was either enumerable doc-integrity residue (fixed in-session) or a design critique re-opening a user-accepted ruling (2-player asymmetry RBI-3; Signal Anchor R18-2 — a panel cannot overturn user acceptance; both stand). **Network AUTHORITY/ATTRIBUTION CHAIN SOUND a 13th round (PROTECT); build BUILDABLE; systems boundary-safe; thesis SOUND.** The recurring reconciliation class recurred a **20th** time on NEW surfaces (the round-20 build lens wrongly declared it extinct — it had relocated GDD→registry at round-20, now back into ACs/preamble): stale 2-field `OnEscapeBeaconActivated` payload (H.15/H.56/F.2 vs the canonical 3-field Conv-2 form), the stale determinism-preamble enumeration (the round-20 deferred item), relay-activate orphans (C.12.4) — **all reconciled this pass** (arity→3-field; preamble extended to H.118/H.120/H.127/H.128 + a forward-maintenance rule; H.105 closed-form window + Logic label; F.4 "strictly stronger"→"strictly weaker"; C.12.1 frozen-baseline qualifier; relay scrub). Counts unchanged (122 ACs / 32 edge cases). Branch `crafting-round2-patch` (rounds 3–21); NOT committed. _(Round-20 + prior history preserved below.)_ **APPROVED (accepted) — round-20 full 7-spec `/design-review` COMPLETE 2026-06-06.** The panel verdict was NEEDS REVISION (16th consecutive non-APPROVED, 7th non-MAJOR) — but the **mechanical spine is clean for the first time in 20 rounds**: network AUTHORITY/ATTRIBUTION CHAIN SOUND a **12th** consecutive round (PROTECT), AC arithmetic verified, buildable-from-artifact, and the 19-round stale-literal/AC-vs-rule recurring class **did NOT recur in the GDD** (the C.10 shorthand convention extinguished it). All 4 blocking items were closed in the same session and the user **accepted the revisions and marked the system Approved** (electing not to run a 7th full panel): **RBI-1** deleted a stale duplicate `notes:` key on `MAGNITUDE_CANISTER_INWINDOW_USE` in `entities.yaml` (the recurring reconciliation class had *relocated to the registry* — a YAML duplicate key resolved to the pre-EC-3 emission position + the round-17-superseded guard); **G-B1** narrowed the Pillar-1 "enforced economically" claim (the quiet all-BIOMASS gather line is intended; the finale is the unavoidable loud beat); **G-B2** stated the 2-player finale asymmetry in Section B (`requiredHolders = 1` → one holds, one works the predator — intended, preserves the lone-survivor win); **UX-B1** added a REQUIRED denominator-change cue to surface 13 + AC **H.130**; plus **EC-B2** the `ITEM_STACK_MAX` stress-denominator justification in F.4. ACs 121→**122**; both G-B1/G-B2 rulings = user-confirmed recommended defaults. **Deferred (advisory, non-blocking) for the implementation phase / a future polish pass:** per-member hold-status HUD obligation + "Beacon-craftable" squad signal (game); G.3 MINERAL conditional-fragility note + "Anchor cost-neutral" reword + OQ.13 Canister-timing lever (economy); determinism-preamble enumeration refresh (qa); H.121 MoveSpeed re-derivation note + tap-hold AC promotion (ux); H.126 CI stub + peakAliveCount ratchet-after-both-drains note (network). **Cross-GDD obligations still owed by siblings:** ED `BEACON_HALF_LIFE`→[64,128]s (backstopped by H.105); RM (`OXYGEN_DRAIN_BC4_FLOOR`, `canister_restore_oxygen`); PA (`PREDATOR_BC4_MIN_COMMIT`/`MAX_KNOCKBACK`/RESONANT-Coil consumer AC/readable-approach); PC (T8/`OnBeaconWindowSurvived`/`OnBeaconWindowFailed{scatter,wipe}`/`OnSquadMemberAliveChanged`/`MoveSpeed`). Review-log round-20 entry. Branch `crafting-round2-patch` (rounds 3–20); NOT committed. _(Round-19 + prior history preserved below.)_ **ROUND-19 AUTHORING PASS + BOTH NARROW GATES COMPLETE 2026-06-06 (a patch, NOT a verdict — next verdict is the round-20 full panel).** Executed the round-18 CD-prescribed sequence: confirmed both USER FORKS (R18-1 = **max-alive-so-far ratchet baseline**; R18-2 = **KEEP Signal Anchor** + written BIOMASS-sink justification) → ONE authoring pass closing all **5 BLOCKING + 10 recommended + 3 nice-to-haves**, ending in the mandatory canonical-literal fanout sweep + AC-vs-rule pass → two narrow fresh-agent gates. **R18-1** — `requiredHoldersBaseline = ceil(peakAliveCount/2)` from a new `peakAliveCount` RunSession field (init `#squadRoster`, ratcheted `max(...,#aliveMembers)` every tick, read once at BCT3), NOT the activation snapshot — closing the activate-late-for-low-baseline lane (schema/init/drain/BCT3/C.10/invariants/C.5.6a/G.6; E.33 rewritten for both edges; H.118/H.124 revised; new H.129). **Stale `BEACON_HOLD_RADIUS = 30`→12 fanout sweep** (the 4 flagged + 2 missed prose/seam/label literals; re-grep confirms only legitimate 30s remain). **R18-3** position-replication trust gap FENCED (new E.34 + F.5 caveat + post-MVP velocity-check forward obligation H.126; document-and-fence, no anti-cheat in MVP). **R18-4** `graceSeconds=0` on `broken=false` server-side (C.9 step d / both C.14 tables / new H.125). **H.121** mobile-recoverability reframed from an asserted radius-edge claim to a falsifiable JOINT inequality bound by a new PA `PREDATOR_BC4_MAX_KNOCKBACK` cap (resolves the load-bearing game-vs-ux grace disagreement by bounding the knockback, not stretching the grace) + locked 375pt surface-13 pixel budget. Recommended sweep: explicit inverse-degenerate inequality (F.4 RM); RESONANT hedge + MINERAL sink analysis → G.3; Canister noise cost in C.2; H.124 straddle→concrete THEN; new H.127 (scatter-vs-wipe precedence) + H.128 (reconnect-during-BC4); E.33 monotonic wording; shorthand pointers (H.73/H.85/E.27); Signal Anchor keep-justification. ACs 116→**121** (H.125–H.129); +1 RunSession field (`peakAliveCount`); +1 edge case (E.34, 32 live); +1 PA forward obligation (`PREDATOR_BC4_MAX_KNOCKBACK`); registry `BEACON_HOLD_MIN_HOLDERS` updated. **Both narrow gates CLEAN: network re-confirm = AUTHORITY/ATTRIBUTION CHAIN SOUND an 11th consecutive round (0 BLOCKING — PROTECT; the E.34 position-spoof fence verified honestly scoped + bounded, no larger forge path); build-from-artifact (general-purpose) = BUILDABLE (0 BLOCKING, 0 IMPORTANT — 1 MINOR C.14 table-parity gap fixed in-session).** **NEXT = round-20 full 7-spec `/design-review` (the closure gate). DO NOT predict APPROVED for round-20.** Review-log round-19 entry. Branch `crafting-round2-patch` (rounds 3–19); NOT committed. _(Round-18 verdict + prior history preserved below.)_ **NEEDS REVISION — round-18 full 7-spec `/design-review` (the closure gate after the round-17 authoring pass) COMPLETE 2026-06-06.** 15th consecutive non-APPROVED; 6th consecutive NEEDS REVISION. Seven lenses (game-designer, economy-designer, network-programmer native; systems-designer + ux-designer + qa-lead + a build-from-artifact gate via general-purpose) + creative-director synthesis. **Continuous-hold + frozen-holder-threshold thesis SOUND — PROTECT. Network re-confirmed AUTHORITY/ATTRIBUTION CHAIN SOUND a 10th consecutive round — PROTECT.** Held NEEDS REVISION not MAJOR: the recurrence is **single-mechanism + enumerable** (the round-17 30→12 sweep again skipped descriptive/seam/label prose — 4 non-executable stale `30` literals: C.10 hysteresis rationale ~L257, C.16 `_memberPosition` seam ~L458/L467 [these mis-calibrate the H.92 test to 30.0], H.92 summary label ~L1875) — an authoring-round self-inflicted miss, not a methodology failure (the sweep caught all executable hits). **5 BLOCKING:** (1) the stale-30 literal sweep; (2) **NEW degenerate line — "activate-late-for-low-baseline"** [game+economy]: R16-1's freeze closed respawn-raises but opened the symmetric lane (a 4-squad delaying activation until 2 teammates die locks `requiredHoldersBaseline=1`); (3) **position-replication trust gap** [network N18-B1, P0]: `HumanoidRootPart.Position` is client-replicated, so a cheat client can spoof into/out of the 12-stud hold radius (forge-win or grief-line-break) — escalated to BLOCKING because `requiredHolders=2` for a 4-squad means one spoof flips the threshold; (4) `graceSeconds` on `broken=false` relies on an unwritten-HUD "client IGNORES it" convention — send `0` server-side [network N18-B2]; (5) **H.121 mobile-recoverability asserted, not substantiated** [ux+qa]: the 3s-grace model assumes the holder is at the 12-stud edge but knockback pushes outward 20–30 studs; omits touch latency + unverified PC `MoveSpeed`; advisory prose can't close "the load-bearing game-vs-ux disagreement"; includes the missing BC4 worst-case surface-13 wireframe/pixel budget. **DISAGREEMENT adjudicated:** systems-designer claimed C.14 `OnBeaconLineBreak` (L389) was stale round-13 text → CD + build-gate + ux confirmed it MISREAD (L389 carries the full round-16 edge-trigger contract); dismissed. **10 recommended** (frozen-baseline legibility cue [game+ux]; Signal Anchor BIOMASS-sink justification must be written if kept; inverse-degenerate inequality still un-formalized [economy, round-16 carryover]; R16-4 hedge to G.3; C.2 in-window noise cost; H.124 straddle; same-tick scatter-vs-wipe AC; reconnect-during-BC4 AC; E.33 monotonicity wording; shorthand-gloss pointers). **5 binding CD rulings (R18-1..R18-5; two are user forks):** R18-1 fix activate-late via **max-alive-so-far ratchet baseline** (recommended) vs accept the lane; R18-2 **keep Signal Anchor with written BIOMASS-sink justification** (recommended) vs cut to 4 recipes; R18-3 position-spoof = FENCE-style risk-acceptance + F.5 caveat + forward-obligation AC for a post-MVP velocity check (don't build anti-cheat now); R18-4 `graceSeconds=0` on restore; R18-5 E.33 feel resolved by R18-1. **NEXT (CD prescription) = round-19: confirm R18-1/R18-2 forks → ONE authoring pass (5 BLOCKING + 10 recommended) ending in a MANDATORY canonical-literal fanout sweep (grep every numeric literal across ALL prose/seam/label/AC text, the durable fix for the sweep-skips-prose class) + AC-vs-rule pass → TWO narrow gates (network re-confirm + general-purpose build-from-artifact); reserve the full panel for round-20. DO NOT predict APPROVED for round-19.** Scope XL. Review-log round-18 entry. Branch `crafting-round2-patch` (rounds 3–17); NOT committed. _(Round-17 + prior history preserved below.)_ **ROUND-17 AUTHORING PASS + BOTH NARROW GATES COMPLETE 2026-06-06 (a patch, NOT a verdict — next verdict is the round-18 full panel).** Closes the round-16 verdict via the CD-prescribed sequence: applied 4 CD rulings + 7 BLOCKING + recommended sweep, then ran two narrow fresh-agent gates. **R16-1** `requiredHolders` FROZEN at activation (new `requiredHoldersBaseline` field + `min(baseline, #aliveMembers)` clamp — a respawn never raises the bar above the activation baseline, a death never relaxes it below [monotonic difficulty], lone-survivor preserved by the clamp; E.33/H.118/H.124 rewritten). **R16-2** `broken=true` EDGE-TRIGGERED (new `_lineBreakBroadcast` field — fire-once-per-line-break-episode, completing the round-15 restore debounce; C.9 step d/C.10/C.14/H.119/H.95). **R16-3** Signal Anchor reframed to pre-activation recon at every claim point (C.2/C.6/C.7/Overview/OQ.15). **R16-4** RESONANT kept Beacon-premium + the aid-necessity/RESONANT honesty hedge surfaced at the Overview + C.5.6a claim points. + the 7 BLOCKING: stale `BEACON_HOLD_RADIUS = 30`→12 swept (F.2 LD / VA.3 Moment 26 worldspace ring / UI.5 item 4); post-defeat orphaned-Canister-emission guard → `beaconActivated && not runOutcomeResolved` (C.6/C.11/E.32/C.15/H.93); F.6 "8 events"→7; C.15 scatter prose → `requiredHolders`; `POSITION_SETTLED_EPSILON` AC (H.123). + recommended sweep: `OnBeaconWindowFailed.reason` typed union; `_craftSessions` `_runEndCleanup` sweep (H.122); RM fire-and-forget; baseline enumeration (H.124). ACs 113→**116** (H.122–H.124; H.118/H.119 revised); +2 RunSession fields (`_lineBreakBroadcast`, `requiredHoldersBaseline`); registry updated. Carried the mandated **canonical-literal fanout sweep** (no stale `30`/`8 events`/`none in radius` remain) + **AC-vs-rule consistency pass**. **Round-17 narrow gates RAN same session, all findings fixed in-session:** network re-confirm = **AUTHORITY/ATTRIBUTION CHAIN SOUND a 9th consecutive round (0 BLOCKING — PROTECT)** + 2 clarity notes folded in (BCT3 read-before-drain; `graceSeconds` ignored on restore); build-from-artifact (general-purpose) = **BUILDABLE, 0 BLOCKING** — 1 IMPORTANT (C.15 `RequestUseItem` still had the loose "in BC4" trigger → fixed to the authoritative `beaconActivated && not runOutcomeResolved`) + 1 MINOR label, fixed; arithmetic (124−8 = 116) verified. **NEXT = round-18 full 7-spec `/design-review`. DO NOT predict APPROVED.** Branch `crafting-round2-patch` (rounds 3–17); NOT committed. _(Round-16 verdict + prior history preserved below.)_ **NEEDS REVISION — round-16 full 7-spec `/design-review` (the designated closure gate, deferred from round-15) COMPLETE 2026-06-06.** 14th consecutive non-APPROVED; 5th consecutive non-MAJOR. **Unanimous across all six delivering lenses** (game-designer, economy-designer, network-programmer native; systems-designer + ux-designer + qa-lead routed through general-purpose after the native subagent types failed to deliver a final report) + creative-director synthesis. **Continuous-hold + holder-threshold thesis SOUND — PROTECT. Network re-confirmed AUTHORITY/ATTRIBUTION CHAIN SOUND an 8th consecutive round — PROTECT** (all 5 forge vectors re-derived vs the round-15 changes; no forge path). Held NEEDS REVISION not MAJOR: recurrence is single-mechanism + fully enumerable, and qa verified the AC section's hold-rule wording is itself clean. **Both round-15 headline deliverables failed to fully hold (the recurrence class, 6th time, on round-15's OWN new surface): (1) stale `BEACON_HOLD_RADIUS = 30 studs` survives the 30→12 change in 3 player-facing spots [TRIPLE-lens]** — worst is VA.3 Moment 26's worldspace ground-ring (the primary "where do I stand to win" cue) authored at 2.5× the real win radius; also F.2 LD row + UI.5 item 4; **(2) the restore-debounce is mechanically incomplete + its ACs invert the rule [systems+qa, ux concurs]** — only `broken=false` is debounced, `broken=true` re-fires every below-threshold tick, and H.119/H.95 assert the opposite of the C.9/C.10 rule (AC-vs-spec contradiction). Other BLOCKING: **post-defeat orphaned in-window Canister emission** (`beaconActivated && !beaconWindowSurvived` stays true after a defeat; a `RequestUseItem` between Heartbeats emits into an ended run — needs `&& not runOutcomeResolved`) [network+qa]; **F.6 "8 named events"→7** [game+systems+qa]; **C.15 `OnBeaconWindowFailed` scatter prose still "none in radius"** not `requiredHolders` [game]; **E.33 respawn-raises-`requiredHolders` needs a binding ruling + a HUD cause-cue** [QUADRUPLE — game/ux/systems/qa]; **economy honesty** (Signal Anchor "bears directly on the window" vs its own "honestly limited"; RESONANT Beacon-components-only at MVP; aid-necessity fence buried in F.4). **4 binding CD rulings (user may override): R16-1** freeze `requiredHolders` at window-open (kills respawn-punishes-holders); **R16-2** `broken=true` fire once per episode (fix H.119/H.95); **R16-3** reframe Signal Anchor as out-of-window recon; **R16-4** keep RESONANT a Beacon-justified premium + surface the honesty hedge. **DISAGREEMENTS adjudicated:** F.6 "8 events" IS a real miss (network misread it as fine); the `broken=true` re-fire IS BLOCKING (ux's "not a WCAG flash" reframe is additive, not exculpatory). **NEXT (CD) = round-17: ONE authoring pass (R16-1..4 + the 7 BLOCKING + recommended sweep) carrying a MANDATORY canonical-literal fanout sweep (grep every constant value) + a MANDATORY AC-vs-rule consistency pass → TWO narrow fresh-agent gates (network re-confirm + general-purpose build-from-artifact — NOT the systems/gameplay/ux/qa subagent types, which fail to deliver here); reserve the full panel for round-18. DO NOT predict APPROVED for round-17.** Review-log round-16 entry. Branch `crafting-round2-patch` (rounds 3–16); NOT committed. _(Round-15 + prior history preserved below.)_ **ROUND-15 AUTHORING PASS APPLIED 2026-06-05 (a patch, NOT a verdict — review-log round-15 entry + `reviews/crafting-and-items-round15-patch-plan.md`).** CD ruling session (all 4 user forks = recommended) + the ~9 mechanical blockers from round-14, all closed. **SENTRY fix:** `BEACON_HOLD_RADIUS` 30→12 + new `requiredHolders = ceil(#aliveMembers/2)` holder threshold (`held ⟺ #windowAliveInRadius >= requiredHolders`) replacing the round-13 `>=1` rule across C.5.6a/C.9/C.10/E.24/E.27/UI-13 + 12 ACs; field `_lineBreakEmptySince`→`_lineBreakSince`. **Grace** kept 3 s, range capped 0–4 (mobile-traversable at radius 12 — resolves the round-14 game-vs-ux load-bearing disagreement by shrinking the radius, not stretching the grace). **Quadruple-lens debounce** closed: `BEACON_LINE_BREAK_RESTORE_DEBOUNCE` knob + `_lineBreakRestoreTicks` + concrete rule (replaced the circular "debounce to the reset" in the C.14 signal-def) + extended H.95. **B-1** scatter-vs-wipe precedence (C.9 step b compute-only, scatter fired after step c gated on aliveMembers non-empty); **N14-B1** wipe-tick line-break guard (step d skipped when runOutcomeResolved); **ux F3** HOLDING/BROKEN colorblind shape/icon; **B-3** concrete `POSITION_SETTLED_EPSILON` positioned predicate; **B-5** dual `OnBeaconActivated`/`OnEscapeBeaconActivated` consumer split + missing UI.3 row. **Signal Anchor** reframed as pre-activation recon (Section B + C.7); **Ruling 3** in-window craft emits at beacon (E.17 rewrite + BT3 BC4-conditional + H.52); **E-1** unit-independent oxygen inequality (F.4 RM); **F6** PA winnable-by-holding + no-one-shot (F.4 PA); **E-2** RESONANT/Coil binding minimum (F.4 PA + OQ.15). New E.33 (respawn raises requiredHolders — a **flagged feel consequence** awaiting user confirm) + H.118–H.121; ACs 109→**113**; new knobs `BEACON_HOLD_MIN_HOLDERS`(derived)/`BEACON_LINE_BREAK_RESTORE_DEBOUNCE`/`POSITION_SETTLED_EPSILON`; +1 RunSession field `_lineBreakRestoreTicks`; `entities.yaml` updated. **NEXT = round-15 narrow gates (re-scoped build-from-artifact + narrow network re-confirm), then round-16 full panel. DO NOT predict APPROVED for round-16.** Branch `crafting-round2-patch`; NOT committed. _(Round-14 verdict + prior history preserved below.)_ **NEEDS REVISION — round-14 full 7-spec `/design-review` (the designated closure gate) COMPLETE 2026-06-05.** 13th consecutive non-APPROVED; 4th consecutive non-MAJOR. Unanimous across all six delivering lenses (game-designer, economy-designer, network-programmer, ux-designer, qa-lead, build-from-artifact via general-purpose; systems-designer's lens failed to deliver twice — domain absorbed by the build-from-artifact trace) + creative-director synthesis. **The round-13 continuous-hold thesis is SOUND — PROTECT it** (genuinely closes KITING + dissolves the ux-B12-2 hysteresis trap). **Network AUTHORITY-CHAIN SOUND a 6th consecutive round — PROTECT (authority wins over blocker-closure); neither network BLOCKING is a forge hole.** **Headline = the "SENTRY" successor to KITING** [game-designer, DESIGN]: closing KITING pushed optimal play one ring outward — `BEACON_HOLD_RADIUS = 30` (a 60-stud sphere) + `≥1-in-radius` rewards "park a sentry in a corner while everyone plays freely," not Section B "hold the line"; needs a CD ruling (shrink radius ~10–15, and/or require `ceil(aliveMembers/2)` holders). **Recurrence: the reconciliation-miss class re-appeared a 5th time, HELD at BLOCKING, NOT escalated to MAJOR** — the misses cluster on the round-13 thesis's OWN new surface (the C.9 step b/c/d reorder + the new `OnBeaconLineBreak` signal) while the old seams are confirmed reconciled (Conv-1/Conv-2 clean); under-scoped-gate pattern, methodology-MAJOR arc stays resolved. ~16 BLOCKING (7 need CD design rulings). **Strongest signal = QUADRUPLE-lens convergence:** the `OnBeaconLineBreak broken=false` debounce is a normative MUST with no concrete algorithm (network + ux-WCAG-flash + qa-no-AC + build-from-artifact-circular) → a sole boundary-holder flaps the squad-level win-state banner at up to 60 Hz. **Other BLOCKING:** scatter-vs-wipe precedence inverts on the grace-expiry-and-death tick (build B-1, contradicts H.115); `OnBeaconLineBreak` fires on the wipe tick (network N14-B1, contradicts C.14); 3 s grace mobile-unrecoverable (ux F1); HOLDING/BROKEN has no colorblind encoding (ux F3); position-validity re-add predicate undefined (build B-3); dual `OnBeaconActivated`/`OnEscapeBeaconActivated` HUD consumer unspecified (build B-5); `canister_restore<3.5 s` derivation circular/unit-dependent (economy E-1); RESONANT has no rational non-Beacon consumer pre-PA / OQ.15 fence too broad (economy E-2); H.108 `_enqueueReadd` seam not in WHEN + no de-dup AC (qa); no AC for break-within-grace→recover→win (qa). **Specialist DISAGREEMENT (CD-flagged, the load-bearing ruling):** the grace dial is over-constrained from BOTH ends — game-designer wants the upper bound CAPPED (~4 s, anti-kiting) while ux-designer wants the default RAISED (5–6 s, mobile-recoverable); likely resolved by changing a different variable (shrink the hold radius so a shorter grace is traversable). **NEXT (CD prescription) = round-15: CD ruling session FIRST (7 design rulings) → ONE authoring pass (rulings + the ~9 mechanical/spec blockers) → TWO narrow gates (re-scoped build-from-artifact + network re-confirm); reserve the full panel for round-16. DO NOT predict APPROVED for round-16.** Branch `crafting-round2-patch` (rounds 3–14); NOT committed. _(Round-13 + prior history preserved below.)_ **ROUND-13 AUTHORING PASS + BOTH NARROW GATES COMPLETE 2026-06-04 (a patch, NOT a verdict — review-log round-13 entry).** Round-12 full-panel closure gate (2026-06-04) → unanimous NEEDS REVISION (12th consecutive non-APPROVED, 3rd non-MAJOR; authority chain SOUND 4th round); headline = KITING (the single-frame window-end check made activate-scatter-regroup the optimal line). **Round-13 KITING ruling (user): FAIL-FAST continuous-hold + ~3 s regroup grace** — new knob `BEACON_LINE_BREAK_GRACE`, field `_lineBreakEmptySince`, squad-level `OnBeaconLineBreak` HOLDING/BROKEN binary (strict, no hysteresis — dissolves ux-B12-2); the round-9 "advisory, kiting unpunished" framing is reversed. All round-12 blockers closed (Conv-1 canister→beacon-pos; Conv-2 `windowDurationSeconds` propagation; Conv-3 `runOutcomeResolved` guard hoisted to `_step`-top; Conv-4 outcome-first BC4-before-bench; ux UX-B12-1/2; economy EC-1a/b + `canister_restore < 3.5 s` ceiling; qa `_clock` cites + Dampener-0.56 AC). **Both narrow gates run: network = AUTHORITY-CHAIN SOUND (5th round, PROTECT); build-from-artifact = 1 BLOCKING + 2 IMPORTANT + 3 MINOR, ALL FIXED in-session** — the BLOCKING was the round-11 respawn re-add being a *direct* `CharacterAdded` write racing the buffered drain → re-routed through a buffered `_pendingReadds`/`_enqueueReadd` queue drained after departures (the round-11 "sanctioned direct add" exemption RETIRED). ACs 105→**109** (H.114–H.117; H.73/H.85/H.89 revised); +1 knob, +2 RunSession fields, +2 signals, +1 test seam; `entities.yaml` updated. **NEXT = round-14 full 7-spec `/design-review` (the closure gate). DO NOT predict APPROVED for round-14.** Branch `crafting-round2-patch`; NOT committed. _(Round-10–12 + history below.)_ **NEEDS REVISION — round-10 full `/design-review` COMPLETE 2026-06-03 (7-spec panel + creative-director).** First non-MAJOR verdict in 10 rounds and first unsplit panel (all 7 → NEEDS REVISION; 9th consecutive non-APPROVED). **Methodology arc RESOLVED:** the fresh-agent build-from-artifact gate cleared the round-6/8 self-verification bar (caught its in-scope schema-omission class 100%); the residual class recurs only at seams the BC4-scoped gate excluded. **Authority/attribution chain re-confirmed SOUND** (no forge path on the new `aliveMembers`/`runOutcomeResolved`/wipe surface) — PROTECT. ~8 BLOCKING, convergences: `aliveMembers` stale outside-BC4/respawn [triple-lens: network/gameplay/systems]; `runOutcomeResolved` cross-system latch timing vs PC [double]; H.89 wipe reconciliation miss [double]; `OnBeaconHoldStateChanged` flood/hysteresis [double]; advisory-vs-evaluative HUD trap [double]; `DAMPENER_REDUCTION` 0.70 ceiling PC-dependent; economy EC-1/2/3 (F.4 inequality violated at short window; OQ.15 overclaim; new scatter-to-breathe line owes a CD ruling); qa AC-coverage gaps (H.94 negative, latch AC, seam-integrity). **ROUND-11 AUTHORING PASS + BOTH GATES COMPLETE 2026-06-04 (a patch, NOT a verdict — see review-log round-11 entry):** closed all 5 convergences + single-lens blockers (CD rulings: EC-3 in-window canister → beacon-position emission; DAMPENER interim cap 0.56; game-B1 prose-honesty). The **widened build-from-artifact gate returned 16 findings, all genuine, all fixed in-session** (sharpest: C3 reason-gated the alive-scope drain to {death,disconnect} — a cancel would have fired a false wipe; A4 widened PC all-dead suppression to the whole post-activation lifecycle; C2 added Crafting's RunEnded-latch subscription; O1 RunStarted init contract; O5 published `windowDurationSeconds` for the escalation). The **narrow network security gate re-confirmed AUTHORITY-CHAIN SOUND** (no forge path from any round-11 edit). ACs 99→105 (H.108–H.113); +2 knobs (`BEACON_HOLD_HYSTERESIS`, `BEACON_HOLD_ESCALATION_LEAD`). PC `OnSquadMemberAliveChanged` respawn re-add + BC4 all-dead suppression are now **BLOCKING** PC obligations. **NEXT = round-12 full 7-spec `/design-review` (the closure gate). DO NOT predict APPROVED for round-12.** *(Round-10 + history below.)* **ROUND-9 AUTHORING PASS APPLIED 2026-06-03 (NOT yet re-reviewed).** Applied the round-8 CD remedy: **inverted authoring order (schema + state model FIRST, rules reconciled against the locked schema), then a fresh-agent build-from-artifact gate replacing the self-administered checklist** (the checklist self-certified clean in rounds 3 + 7 and missed its target class both times). User-approved 4 design decisions: Blocker #1 → named `_windowPrevInRadius` prev-tick field (edge-detection, presentation-only); #3 → server-authoritative `aliveMembers` set (the canonical alive-predicate); #10 → window-end-only eval, kiting allowed, HUD advisory; #11 → Section B Anchor prose range-honest (no mechanical change). All 13 round-8 blockers resolved at the rule/schema level: the three round-7-introduced defects (#1 crossing-detection, #2 `_pendingDepartures`/`_enqueueDeparture` schema+seam gap, #3 alive-predicate) + #4 backward-clock guard (`_clock()` monotonic + `max(0,…)`) + #5 single-decision-point wipe (BC4 handler owns survived/scatter/wipe, idempotent `runOutcomeResolved` latch, `OnBeaconWindowFailed.reason∈{scatter,wipe}`) + #6 cancel de-dup ({playerId,scope}) + #7 Crafting-side fail-fast window-range init assertion (ED registry edit stays its fenced obligation) + #8/#9 Group-B fenced as RM+PA forward obligations (provisional `canister_restore`; inverse-degenerate AC) + #10/#11/#12 (suppress surfaces 5+9) /#13 (merged-chevron direction vector + tap-hold release feedback). **The fresh-agent gate (gameplay-programmer, given ONLY C.8/C.9/C.10/C.16) returned 19 omissions; ~7 were genuine schema-omission/ambiguity defects of the exact recurring class — all fixed in-session** (position-READ seam unnamed→`_memberPosition`; C.8-vs-C.10 window-scope predicate mismatch; `dist3D` axis ambiguity→3D Euclidean; `_windowPrevInRadius` clear-on-exit; bench→session map `_craftSessions`; BCT5 latch; respawn signal→PC forward obligation). **This validates the methodology change.** New schema fields: `aliveMembers`, `_pendingDepartures`×2, `_windowPrevInRadius`, `runOutcomeResolved`, `_craftSessions`; seams `_enqueueDeparture`/`_memberPosition` tabled. Counts: 4 recipes / 4 item types / 24 VA moments / 30 edge cases / **99 ACs** (H.102–H.107) / 7 client events. **Authority/attribution chain untouched (must be re-confirmed at round-10 — the gate was build-completeness, not a network adversary).** Records: review-log round-9 entry. **NEXT = round-10 full `/design-review`. DO NOT predict APPROVED.** Branch `crafting-round2-patch`; NOT committed. _(Round-8 + prior history preserved below.)_ **ROUND-8 full `/design-review` COMPLETE 2026-06-03 → MAJOR REVISION NEEDED** (8th consecutive non-APPROVED). 7 specialists + creative-director. game-designer = MAJOR; the other six (systems/economy/network/ux/qa/gameplay) = NEEDS REVISION; CD adjudicated MAJOR on **self-verification methodology, not design**. **Authority/attribution chain confirmed SOUND — round-7 did NOT regress it.** ~13 BLOCKING. The decisive finding: round-7's OWN three additions each introduced fresh mechanism-implicit / schema-omission defects (the recurring root-cause class) — (1) `OnBeaconHoldStateChanged` crossing-detection needs cross-tick state that C.10 explicitly forbids storing (found independently by systems + gameplay + qa); (2) `_pendingDepartures` referenced ~6× across the tie-break spine but absent from BOTH C.10 schemas, AND `_enqueueDeparture` cited as a C.16 seam but absent from the seam table; (3) `windowAliveInRadius` alive-predicate undefined (the whole half-destroyed-HRP safety claim rests on it). Plus: D.1/window no backward-clock guard; BCT-DEFEAT wipe double-path (Crafting drain + PC all-dead, no mutex); `RequestCraftCancel` double-enqueue; `BEACON_HALF_LIFE` R4-1 registry edit still UNAPPLIED ([45,180]→[64,128] pending ED, no Crafting init assertion); Group-B bunker fix is **taxed-not-closed** (silent-hold-no-canister line untouched; economy gated on unwritten RM+PA; F.4 RM inequality has 2 undefined operands; inverse "never use canister" degenerate has no closing AC); scatter-defeat feedback trap (continuous HUD vs single-tick window-end eval); Anchor "45s<49s" false across ED's half-life range; ≤4 BC4 HUD cap breached (surfaces 5 Coil-Billboard + 9 Anchor-chevron off the suppress-list); surface-8→13 merge directional semantics undefined; tap-hold cancel-feedback unspecified. **CD diagnosis:** the mandated schema-completeness sweep self-certified as clean while missing `_pendingDepartures` — same failure mode as the round-3 reconciliation sweep. **CD remedy for next pass:** stop self-administered checklists; invert authoring (schema + state model FIRST, rules second), then run a **fresh-agent build-from-artifact gate** (hand a subagent only the schemas → every invented field is a confirmed omission). User chose to **STOP & revise in a fresh session**. NEXT = authoring pass then re-review. **DO NOT predict APPROVED.** Branch `crafting-round2-patch`; NOT committed. _(Round-7 + prior history preserved below.)_ **Round-7 authoring pass APPLIED 2026-06-02 (NOT yet re-reviewed).** Per the round-6 CD sequencing: CD Group-B ruling first (user-approved **Hybrid A+B** — in-window Oxygen Canister emission `MAGNITUDE_CANISTER_INWINDOW_USE` as the bunker counter + falsifiable RM/PA forward-obligation contracts with binding ACs), then ONE authoring pass — **Group B** (+ Signal Anchor re-weight 1/1/1→2/1/0, BIOMASS-sink fix, OQ.15 structural-resolved); **Group A** win-condition data model (single per-tick `windowAliveInRadius` snapshot kills the two-sources-of-truth; `_enqueueDeparture` dual-scope dispatch seam; `benchMaxCraftersEffective` storage field; D.6a Stage-2 ownership ruled); **Group C** (≤4-surface BC4 HUD cap + suppress-list; `OnBeaconHoldStateChanged` per-member hold-state signal; Crafting-owned WCAG flash cap); **Group D** (`_simulateBindToClose` seam; H.15 Logic/Integration split; H.39 crossing-test reframe); recommended tail (Section B prose; RequestEquipItem burst; RequestCraftCancel buffering; rate-limit window 2s→3s; 9 new ACs H.93–H.101; test-type relabels). Counts: 4 recipes / 4 item types / 24 VA moments / 30 edge cases / 93 ACs / 7 client events. Self-stress ran: reconciliation-fanout **caught a genuine stale round-5 miss** (recipe panel "5 rows"→"4 rows" in 4 surfaces); schema-completeness sweep clean; **authority/attribution chain untouched & protected**. **NEXT = THREE narrow fresh-agent gates** (economy; systems+network jointly on the Group-A schema; ux) — NOT a full panel — then **round-8 full `/design-review`**. **DO NOT predict APPROVED for round-8.** Records: review-log round-7 entry + `crafting-and-items-round7-patch-plan.md`. Not committed. _(Round-6 + prior history preserved below.)_ **MAJOR REVISION NEEDED — round-6 `/design-review` complete 2026-06-02 (the designated closure-gate full 7-spec panel + creative-director).** 5th consecutive non-APPROVED, but **converging, not treading water** — the round-5 reconciliation-fanout checklist closed the round-4 named class (only a stale "Moment 25" survived), the D.1 clock-elapsed form is bit-exact, C.16 + the anchorPosition payload are resolved, and **network confirmed the win-condition authority + attribution chain is SOUND (no forge path) — protect it.** Held at MAJOR on the economy adjudication. Specialist split: 5 NEEDS REVISION (game/systems/ux/network/qa) / 1 MAJOR (economy); CD adjudicated MAJOR. Blocking groups: **A — win-condition data model unspecified (3-lens convergence: systems+network+qa)**: no alive-set schema field (BCT4 reads *live* HumanoidRootPart.Position while H.77 tie-break reads a *buffered* pre-drain set — two sources of truth, undermining the R4-5 determinism fix); `_pendingDepartures` dual-scope dispatch is intent-without-mechanism; `benchMaxCraftersEffective` has no storage field; D6a Stage-2 (PC stationary attenuation) ownership undefined. **B (decisive) — economy**: R4-2 Squad Relay cut WORSENED canister-bunker dominance (sole oxygen line now the cheapest/quietest 2/0/0 recipe, stackable ×10); aid-necessity (C.5.6a) is an unfalsifiable "design target contingent on two UNWRITTEN GDDs" (RM oxygen-drain floor + PA min-aggression); BIOMASS sink degenerate (Medium nodes dead content) — a structural material-tier decision mis-filed as tuning (OQ.15). **C — UX**: BC4 HUD density (~8–9 surfaces on iPhone SE) self-flagged "blocking-for-implementation"; scatter hold-state needs per-member live distance no signal carries; WCAG 2.3.1 flora-flash offloaded to the unwritten ED flora author with no Crafting-owned cap. **D — AC testability**: H.90 (BindToClose) not headlessly testable (needs `_simulateBindToClose` seam); H.15 mislabeled Integration (unreachable sprint gate — needs H.73/H.76-style split); H.39 midpulse "bit-exact" only holds at the default 0.5. CD methodology note: rules land while the mechanisms beneath them stay implicit — the alive-set gap was arguably *created by* the R4-5 tie-break fix. **NEXT (CD): a CD design ruling on Group B BEFORE authoring (structural balance call, not hygiene); then ONE authoring pass (Groups A/C/D + AC tail); then THREE narrow gates for round-7 (economy; systems+network jointly on the schema; ux), NOT a full panel. User chose to STOP & revise in a fresh session. DO NOT predict APPROVED for round-7.** Not committed. _(Round-5 + prior history preserved below.)_ **NEEDS REVISION — round-5 AUTHORING pass applied 2026-06-02 (CD rulings R4-1..R4-5 + ~15 round-4 blockers + recommended sweep). NOT yet re-reviewed.** Decisions: **Squad Relay CUT** (R4-2; aid suite 4→3, recipes 5→4); `OnSignalAnchorTripped`→`anchorPosition`; scatter-defeat hold zone locked to **world-ring + HUD state**; ED `BEACON_HALF_LIFE`→[64,128]s lock (R4-1, OQ.14 CLOSED); D.1 from-start closed form (R4-3, kills the IEEE-754 drift); C.16 forbid narrowed to handler bodies + inline `_clock()` (R4-4); death/disconnect buffered in the Heartbeat handler covering `Players.PlayerRemoving` (R4-5). Reconciliation-fanout sweep ran (G.8 tap-hold + VA.5 RELAY fixed). H.73/H.76 relabeled Integration+unverified-until-PC; new ACs H.88–H.92. Counts: 4 recipes / 4 item types / 23 VA moments / 84 ACs / 29 edge cases / 7 client events. **NEXT = TWO narrow fresh-agent gates (qa-lead/determinism + network/C.16+attribution), NOT a full panel; round-6 full panel is the closure gate. DO NOT predict APPROVED for round-6.** Not committed. _(Round-4 review + prior history preserved below.)_ **NEEDS REVISION — round-4 `/design-review` complete 2026-06-02 (verdict MAJOR REVISION NEEDED).** Full 7-spec panel + creative-director. Specialist split: 3 NEEDS REVISION (game-designer, systems-designer, economy-designer) / 3 MAJOR REVISION NEEDED (qa-lead, network-programmer, ux-designer). CD held at MAJOR because the reconciliation-miss class flagged in rounds 2 AND 3 survived the round-3 self-stress sweep (the sweep methodology, not the design, is the failure). Defect class HAS narrowed — thesis sound; network affirmed the win-condition authority + attribution chains are SOUND (no forge path). ~15 BLOCKING, grouped: (1) **reconciliation misses** — G.8 still lists `TAP_HOLD_COMMIT_DURATION` as "Reserved…none in MVP" (contradicts B6 place+activate tap-hold); VA.5 item 3 still lists the retired RELAY tier; (2) **`OnSignalAnchorTripped` payload** `anchorPosition` (C.7/C.14/C.15) vs `predatorPosition` (F.2/UI.3) — unresolved 3 rounds; (3) **tie-break determinism** — C.8/C.9 declare order but Roblox has no Heartbeat-priority API; C.16 fixes tests not production; disconnect (`Players.PlayerRemoving`) at window-end uncovered; (4) **IEEE-754 float accumulation** — H.28/H.30/H.31/H.41 assert exact equality after 300–600 sequential adds (drifts ±1–2 ULP); C.16 seam fixes inputs not the D.1 algorithm; (5) **C.16 self-contradiction** — C.9 BCT3 step 2 instructs `workspace:GetServerTimeNow()` directly, which C.16 says reviewers MUST reject; (6) **economy** — C.2 "realistic" 18-gather claim unsupported/un-validatable until RM+PA (OQ.13); Squad Relay economically dominated under the 30-stud hold; (7) **OQ.14** — win-window timing derived from ED `BEACON_HALF_LIFE` [45,180]s → degenerate windows [24.6,98.9]s — blocking under-spec, not an open question; (8) **UX** — BC4 HUD density on iPhone SE deferred, scatter-defeat hold-zone legibility (a lose condition) deferred, WCAG 2.3.1 flora-flash mis-marked Recommended; (9) **AC gaps** — Relay rate-limit, double-equip outcome, Survived/Failed mutual-exclusion, BindToClose, `beaconActivationTime` invariant; H.73/H.76 still mislabeled [P0] Logic (round-2 B3 never resolved, silently dropped from round-3). **CD binding rulings (user may override): R4-1** ED tightens `BEACON_HALF_LIFE`→~[64,128]s (Crafting does NOT decouple); **R4-2** Squad Relay **CUT** (3rd round on watch, now dominated); **R4-3** respecify D.1 as from-start `progress = craftRate × stepCount × dt`; **R4-4** narrow the C.16 forbid to handler bodies; **R4-5** buffer death/disconnect inside the Heartbeat handler, cover `Players.PlayerRemoving`. **Process: round-5 = authoring + TWO narrow gates (qa-lead/determinism + network/C.16+attribution), NOT a full panel; full panel returns at round-6. DO NOT predict APPROVED for round-6.** _(Round-3 authoring-pass + round-3 re-review summaries preserved below.)_ **Round-3 authoring pass applied 2026-06-01.** The round-3 authoring session landed all round-3 blockers: B1 proximity victory (`BEACON_HOLD_RADIUS = 30`) + new `OnBeaconWindowFailed` scatter-defeat signal; B2 aid-necessity relocated to RM oxygen-drain-floor + PA min-aggression forward obligations; B4 LOS reachability raycast; B5 Heartbeat-pinned window timer; B6 tap-hold place+activate; B7 H.80→all-8-handlers; B8/B9 new C.16 test-injection seam + exact-AC rewrites; D.1 div-by-zero guard; H.23/C.6 relay-consume reconcile; G.6 derived-range fix (ED `BEACON_HALF_LIFE`→~[64,128]s obligation, OQ.14); relay rate-limit + equip atomic order; BT3/BT5 tie-break; `MIN_SQUAD_SIZE=2` solo handling; Squad Relay KEPT (burst→MID 0.18, `MAGNITUDE_CRAFT_RELAY` retired). Counts: edge cases 29, ACs 80, magnitudes 4. Self-stress pass caught + fixed the scatter-defeat signal gap. Not committed. **DO NOT predict APPROVED for round-4.** _(Round-3 re-review verdict preserved below.)_ — **MAJOR REVISION NEEDED** (round-3 re-review 2026-06-01 — full 7-spec panel + creative-director; unanimous). **Round-3 ran against the document with Group A committed (`b26fb89`) but Group B still open — so all 9 Group B blockers re-confirmed (B1 victory-proximity, B2 aid-necessity, B4 cross-wall placement, B5 Heartbeat-timer pin, B6 activation friction, B7 H.80 3-of-8 handlers, B8 H.27/H.61 determinism, B9 H.74/H.77 injection), PLUS new blocking surface only reachable now: D.1 div-by-zero on `CRAFT_BASE_DWELL_TIME=0`; H.23 ("Relay consumed at placement") contradicts C.6 ("on activation"); G.6 tuning-range `35–70 s` unsupported by the decay formula (nominal ~49 s correct); `RequestActivateRelay` missing per-player rate limit + `RequestEquipItem` missing atomic-step order; BT3/BT5 same-Heartbeat tie-break undeclared; Signal Anchor lifetime 45 s < window 49 s; solo-play (squad=1) unaddressed; `BindToClose` during BC4 undefined. economy: canister-stack-and-bunker is the probable dominant line (BIOMASS-only Canister is free; C.2 sanity-check validates it); RELAY tier 0.20 inside HYSTERESIS_BAND of MID 0.18. ux: Coil-badge final-10s warning color-only (violates own colorblind rule); countdown "ring" vs art-bible single-circle lock. network POSITIVE: win-condition authority + attribution chains are sound (no forge path). qa-lead: AC count 73 confirmed; 13 timing ACs need a dt/timer-injection harness API. CD: "sound thesis, converging — but stop running full panels against known-open blockers." **Two binding CD rulings (user may override): B1 → victory requires ≥1 alive AND within `BEACON_HOLD_RADIUS` (~30 studs) of the beacon at window-end; B6 → beacon placement AND activation are tap-hold.** B2 to be RELOCATED to named RM/PA forward obligations, not solved in Crafting. **Next session = AUTHORING pass, not a review** (rulings → relocate B2 → declare test-injection API + pin Heartbeat timer → mechanical sweep w/ self-stress), THEN a round-4 panel. Do NOT predict APPROVED for round-4. Squad Relay on watch as a possible cut candidate. _(Round-2 re-review 2026-06-01 preserved below.)_ Round-1 (2026-05-01, 24 BLOCKING/6 themes) was patched via the survival-window redesign (Beacon activation opens a ~49 s window; victory only on surviving it; cut to 5 recipes; order-of-ops collapsed; squad-scaled bench cap; schema+net fixes) — round-1 themes 2 (order-of-ops) & 6 (bench cap) RESOLVED, others PARTIAL. Round-2 re-review (7 specialists + creative-director) verdict: **sound thesis, unfinished patch.** Group A (reconciliation misses) FIXED this session: E.9 body still won-at-activation; C.11.3/H.39 midpulse still `crafters[1]`; BCT5 `From` listed terminal BC5; game-concept.md win-condition reconciled to the window model; registry Coil formula + H.67 craft-times corrected. Group B PENDING (design decisions): victory-proximity rule undefined (activate-and-scatter); aid-suite-necessity unsubstantiated pending PA+RM GDDs (Canister-stack-and-bunker risk); BCT-DEFEAT leans on unverified PC path; cross-wall placement exploit; survival-window timer must pin to Heartbeat; Beacon activation friction (round-1 B19) still open; H.80 handler scope; round-1 carryover H.27/H.61. UX dependency flagged via UI.5 — `/ux-design crafting-bench` before epics. Review log: [crafting-and-items-review-log.md](reviews/crafting-and-items-review-log.md) | [crafting-and-items.md](crafting-and-items.md) | Player Controller (interaction) |
| 7 | HUD | UI | MVP | Not Started | — | Resource Management, Ecological Disturbance, Crafting & Items |

**Total MVP**: 7 systems (down from 21 in the Unity scope).

> **Ecological Disturbance — file structure (restructured 2026-05-30):** the ED GDD was decomposed *by content type* into **three files** (relocation only — no design content changed, no verdict change, no BLOCKING item resolved or introduced):
> - `ecological-disturbance.md` — the GDD (8 design sections + AC contracts).
> - `ecological-disturbance-verification.md` — test-infrastructure prerequisites + testability flags (and, in a later pass, the embedded CI-YAML / fixture-mock specs / evidence templates still interleaved in the AC section).
> - `ecological-disturbance-forward-obligations.md` — F.2a / F.2a-IMPORTANT / F.2b cross-GDD coordination tracking, incl. the still-open round-20 Session B/C BLOCKING items.
>
> The ED status above is unchanged by the restructure. See `reviews/ecological-disturbance-decomposition-plan.md` and the review-log Restructure entry.

---

## Roblox Built-Ins (Replacing Former MVP Systems)

The following systems were MVP under the Unity scope but are now provided by
Roblox or external standard libraries — no GDD required:

| Former System | Replacement |
|---------------|-------------|
| Input | `UserInputService` + `ContextActionService` |
| Save/Load | `DataStoreService` via ProfileStore (cosmetics + meta only) |
| Settings | Roblox built-in settings menu |
| World Streaming/LOD | Roblox `StreamingEnabled` |
| Death & Respawn | Roblox built-in `Humanoid.Died` + minor customization |
| World Generation | Hand-built map (not procedural) — no system needed |
| Networking framework | Roblox `RemoteEvent`/`RemoteFunction` + Knit |

---

## Deferred (Post-Launch Updates)

The following systems were MVP under the Unity scope but are now deferred. Some
may return as post-launch updates; others are cut entirely.

| System | New Priority | Reason |
|--------|--------------|--------|
| Inventory (separate GDD) | Folded into Crafting & Items | Collapsed to reduce documentation overhead |
| Item Database (separate GDD) | Folded into Crafting & Items | Collapsed to reduce documentation overhead |
| Biome | Cut (single hand-built map) | Single-map MVP |
| Fauna AI | Cut for MVP | Predator only at launch; ambient creatures decorative |
| Base Building | Cut for MVP | Big system, defer to v2 or later |
| Ecological Footprint | Cut for MVP | Tied to Base Building |
| Knowledge/Progression Tracking | Cut for MVP | Recipes unlock via crafting only |
| Adaptive Audio | Cut for MVP | Use Roblox built-in audio sources |
| Player Interaction (separate GDD) | Folded into Player Controller + Resource Node | Boundary too narrow for own GDD |
| Predator Perception (separate GDD) | Folded into Predator AI | Consolidated into one state machine |
| Predator Pathfinding (separate GDD) | Folded into Predator AI | Uses Roblox `PathfindingService` directly |
| Multi-planet | Cut entirely | Single map only; no multi-world vision |
| Co-op (separate GDD) | Implicit in all MVP systems | Server-authoritative is the default |
| Win condition GDD | Folded into Crafting & Items | Win = craft + activate escape beacon (TBD) |

---

## Categories

| Category | Description | Systems |
|----------|-------------|---------|
| **Survival** | The pressure-creating loop | Resource Management, Resource Node |
| **Ecosystem** | The reactive world | Ecological Disturbance |
| **Predator** | The apex threat | Predator AI |
| **Crafting** | Capability progression and the win condition | Crafting & Items |
| **Player** | How the player moves and interacts | Player Controller |
| **UI** | Player-facing readouts | HUD |

---

## Priority Tiers

| Tier | Definition | Target |
|------|------------|--------|
| **MVP** | All 7 systems above. Required for the launch hypothesis test. | 2–4 months |
| **Post-launch v1** | Map variants, predator variants, meta-progression, daily challenges | +2 months |
| **Live ops** | Seasonal events, new maps, new variants, balance tuning | Continuous |

---

## Dependency Map

> **Convention — Inverted Bus.** "Depends on" in this index = a system reads from
> or queries another system's API. Publishing emissions to the Ecological Disturbance
> bus is **not** treated as a dependency in this column — publishers do not couple
> to the bus's runtime state, only to its emission contract (which is a forward-
> obligation tracked in each system's GDD F.2a). This keeps the bus a pure
> publish-target with zero in-bound deps in the table; consumer-side coupling is
> what gates design order. Forward-obligations (publish-side contracts) are listed
> per-system in the GDD `F.2a` Deferred Contracts subsection, not here.

### Foundation Layer (no dependencies)

1. **Ecological Disturbance** — pure event bus + signal definitions. Other systems publish events to and subscribe from it. Dependency-free by construction.
2. **Player Controller** — wraps Roblox `Humanoid` + custom interaction. Depends on no MVP-internal systems for design. (Publishes `Sprint` + `Light` emissions to ED — forward-obligation only, not a consumer-side dep.)

### Core Layer (depends on Foundation)

3. **Crafting & Items** — depends on Player Controller for interaction. Defines item schema (including oxygen consumable + escape beacon recipe). (Publishes `Beacon` + `Craft` emissions to ED — forward-obligation only.)
4. **Resource Management** — depends on Crafting & Items for the oxygen item definition.

### Feature Layer (depends on Core)

5. **Resource Node** — depends on Crafting & Items for yield definitions. (Publishes `Gather` emissions to ED — forward-obligation only.)
6. **Predator AI** — depends on Player Controller (target tracking) **and** Ecological Disturbance (queries the field for hottest hotspot, subscribes to `TierCrossedEvent` etc.). The first true ED consumer.

### Presentation Layer (depends on Feature)

7. **HUD** — depends on Resource Management (oxygen meter), Ecological Disturbance (subscribes to `OnMeterUpdate` + `OnDisturbanceAlert`), Crafting & Items (inventory display).

### Circular Dependencies

- **None.** ED is a publish-target with zero in-bound deps; only Predator AI and HUD consume from it via queries/subscriptions.

---

## High-Risk Systems

| System | Risk Type | Risk | Mitigation |
|--------|-----------|------|------------|
| **Ecological Disturbance** | Design | If players can't read what creates disturbance, the loop feels random. The unique hook fails. | Prototype the legibility signal early. Visible disturbance meter on HUD must update perceptibly when actions are taken. |
| **Predator AI** | Technical + Design | A "dumb" predator breaks the entire premise. Roblox `PathfindingService` may behave unexpectedly with dynamic obstacles. | Designate as highest-priority prototype before architecture is finalized. Run `/prototype predator-ai` early. |
| **Co-op disturbance balance** | Design | More players = more disturbance = harder game. Must feel proportionate, not punishing. | Prototype with 2 vs 4 player squads; iterate on disturbance multipliers. |
| **Player Controller** | Design (empirically observed) | Round-1 review surfaced 28 BLOCKING items across 4 themes; CD verdict ruled Pillar 1 (Quiet Is Power) NOT DELIVERED at the rule level. Foundational rewrite required, not surgical fixes. | Round-2 revision via fresh sessions, one theme per session per `.claude/docs/context-management.md`. Priority: close Pillar 1 transaction-integrity loopholes before any other theme. See [player-controller-review-log.md](reviews/player-controller-review-log.md). |
| **Crafting & Items** | Design (empirically observed) | Round-1 review surfaced 24 BLOCKING items including dominant-strategy collapse (Beacon dominates aid suite; Composite Patch + Wrap are trap recipes), violating Pillar 4 (Rounds Not Saves). Pillar collision on iPhone SE for primary platform. | Round-2 revision targeted (not foundational). Load-bearing CD question to resolve: is Beacon meant to dominate as "you earned the win" payoff, or be one option among several? UX dependency (`/ux-design crafting-bench`) flagged before epic authoring. See [crafting-and-items-review-log.md](reviews/crafting-and-items-review-log.md). |
| **Camera (OQ.3)** | Scope | Player Controller OQ.3 (spectator camera) may resolve as a new MVP system, raising count from 7→8. Reshapes design order and effort estimates if it lands. | Pre-decide OQ.3 in PC round-2 Theme 6 before sprint planning. If new system: insert at Foundation/Feature layer per its actual coupling. |

---

## Recommended Design Order

| Order | System | Layer | Effort (forecast) | Effort (actual to date) | Notes |
|-------|--------|-------|-------------------|--------------------------|-------|
| 1 | **Ecological Disturbance** | Foundation | M | **L+ (6 review rounds + round-7 review + round-7a trivial sweep applied 2026-05-05; round-7a heavy-patches session pending)** | Design first — defines the event contract everyone consumes. Forecast underestimated review depth. Round-6 surfaced 5 new Tier-1s in round-5 cap-rule + predator-listener additions; round-7 fresh-session revision surfaced 17 BLOCKING; round-7a trivial sweep closed 4 by mechanical fix (incl. 2-location residue caught by own-verification grep). |
| 2 | Player Controller | Foundation | S | **L+ (round-1 NEEDS REVISION; CD ruled foundational rewrite)** | Parallels Disturbance in design order. Forecast underestimated; round-2 will be 1.5–2× round-1 effort per [review log](reviews/player-controller-review-log.md). |
| 3 | Crafting & Items | Core | M | **L+ (APPROVED-by-acceptance at round-20, 2026-06-06 — 20 review rounds + 9 authoring passes. Round-20 full panel = NEEDS REVISION but mechanical spine clean for the first time [network authority SOUND 12th round; AC/build/systems lenses clean; the 19-round stale-literal recurring class extinguished in the GDD]; 4 blocking closed in-session + user accepted the revisions, marking Approved rather than running a 7th panel. The recurring reconciliation class had relocated GDD→registry [the `entities.yaml` duplicate-`notes` key]; fixed.)** | After Player Controller. Largest GDD due to consolidation. Forecast (M) drastically underestimated — actual L+ across 20 review rounds; the cross-GDD win-condition coupling (PC/ED/RM/PA) + the continuous-hold finale design drove the round count. |
| 4 | Resource Management | Core | S | — (not started) | After Crafting & Items. |
| 5 | Resource Node | Feature | S | — (not started) | After Crafting & Items. (Publishes to ED — no ED-consumer dep per inverted-bus convention.) |
| 6 | **Predator AI** | Feature | L | — (not started) | Highest-risk system. Prototype before final GDD. After Disturbance + Player Controller. |
| 7 | HUD | Presentation | S | — (not started) | After all consumers exist (Resources, Disturbance, Crafting). |

*Effort: S = 1 session, M = 2–3 sessions, L = 4+ sessions, L+ = 5+ sessions including review rounds. Forecasts are pre-design estimates; actuals reflect observed cost including review/revision cycles.*

---

## Progress Tracker

| Metric | Count |
|--------|-------|
| Total MVP systems | 7 |
| Design docs started | 3 |
| Design docs reviewed | 3 (ecological-disturbance.md ****Round-20 Session A patches APPLIED 2026-05-11** (this session, fresh `/clear` per round-19 CD recommendation; chrusht-direct authoring + 4-layer subagent gates + round-15 protocol + round-17 two refinements; **2 BLOCKING (B-CD18-2 ~165s→~160s arithmetic-double-count closure at 4 citation sites; B-CD18-3 F.2b R17-I-H39e-YAML + R17-I-H39f-YAML rows authored) + 6 IMPORTANT (I-QA18-1/2/3/4/5 + I-GP18-1) + 2 round-19 gameplay-programmer findings (GP19-1 OnPlayerDiedPayload import contract; GP19-2 C.3.4 free-variable hoist) — all 10 patches closed at the rule level**. Subagent gates: static-analysis-first pass + spec-arithmetic validation pass (round-19 NEW protocol layer; re-derived `S(N)=0.65 ⇒ 2s × 79.4 ≈ 159s ≈ ~160s` against the bare formula; confirmed `S_inf = 0.6644` and `−0.5644` coefficient embed first-pulse exemption mechanically — NO double-count) + red-team gameplay-programmer subagent (surfaced 1 BLOCKING R20-RT-1 nested-call form gap + 4 IMPORTANT R20-RT-2/3/4/5 + 1 NICE-TO-HAVE R20-RT-6, all 6 closed in-session per same-session-update rule) + cross-section consistency qa-lead subagent (surfaced 1 BLOCKING R20-CS-1 F.2b R17-I16-14 rationale-drift back-edit, closed in-session per same-session-update rule). GDD grew 2374 → ~2440 lines (+66 net). Per-canonical-name fanout grep PASS on all 5 names — no regression of round-17 closure. Round-20 Session B + Session C patches PENDING in subsequent fresh `/clear` sessions per CD recommendation. **DO NOT predict APPROVED for round-21 verdict review** — seventh-consecutive-falsified-APPROVED-prediction rule applies if implementability ceiling not closed by Session B+C. Earlier round-19 verdict review COMPLETE 2026-05-11** (this session, fresh `/clear`; full 7-spec panel + creative-director synthesis ran on un-patched round-18 state per user request — round-19 patch session was the original CD plan; verdict NEEDS REVISION (not light) — 7 BLOCKING + ~24 IMPORTANT; specialist split 7 NEEDS REVISION / 0 APPROVED (network-programmer flagged "NEEDS REVISION (minor)" with no BLOCKING). Round-18 BLOCKING all CONFIRMED still open (B-CD18-1 SQUAD_T threshold inert; B-CD18-2 D.7 ~165s arithmetic double-count; B-CD18-3 F.2b missing R17-I-H39e-YAML + R17-I-H39f-YAML rows). FOUR new BLOCKING surfaced this round: (4) systems-designer BLOCKING-1 D.7 `S_inf = 0.6644` not pinned to explicit `r` (two derivations give 0.6637 vs 0.6653); (5) systems-designer BLOCKING-2 G.5 safe-range "floor noise" rationale conceptually wrong; (6) systems-designer BLOCKING-3 D.1 worked-example expiry `~116 s` inconsistent with stated `initialMagnitude = 0.25` (correct = ~109 s); (7) ai-programmer NEW BLOCKING `:ReleasePredatorLock` yield-free contract (C.1.11 step 4 sub-steps 4a–4c) incompatible with Predator AI's plausible `PathfindingService:FindPathAsync` async-yielding pathfinding architecture; no spec for cancellation-path integration. game-designer's "Player Fantasy fidelity compound" collapsed into B-CD18-1 by CD adjudication. Pillar fidelity: P1 WEAKENED (downgrade from STRENGTHENED-with-caveat per round-18 — round-19 surfaced new arithmetic + yield-free BLOCKING items that weaken Pillar 1 enforcement further); P2 NEUTRAL-with-concern; P3+P4 NEUTRAL. Per-canonical-name fanout grep: PASS on all 5 names — no regression of round-17 closure. CD adjudication: arithmetic surface NOT converging (round-19 surfaced 3 new arithmetic BLOCKING undetected by rounds 12–18); structural surface IS converging (registry hygiene + per-canonical-name fanout PASS). Round-18 "protocol is converging" thesis PARTIALLY FALSIFIED. CD authored 2 new pattern memory files: `feedback_worked_example_arithmetic_trap.md`, `feedback_cross_gdd_contract_owned_by_caller.md` (added to `.claude/agent-memory/creative-director/`). **Round-20 patches recommended: THREE sequential fresh-context sessions** — Session A trivial-sweep (citation-site arithmetic propagation, false explanatory claims, prose fixes); Session B arithmetic + derivation (B-CD18-1 design ruling, S_inf pinning, G.5 row rewrite, D.1 example fix; NEW spec-arithmetic validation pass before this session); Session C cross-GDD seam (pathfinding-yield contract resolution + Predator AI GDD prereq) + IMPORTANT cluster (~24 items including the round-18 18 + 6 round-19 fresh). DO NOT predict APPROVED for round-20 — seventh-consecutive-falsified-APPROVED-prediction rule will apply if round-20 fails. Earlier round-18 entry preserved: round-18 verdict review COMPLETE 2026-05-08 (this session, fresh `/clear`; full 7-spec panel + creative-director synthesis ran per round-18 plan; verdict NEEDS REVISION (light); 3 BLOCKING + 18 IMPORTANT + ~9 NICE-TO-HAVE; specialist split 6 NEEDS REVISION / 1 APPROVED; CD-adjudicated 5 severity disagreements; per-canonical-name fanout grep PASS; pillar fidelity intact P1+P3 STRENGTHENED, P2+P4 NEUTRAL; 3 BLOCKING are B-CD18-1 SQUAD_T threshold inert below D.7 asymptote, B-CD18-2 D.7 ~165s arithmetic double-counts first-pulse exemption, B-CD18-3 H.39e+H.39f YAML forward obligations not recorded in F.2b (3-spec convergence). Defect surface CONVERGING — BLOCKING down 40% from round-16 (5→3). Round-19 patches recommended in single fresh `/clear` session (S-scope; ≤2 hours). DO NOT predict APPROVED for round-19 — sixth-consecutive-falsified-APPROVED-prediction rule applies. CD wrote 3 new pattern memory files. Earlier round-17 entry preserved: round-17 patch session APPLIED 2026-05-08 (chrusht-direct authoring + red-team gameplay-programmer subagent + cross-section consistency qa-lead subagent with round-17 per-canonical-name fanout refinement + static-analysis-first protocol layer; 5 of 5 round-16 BLOCKING + 6 of 15 round-16 IMPORTANT closed inline at the rule level; 9 subagent findings closed in-session per same-session-update rule; 1 red-team finding REJECTED as math error; new CD pattern memory file `feedback_canonical_surface_name_fanout_check.md`; GDD ~2229 → ~2400 lines (+171 net). Earlier round-16 entry preserved: round-16 verdict review COMPLETE 2026-05-07; verdict NEEDS REVISION (small tail); 5 BLOCKING + 15 IMPORTANT + ~3 NICE-TO-HAVE; 4 NEEDS REVISION / 3 APPROVED specialist split; CD-adjudicated 4 severity disagreements; pillar fidelity intact (Pillars 1+3 STRENGTHENED, 2+4 NEUTRAL). Earlier round-15 entry preserved: round-15 heavy cluster APPLIED 2026-05-07 — all 10 round-14 BLOCKING heavy-cluster items + 6 red-team findings + 3 cross-section findings closed inline; GDD ~1966 → ~2300 lines (+334 net). **Round-11 heavy patches APPLIED 2026-05-07** (this session, fresh `/clear` per round-10 CD authoring-method-shift recommendation). All 6 round-10 BLOCKING + 3 multi-spec convergent IMPORTANT clusters absorbed; 8 single-spec IMPORTANTs preserved as F.2a-IMPORTANT R11-I1..R11-I8 forward obligations. DR-4 = Option B BindableEvent monopoly architecture replaces round-9 KnitInit/KnitStart connection-order rule; DR-5 = Option A H.39e CI lint AC authored. Red-team gameplay-programmer subagent (fresh session per CD) ran post-patch and caught 3 BLOCKING + 5 IMPORTANT defects all closed in same session before round-12 panel submission. GDD ~1660 → 1823 lines (+163 net round-11 patches). DO NOT predict APPROVED for round-12 per CD pattern memory at `feedback_mechanical_translation_ceiling.md`. Earlier round-10 entry preserved: round-10 design-review complete 2026-05-06 — verdict NEEDS REVISION (round-9 prediction of APPROVED falsified). Full 7-spec panel + CD synthesis; 6 of 7 NEEDS REVISION; only performance-analyst APPROVED (PA scope-correct only — did not look for the defect classes that broke this round). 6 BLOCKING (3 multi-spec convergent) + 17 IMPORTANT + 5 NICE. Defect surface shifted to authoring-fidelity defects: editorial residue ("wait this is too low; let me revise" literal in normative AC body at H.41l), cross-reference orphans (H.39e cited from C.1.11 step 5 but absent from Section H), unverified Roblox `:Connect` fire-order on Humanoid.Died (3-spec convergent, HIGHEST safety risk on HIGH-risk auto-updating live platform), C.3.4 State C discriminant under-specified, `_capCueDecayPassesRemaining` hardcoded to 5 instead of `math.ceil(1.0 × FIELD_UPDATE_HZ)`. CD pattern observations #1 + #2 BOTH validated again. **NEW pattern persisted as agent memory** — `feedback_mechanical_translation_ceiling.md`: TWO consecutive mechanical-translation rounds have falsified APPROVED predictions (round-7c → 8 fail; round-9 → 10 fail). Mechanical-translation method has a ceiling for L-scope GDDs at round 7+; defect surface shifted to authoring-fidelity which requires out-of-author reviewer. Round-11 plan: authoring method shift — fresh /clear + batched AskUserQuestion design rulings (DR-4 Roblox :Connect strategy / DR-5 H.39e author-vs-remove) + chrusht-direct authors 6 BLOCKING + ONE specialist red-team subagent before round-12 panel; do NOT predict APPROVED for round-12. Earlier round-9 entry preserved: round-9 heavy patches APPLIED 2026-05-06 — closed all 21 round-8 BLOCKING items across 4 convergence clusters + 9 novel singletons via mechanical translation; 3 batched design rulings landed on CD-recommended options (DR-1=A MeterUpdatePayload extension; DR-2=B co-location amendment; DR-3=B fire-ownership move to DisturbanceService); 30 IMPORTANT items folded as round-9 R9-I1..R9-I10 forward obligations; round-9 grep-sweeps verified clean. Earlier round-8 review entry preserved: round-8 review complete 2026-05-06, **MAJOR REVISION NEEDED** — full 7-specialist panel + CD synthesis; ALL 7 specialists returned NEEDS REVISION; ~21 BLOCKING across 4 convergence clusters (C.1.11 ordering / missing API contract — 5-way; H.41j/H.41k test-seam gaps — 3-way; HUD data-surface gap — 2-way; H.36/H.36a profilebegin scope stale — 2-way) + 9 novel singletons (incl. co-located beacons defeat cap intent, solo-nearby player under-filled buffer makes cap unreachable, H.39 grep misses assignment-form, H.39d CI gate bypassable on direct-push-to-main, INVESTIGATE_FALLBACK_DURATION blocks H.30c bootstrap, --!strict nil-guard on Character.PrimaryPart missing, R7c-I1 permanent-Calm attractor unenforced); round-7c prediction of APPROVED falsified; CD pattern-observations: held-out 2-specialist protocol mis-calibrated for round 7+ (needs full panel), mechanical-translation patch sessions are self-validating not self-stressing (need explicit self-stress before APPROVED prediction); round-9 patch session in fresh `/clear` recommended with batched AskUserQuestion design rulings up-front followed by chrusht-direct or specialist-spawned pair-check on Cluster 1; awaiting round-9 fresh session. Earlier ecological-disturbance.md round-7 review complete 2026-05-05, **MAJOR REVISION NEEDED** — 17 BLOCKING + 26 IMPORTANT + 11 NICE-TO-HAVE across 7 specialists + CD synthesis; GDD unchanged since round-5 so all round-6 items remain live; 5-specialist convergence on EmissionType, 4-specialist on OnPredatorLockChanged, 3-specialist on beaconScaleFactor; CD adjudicated 4 specialist disagreements into round-7a design rulings; CD: "Revision velocity now producing as many problems as it solves; document will not converge under round-by-round narrow-scope patches"; CD recommends TWO sequential fresh sessions (round-7a trivial sweep + design-decisions, round-7b held-out adversarial pass); round-7a trivial sweep applied 2026-05-05 (4 of 17 BLOCKING closed); awaiting round-7a heavy-patches session. crafting-and-items.md round-1 review complete 2026-05-01, MAJOR REVISION NEEDED, awaiting round-2 in fresh session; player-controller.md round-6 review complete 2026-05-05, MAJOR REVISION NEEDED, awaiting round-7 TWO sequential fresh sessions) |
| Design docs approved | 1 (crafting-and-items.md — APPROVED-by-acceptance at round-20, 2026-06-06: panel verdict NEEDS REVISION but all 4 blocking closed in-session + user-accepted; mechanical spine clean, network authority SOUND 12th round) |
| Design docs designed (pending review) | 0 |
| Design docs needing revision | 2 — crafting-and-items.md moved to APPROVED-by-acceptance at round-20 (2026-06-06); remaining: ecological-disturbance.md + player-controller.md. (ecological-disturbance.md ****Round-20 Session A patches APPLIED 2026-05-11** (this session, fresh `/clear` per round-19 CD recommendation; chrusht-direct authoring + 4-layer subagent gates + round-15 protocol + round-17 two refinements; **2 BLOCKING (B-CD18-2 ~165s→~160s arithmetic-double-count closure at 4 citation sites; B-CD18-3 F.2b R17-I-H39e-YAML + R17-I-H39f-YAML rows authored) + 6 IMPORTANT (I-QA18-1/2/3/4/5 + I-GP18-1) + 2 round-19 gameplay-programmer findings (GP19-1 OnPlayerDiedPayload import contract; GP19-2 C.3.4 free-variable hoist) — all 10 patches closed at the rule level**. Subagent gates: static-analysis-first pass + spec-arithmetic validation pass (round-19 NEW protocol layer; re-derived `S(N)=0.65 ⇒ 2s × 79.4 ≈ 159s ≈ ~160s` against the bare formula; confirmed `S_inf = 0.6644` and `−0.5644` coefficient embed first-pulse exemption mechanically — NO double-count) + red-team gameplay-programmer subagent (surfaced 1 BLOCKING R20-RT-1 nested-call form gap + 4 IMPORTANT R20-RT-2/3/4/5 + 1 NICE-TO-HAVE R20-RT-6, all 6 closed in-session per same-session-update rule) + cross-section consistency qa-lead subagent (surfaced 1 BLOCKING R20-CS-1 F.2b R17-I16-14 rationale-drift back-edit, closed in-session per same-session-update rule). GDD grew 2374 → ~2440 lines (+66 net). Per-canonical-name fanout grep PASS on all 5 names — no regression of round-17 closure. Round-20 Session B + Session C patches PENDING in subsequent fresh `/clear` sessions per CD recommendation. **DO NOT predict APPROVED for round-21 verdict review** — seventh-consecutive-falsified-APPROVED-prediction rule applies if implementability ceiling not closed by Session B+C. Earlier round-19 verdict review COMPLETE 2026-05-11 — verdict NEEDS REVISION (not light); 7 BLOCKING + ~24 IMPORTANT** (full 7-spec panel + CD synthesis on un-patched round-18 state); 3 round-18 BLOCKING CONFIRMED + 4 NEW BLOCKING (D.7 S_inf pinning, G.5 safe-range rationale wrong, D.1 worked-example expiry mismatch, `:ReleasePredatorLock` yield-free vs pathfinding incompatibility). Pillar 1 WEAKENED. Round-20 plan: THREE sequential fresh-context sessions (trivial-sweep → arithmetic-derivation → cross-GDD seam + IMPORTANT cluster). Earlier round-18 entry preserved: round-18 verdict review COMPLETE 2026-05-08 — verdict NEEDS REVISION (light); 3 BLOCKING + 18 IMPORTANT (full 7-spec panel + CD synthesis); B-CD18-1 SQUAD_T threshold inert below D.7 asymptote, B-CD18-2 D.7 ~165s arithmetic double-counts first-pulse exemption, B-CD18-3 H.39e+H.39f YAML forward obligations missing from F.2b (3-spec convergence + same-session-update violation). Defect surface CONVERGING — BLOCKING down 40% from round-16 (5→3); per-canonical-name fanout grep PASS; pillar fidelity intact (P1+P3 STRENGTHENED, P2+P4 NEUTRAL). Round-19 patches recommended in single fresh `/clear` (S-scope; ≤2 hours). DO NOT predict APPROVED for round-19. Earlier round-17 entry preserved: round-17 patch session APPLIED 2026-05-08 — all 5 round-16 BLOCKING + 6 of 15 round-16 IMPORTANT closed inline at the rule level; red-team gameplay-programmer subagent (round-11 protocol) + cross-section consistency qa-lead subagent (round-13 protocol with round-17 per-canonical-name fanout refinement) + static-analysis-first authoring pass (round-15 protocol) all ran; 9 subagent findings closed in-session per same-session-update rule; 1 red-team math-error finding REJECTED; new CD pattern memory file `feedback_canonical_surface_name_fanout_check.md`. Earlier round-11 entry preserved: round-11 heavy patches APPLIED 2026-05-07 — all 6 BLOCKING + 3 multi-spec convergent IMPORTANT clusters closed; red-team subagent caught + closed 3 additional BLOCKING + 5 IMPORTANT in same session; pending round-12 verdict in fresh `/clear` session, do NOT predict APPROVED per CD mechanical-translation-ceiling pattern memory; earlier round-10 entry preserved: round-10 review complete 2026-05-06 NEEDS REVISION; 6 BLOCKING + 17 IMPORTANT; crafting-and-items.md round-2 pending; **player-controller.md round-6 design-review COMPLETE 2026-05-05 verdict MAJOR REVISION NEEDED — round-5 patch was PARTIALLY APPLIED (R5-B1 + R5-B2 closures only; R5-B3/B4/B5/B6 SKIPPED — carried into round-6 as R6-B1/B2/B3/B4 with 5-specialist convergence on R6-B1 + 4-specialist convergence on R6-B4). 8 NEW BLOCKING introduced by R5-B2's reconciliation-tick design surface (R6-B5 `deathDeductionCommitted` T6-clearance gap = same defect class as round-4 B6 reproduced for new flag, 3-specialist convergence; R6-B6 registry divergence; R6-B7 testability seam gap; R6-B8 Pillar 1 silent reopen via failure-then-reconciliation path; R6-B9 dual-table keying contradiction with F.4, 3-specialist convergence; R6-B10 step-5 warn runtime crash on destroyed Player; R6-B11 zero AC coverage for tick subsystem; R6-B12 Death SFX false-cue on `reconciliation-recovery` broadcast). 12 BLOCKING + 12 IMPORTANT total. CD: "Patch perimeter has regressed. Pillar 2 does NOT hold honestly." Meta-pattern: heavy design surface co-resident with trivial fixes ate the trivial fixes. **Round-7 patch sequencing per CD recommendation: TWO SEQUENTIAL FRESH SESSIONS — Session 1 trivial sweep (~30-45 min) before Session 2 reconciliation-tick lifecycle patch (~2-3 hours). Do NOT attempt in one session.** After round-7 patches both land, run round-8 design-review for verdict before advancing to Theme 2. Original round-5 design-review verdict was NEEDS REVISION — round-4 closures B6–B11 verified at rule level but round-4 patch prose introduced 6 NEW BLOCKING at three seams: R5-B1 Path B + lantern axis (Pillar 1 phantom emissions); R5-B2 B8 concurrent-path rollback race (Pillar 2 silent cost-loss, 4-specialist convergence); R5-B3 C.5.3 predicate violates C.11 (testability undone); R5-B4 entities.yaml `pulseEmissionMagnitude` pre-B9 ordering (registry divergence — same class as round-3 B5); R5-B5 entities.yaml `GRACE_REENTRY_COOLDOWN` missing `>=` annotation (B11 incompletely applied); R5-B6 `PING_ORIGIN_TOLERANCE` undefined + camera-angle impossible (trust-boundary spec-completeness, CD-reframed Theme-1-spec/Theme-2-tuning). CD: "Not the design is broken — the patch prose introduced ambiguity at three seams. Round-5 is the cleanup pass." Round-5 patch pending fresh session (~2-3 hours, scope M, 5 textual fixes + 1 design decision on R5-B2 failure-recovery semantics + ~3-4 IMPORTANT pair-bound prose additions including 2 carried-unfulfilled audio IMPORTANTs from round-4 F4-R4/F6-R4); after patch lands, run round-6 design-review to verdict before advancing to Theme 2. Themes 2/3/4 + cross-cut + OQ.3 + HUD GDD forward obligations still pending fresh sessions**) |

**Trajectory note**: PC + Crafting both moved Designed→NEEDS REVISION on 2026-05-01 after round-1 design-review verdicts. The "designed (pending review)" count was 2 on 2026-04-30 immediately after authoring; round-1 reviews surfaced major blockers and reset both to NEEDS REVISION. This is normal for the first review pass; converging on APPROVED typically takes 2–6 review rounds in this project.

(Unity-era progress reset at pivot. Archived GDDs: `design/archive/unity-scope/gdd/`.)

---

## Next Steps

- [x] `/art-bible` — Roblox visual identity derived; output: [`design/art/art-bible.md`](../art/art-bible.md). Unity art bible archived under `design/archive/unity-scope/`.
- [x] `/design-system ecological-disturbance` — drafted 2026-04-29; **rounds 1–12 of design-review complete (12 fresh-session re-review cycles); round-12 verdict MAJOR REVISION NEEDED 2026-05-07 (8 BLOCKING + 15 IMPORTANT; 4 BLOCKING are 3-spec convergent).** Round-11's red-team intercept closed section-local defects but round-12 surfaced a new cross-section coherence defect class. Round-13 plan: chrusht-direct patches + red-team subagent + new cross-section consistency pass (per updated CD pattern memory). Defines the central event contract everyone consumes.
- [x] `/design-system player-controller` — drafted 2026-04-30; round-1 design-review complete 2026-05-01; verdict MAJOR REVISION NEEDED (28 BLOCKING across 4 themes; round-2 pending fresh session). Camera-system decision (OQ.3) pending — may raise MVP system count from 7 to 8.
- [x] `/design-system crafting-and-items` — collapsed Crafting + Inventory + Item Database GDD (Designed 2026-04-30). **Rounds 1–12 of design-review complete; round-12 full-panel closure gate (2026-06-04) → NEEDS REVISION (12th consecutive non-APPROVED, headline = KITING). ROUND-13 authoring pass + both narrow gates done 2026-06-04** (KITING ruling = fail-fast continuous-hold + ~3 s grace; all round-12 blockers + all gate findings closed incl. a BLOCKING respawn-re-add buffering fix; network AUTHORITY SOUND 5th round; ACs 109). NEXT = round-14 full 7-spec panel (the closure gate); DO NOT predict APPROVED. NOT committed (branch `crafting-round2-patch`). See the status table (system 6) + [review log](reviews/crafting-and-items-review-log.md). **UX dependency**: `/ux-design crafting-bench` required before epic authoring per UI.5.
- [ ] `/design-system resource-management` — oxygen-only
- [ ] `/design-system resource-node`
- [ ] `/prototype predator-ai` — before authoring the Predator AI GDD
- [ ] `/design-system predator-ai` — after prototype validates the approach
- [ ] `/design-system hud`
- [ ] `/review-all-gdds` — cross-system consistency check after all 7 are drafted
- [ ] `/gate-check` — phase gate before architecture
