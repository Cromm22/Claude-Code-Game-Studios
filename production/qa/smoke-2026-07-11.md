# Smoke Check Report

**Date**: 2026-07-11
**Sprint**: Sprint 1 (2026-07-06 → 2026-07-20)
**Engine**: Roblox Studio (live platform) + Luau + Knit; headless test runtime = Lune v0.10.5 (`.tools/lune.exe`, gitignored; CI installs its own via Foreman)
**QA Plan**: Not found — `/qa-plan sprint` was never run (known gap, flagged in `production/sprints/sprint-1.md`'s own ⚠ No QA Plan warning block). Smoke checklist drawn from `tests/smoke/critical-paths.md` instead.
**Argument**: sprint
**Build/commit**: `46103e0` (branch `crafting-round2-patch`, working tree clean at time of check)

---

## Automated Tests

**Status**: PASS — 24/24 test files, 0 failures, exit code 0
**Command**: `.tools/lune.exe run tests/run_tests.lua tests/unit tests/integration`
**CI**: configured (`.github/workflows/tests.yml` — Foreman-installed Lune; plus `gdd-invariants.yml`). NOTE: CI has never actually fired — branch pushes alone don't trigger the workflow; it needs a PR to `main` or a direct push to `main`. All 24/24 results in this report are from the local Lune runner, independently re-verified by the coordinator after every story's code-review fix pass.

Known headless-harness limits (each disclosed in the relevant test file, not silently skipped): live Roblox/Knit instances cannot be constructed under Lune, so Knit-wrapper behavior is proven via disclosed Fake-mirror harnesses + static/structural source pins; PERF ACs (H.28/H.29-class) and `game:BindToClose`/`Player:LoadCharacter` live semantics are DEFERRED to the `/prototype predator-ai` spike / first Studio boot, per the affected stories' own Out-of-Scope sections.

---

## Test Coverage

All 15 implemented (done) sprint stories — coverage verified individually at each story's `/story-done` closure against its declared Test Evidence path:

| Story | Type | Test File | Status |
|---|---|---|---|
| rc-1 Core Aggregator | Logic | `tests/unit/runcontroller/` (aggregator suite) | COVERED |
| rc-2 GetRunEndedSignal | Integration | `tests/integration/runcontroller/get_run_ended_signal_test.luau` | COVERED |
| ed-1 Constants/Bootstrap | Integration | `tests/integration/ecological-disturbance/service-bootstrap-config-validation_test.luau` | COVERED |
| ed-2 Emit() intake | Logic | `tests/unit/ecological-disturbance/emission-schema-server-emit-intake_test.luau` | COVERED |
| ed-3 Spatial grid | Logic | `tests/unit/ecological-disturbance/spatial-grid-live-source-cap-eviction_test.luau` | COVERED |
| ed-4 Attribution archive | Logic | `tests/unit/ecological-disturbance/attribution-archive-ttl-cap_test.luau` | COVERED |
| ed-5 Decay/fieldValue | Logic | decay/falloff suites (incl. NaN-clamp regression) | COVERED |
| ed-6 Tier classification | Integration | `tests/integration/ecological-disturbance/tier-crossed-signal-subscription_test.luau` | COVERED |
| ed-7 Squad aggregate t | Logic | `tests/unit/ecological-disturbance/squad-aggregate-t-accessor_test.luau` | COVERED |
| ed-8 Hottest hotspot | Logic | hotspot query suite | COVERED |
| ed-9 Band-crossed signal | Integration | `tests/integration/ecological-disturbance/disturbance-band-crossed-signal_test.luau` | COVERED |
| ed-10 Died monopoly/fan-out | Integration | `tests/integration/ecological-disturbance/humanoid-died-monopoly-onplayerdied-fanout_test.luau` | COVERED |
| ed-14 Streaming/flora push | Integration | `tests/integration/ecological-disturbance/streaming-chunk-membership-flora-push_test.luau` | COVERED |
| pc-1 Locomotion/stamina | Logic | locomotion/stamina suite | COVERED |
| pc-2 Lantern/gather gate | Logic | `tests/unit/player-controller/lantern-gather-gate_test.luau` | COVERED |
| pc-3 Emission publisher | Logic | `tests/unit/player-controller/disturbance-emission-publisher_test.luau` | COVERED |
| pc-4 Death-cost reconciliation | Logic | `tests/unit/player-controller/death-cost-reconciliation-core_test.luau` | COVERED |
| pc-5 T5/T6 side-effects | Integration | `tests/integration/player-controller/death-respawn-side-effects_test.luau` | COVERED |
| pc-6 Path B/grace timer | Logic | `tests/unit/player-controller/path-b-oxygen-grace-timer_test.luau` | COVERED |
| pc-7 Input locks/renderScope | Integration | `tests/integration/player-controller/dead-player-input-locks-renderscope_test.luau` | COVERED |
| pc-9 Run-end contract | Integration | `tests/integration/player-controller/run-end-contract_test.luau` | COVERED |
| pc-11 NetworkOwnership/bandwidth | Integration | `tests/integration/player-controller/networkownership-bandwidth_test.luau` | COVERED (2 PERF items PENDING → prototype spike, per story Out-of-Scope) |

**Summary**: 22 done stories, all COVERED; 0 manual-type stories; 0 MISSING; 12 backlog stories unimplemented and out of smoke scope.

*(Correction 2026-07-11, caught by the /team-qa qa-lead's consistency pass: this summary originally said "15 done stories" — a coordinator tally error. The authoritative count from `production/sprint-status.yaml` is 22 done / 12 backlog of 34, matching the table above. Process note: derive this count programmatically from the yaml, not by hand.)*

---

## Manual Smoke Checks

Per user confirmation 2026-07-11: **ALL manual items N/A — no playable build exists yet.** `tests/smoke/critical-paths.md`'s own header notes the core-mechanic entries are "currently unimplemented, Pre-Production." Specifically:

- [-] Items 1–3 (lobby launch/run start/UI input) — N/A: no lobby/HUD exists (HUD epic has no sprint-1 stories); `Packages/` is unvendored (Knit/Signal — **TD-019**) so no Studio/Rojo boot is currently possible
- [-] Items 4–8 (core mechanic loop) — N/A: placeholders in the checklist itself, pending the vertical slice's first playable
- [-] Item 9 (ProcessReceipt) — N/A: Save/Load (ADR-0007) fully deferred from the vertical slice
- [-] Item 10 (rate-limit drop) — covered at the AUTOMATED level (pc-1/pc-2's rate-limit tests + pc-7's flood/no-anchor-perturbation test); live RemoteEvent behavior pending first boot
- [-] Items 11–12 (framerate/bandwidth) — N/A: DEFERRED to the `/prototype predator-ai` spike per ADR-0008's own Validation Criteria

---

## Missing Test Evidence

None. All Logic and Integration stories marked done have passing automated evidence at their declared paths.

---

## Verdict: **PASS**

- Automated tests: PASS (24/24, exit 0)
- All manual checks: N/A (user-confirmed — no playable build exists in Pre-Production; this is expected state, not a failure)
- No MISSING test evidence

**Carried context for QA hand-off (advisory, not gating)**:
1. No QA plan exists (`/qa-plan sprint` never run) — `/team-qa sprint` will need to generate its own test plan as part of its cycle.
2. The un-bootable state (TD-019: Packages unvendored) bounds what manual QA can do — QA scope is effectively the automated suite + document/code review until the first Studio boot (natural home: the `/prototype predator-ai` spike).
3. Open tech debt going into QA: TD-001/002/003 (pc-2 advisories), TD-005 (touch platform hardcode — do NOT trust mobile playtests), TD-006 (OnSprintPulse unowned), TD-007 (control-manifest drift), TD-008 (ADR-0004 doc fixes), TD-009 (tier-stuck prune — PA prerequisite), TD-010 (_setTestClock seam), TD-011/012 (ed-10 races/lint robustness), TD-014 (OnPlayerDied payload widening before HUD), TD-015 + TD-017 (RM-async activation-class rulings, bundled), TD-016 (userId-keyed seam GDD sync), TD-018 (SquadStateSnapshot type sync before HUD/analytics), TD-019 (vendor packages). TD-004 and TD-013 are Resolved.
