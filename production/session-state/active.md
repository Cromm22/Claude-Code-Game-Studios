# Session State

## Session Extract — pc-5 /story-readiness+/dev-story+/code-review+/story-done → ALL MUST-HAVES DONE (2026-07-09)
- **pc-5 (Death/Respawn Side-Effects & Cross-Service Signals, Must-Have, 1.5d) closed COMPLETE WITH NOTES — Sprint 1's Must-Have list is now 100% done** (rc-1/2, ed-1/2/3/10, pc-1/3/4/5). Full pipeline: readiness READY (all 4 TR-IDs verified; mocks sanctioned by the story's own text) → dev-story (gameplay-programmer; needed FOUR SendMessage resumes total across implementation+fixes — worst of the session, matching the ed-14 precedent) → code-review (2 parallel specialists; engine needed 1 resume, QA needed 1) → CHANGES REQUIRED → all fixes applied → story-done. Suite 21/21, exit 0, independently verified after implementation AND after fixes.
- Implementation: `PlayerControllerDeathSideEffectsLogic.luau` (new pure selection module: ResolveDeathCause/ShouldFire*PreYield/SelectPostCommitSideEffects keyed on dispatchOrigin×pathOrigin) + PC service wiring (pre-yield g/h/i, origin-selected commit sets, `_completeRespawn` T6 payload, `OnPlayerDied` Client signal per ADR-0006 registry #10, `_deathEffectsState` e/f proxies, `_isPlayerDead` heartbeat-skip stamina freeze, 6 new injectable seams). **Key design: dispatchOrigin/pathOrigin as call-scoped params — ADR-frozen ReconcileRow untouched, zero pc-4 logic/test changes needed; engine review confirmed the ADR supersedes the GDD's fuller row prose.** Path-B seam ready for Story 006: `_beginDeathFlow(player, now, "B")`.
- **Code review verdict CHANGES REQUIRED — 2 real BLOCKING engine findings, both fixed same-session**: (1) T6 fires for departed players (TryRespawn never checks liveness — this DISPROVED part of TD-013's recorded claim) and `LoadCharacter()` on the departed instance would error, aborting the shared loop and silently skipping a live teammate's respawn that tick → fixed: liveness re-validation inside a now-`task.spawn`'d per-player T6 remainder + pcall backstop; (2) `LoadCharacter()` ran synchronously inside the Heartbeat handler against the file's own yield-discipline pattern → same fix. QA caught a **false test-comment coverage claim** (an isSameLife object-identity assertion that didn't exist — now real) + 3 assertion-tightening gaps (H.72 at-fire-time capture, H.75 5th proxy, H.70 grace sentinels) — all applied, plus 3 new static pins (T6 ordering, LoadCharacter pcall, PlayerRemoving clears). The fix pass also caught+fixed a genuine Luau ambiguous-syntax parse error in its own new test code via loadstring pre-verification.
- `docs/tech-debt-register.md`: **TD-013 CORRECTED** (T6-fires-for-departed-players; scope re-specified for Story 006 = 3 tables + departed-T6 state recreation + `_deathEffectsState` late-recreation; hook-idempotency facet CLOSED by dispatchOrigin). **TD-014** (OnPlayerDied payload omits GDD-required deathTimestamp/deathEventId — widen before Story 007/HUD consume the 2-arg shape). **TD-015** (stale-INITIAL-dispatch races a newer life — isSameLife deliberately reconciliation-only; dormant until RM's real async call; needs ruling). **TD-016** (userId-keyed PredatorService seams vs the approved GDD's `(player)` text — reconcile at PA-epic first touch, flag to game-designer).
- `production/sprint-status.yaml`: `pc-5` → done. Blockers cleared: **pc-7** (TD-014 note embedded), **pc-9** (Crafting still mocked). Remaining unstarted: pc-6/pc-7/pc-8/pc-9 (should-have), ed-11/12/13/15/16/17 + pc-10/12/13/14/15 (nice-to-have; ed-12/15/16/17, pc-10/12/13/15 still dep-blocked).
- Not yet committed: the ENTIRE pc-4 + pc-5 batch (both implementations, both test files, all review fixes, TD-013..016, both story files, sprint status, session state). Last commit `34898c5` (ed-9+ed-10).

**NEXT — SPRINT CLOSE-OUT SEQUENCE (all Must-Haves done)**: (1) `/smoke-check sprint` → (2) `/team-qa sprint` → (3) `/gate-check` only after QA returns APPROVED/APPROVED WITH CONDITIONS. Alternative: pull in should-have stories first (pc-6/pc-7/pc-8/pc-9, ed-11/ed-13 ready) — user's call. Strongly consider committing the pc-4+pc-5 batch first. Carried into close-out: TD-014/015/016 + the LoadCharacter Studio-verification item + the no-QA-plan warning from sprint planning.

---

## Session Extract — commit `34898c5` + pc-4 /story-readiness+/dev-story+/code-review+/story-done (2026-07-09)
- **Committed `34898c5`** (user-directed): the full ed-9+ed-10 batch — 13 files, +2127 lines. Branch `crafting-round2-patch`, NOT pushed.
- **pc-4 (Death-Cost Reconciliation Core, Must-Have, 2.0d) closed COMPLETE WITH NOTES** — the full pipeline ran: `/story-readiness` READY (all 8 TR-IDs active+matching, ADR-0005 Accepted, manifest current; RM receiver = sprint-sanctioned mock) → `/dev-story` (gameplay-programmer; one SendMessage resume needed — stalled at "Now the KnitStart additions:", the session-recurring pattern) → `/code-review` (2 parallel specialists, BOTH delivered without resume) → fixes → `/story-done`. Suite 19/19 → **20/20, exit 0**, independently re-verified after every fix.
- Implementation: `PlayerControllerDeathReconciliationLogic.luau` (new zero-require pure state machine — mint/BeginDispatch/TryCommitDispatch-with-post-yield-recheck/rollback/staleness-sweep/TryRespawn/AbandonRow) + PC service wiring (GetOnPlayerDiedSignal subscription in KnitStart, 3 injectable seams in KnitInit incl. the RM stub `_requestSquadOxygenSpendFn` [warn-once + unconditional success], third Heartbeat connection, real `game:BindToClose` registration) + 30-test-scenario unit file. **Split-dispatch design** (BeginDispatch/TryCommitDispatch as separate primitives) chosen specifically to make H.54's mid-yield race deterministically testable under Lune — engine reviewer traced it race-proof for every specified scenario. ADR-0005's open staleness-threshold question resolved as `2×RECONCILIATION_TICK_INTERVAL=10s` (named constant, GDD H.38/H.54-derived).
- Code review: **APPROVED WITH SUGGESTIONS** (engine MINOR NOTES — ADR-0005/0001/0004 all PASS; QA TESTABLE with 4 non-blocking gaps). **Headline finding (engine Finding 4): the implementation's "leak bounded by squad size ≤4" header claim was WRONG** — a player disconnecting mid-death leaves a stale `currentDeathRecord` pointer that makes the H.39 orphan check retain their committed row PERMANENTLY; the real bound is cumulative mid-death disconnects per session. Window nearly unreachable today only because the RM stub is synchronous. Header comment corrected; **TD-013 logged** covering both facets + the pre-yield-hook-fires-per-retry idempotency requirement for Story 005's payloads.
- Fixes applied same-session: `DEATH_OXYGEN_SPEND_AMOUNT`/`_REASON` named constants (real+Fake call sites); warn-once flag moved class-table→instance (`self._hasWarnedMissingResourceService`, the forbidden static-singleton shape); `BeginDispatch` Pending-precondition assert; +3 tests (wrapper-level H.54 with spend-call-count==2/one-commit via a re-entrant seam fn; multi-player same-tick recovery+respawn independence; RM-stub static shape pin).
- `production/sprint-status.yaml`: `pc-4` → done (2026-07-09). Blockers cleared: **pc-5** (Must-Have — now the LAST unstarted Must-Have; TD-013 idempotency note embedded in its blocker field), **pc-6** (TD-013 teardown note embedded), **pc-14** (nice-to-have).
- Not yet committed: everything pc-4 (implementation, tests, review fixes, TD-013, story file, sprint status, this entry).

**NEXT**: **pc-5 (Death/Respawn Side-Effects, Must-Have, 1.5d) is the final unstarted Must-Have** — after it closes, the Must-Have list is done → sprint close-out (`/smoke-check sprint` → `/team-qa sprint` → `/gate-check`). pc-5's implementer MUST read pc-4's Completion Notes + TD-013 first (hook idempotency per deathEventId; `ResetGraceAnchorsOnDeath` is an existing unwired seam to call from the commit hook; `_checkRespawnTimers` needs the real T6 payload call site added). Also ready: pc-6, ed-11, ed-13, pc-14. Consider committing the pc-4 batch first.

---

## Session Extract — resume: ed-10 recovered + /code-review + /story-done (2026-07-09)
- **Session opened on "resume" and discovered unrecorded work**: an interrupted 2026-07-08 session (after the ed-9 extract below was written) had fully implemented `ed-10` (Humanoid.Died Monopoly & OnPlayerDied Fan-out — Must-Have) — `DisturbanceServiceDeathFanoutLogic.luau` + integration test + `DisturbanceService.luau` wiring, suite 18→19 files all passing — but died before code review, story closure, sprint-status update, or any session-state entry. The empty `Packages/`+`src/controllers/` `.gitkeep` dirs are that session's harness/Rojo-mapping placeholders (per `default.project.json`), harmless.
- Completed the broken pipeline: `/code-review` (2 parallel specialists) → verdict **APPROVED WITH SUGGESTIONS** → cheap fixes applied → `/story-done` → **COMPLETE WITH NOTES**. Suite re-verified 19/19, exit 0 after all fixes.
- **One real defect found and fixed**: `_deathLockSnapshot` had no `PlayerRemoving` cleanup — identical leak class to the `_loadedChunks`/`_floraDeltaCache` clears in the SAME handler (both reviewers found it independently; QA filed it as a formal S3 bug). Fixed with the matching one-line clear + a static regression test pinning the clear inside the handler.
- Review hardenings applied same-session: double-fire test now wraps EACH `_onHumanoidDied` call in its own coroutine-completion yield-free proof (the AC says "on EACH fire, not just the first" — previously only fire 1 was runtime-proven); the snapshot-ordering static test now bounds its search to `_onHumanoidDied`'s body (was incidentally-correct whole-file-tail search); `stripLuaComments`'s caveat comment re-scoped (its "safe for code-structure checks" claim was FALSE for the repo-wide monopoly grep — a string literal containing `--` before a rogue `.Died:Connect(` would false-negative the security-critical check).
- `docs/tech-debt-register.md`: **TD-011 added** (PlayerAdded/CharacterAdded already-existing-instance races — a player present before KnitInit's connect line, or a character spawned before the nested connect, is silently never death-instrumented; needs an explicit design call since the standard guards touch the statically-pinned monopoly chain — target Story 011's touch). **TD-012 added** (textual monopoly grep is early-warning only — comment-stripper false-negative + 4 indirection evasions documented; Story 016's H.39e lint must be AST-robust to both classes). **TD-010 extended** (`deathTimestamp` now also under the no-`_setTestClock`-seam caveat, same `_setNow` workaround).
- ADR compliance verified twice (coordinator + engine specialist): ADR-0001/0004/0005 all COMPLIANT — raw BindableEvent per the frozen C.1.11 closure, KnitInit chain per the corrected ADR-0004 diagram, no-dedup per ADR-0005's consumer-owned-idempotency decision. N10's per-pass registry snapshot correctly still open (Story 011's obligation, stub in place).
- `production/sprint-status.yaml`: `ed-10` → `done` (2026-07-09). Blockers cleared: **ed-11** (deps ed-1+ed-10 both done), **pc-4** (Must-Have — hard blocker ed-10 done; RM receiver still mocked per sprint-1 plan), **ed-13** (stale — deps ed-6/ed-7 closed 07-08, blocker never cleared by that batch).
- Both review subagents delivered final reports WITHOUT needing a SendMessage resume — first batch this sprint to break the every-agent-stalls pattern.
- Not yet committed: everything from ed-9 + ed-10 (implementation, tests, review fixes, register, story files, sprint status, this entry). Last commit remains `c79a488`.

**NEXT**: Must-Have remaining: **pc-4** (Death-Cost Reconciliation Core, 2.0d, now unblocked — needs the RM `RequestSquadOxygenSpend` mock) then **pc-5** (depends on pc-4). Also ready: ed-11, ed-13 (nice-to-have). Consider committing the two-story backlog (ed-9+ed-10) before continuing — nothing has been committed since `c79a488`. After pc-4/pc-5, the Must-Have list is done → sprint close-out sequence (`/smoke-check sprint` → `/team-qa sprint` → `/gate-check`).

---

## Session Extract — /dev-story+/code-review+/story-done ed-9 (2026-07-08)
- `ed-9` (OnDisturbanceBandCrossed Squad-Aggregate Band-Crossing Signal) was the sole `ready-for-dev` story (only unblocked by ed-6/ed-7's prior closure). Implemented via a single dev-story dispatch (no batching needed — one story, no shared-file conflict risk this time). Test suite grew 17→18 files, 18/18 passing, exit 0.
- Clean reuse of Story 006's `TierClassificationLogic.ClassifyWithCascade` via callback injection (the implementer's own first draft used a direct `require()`, hit the known `script.Parent`-relative-require-doesn't-resolve-under-Lune landmine, and self-corrected before delivery — good instinct, no coordinator intervention needed).
- Code review: **APPROVED WITH SUGGESTIONS**, no required changes. Reviewer independently traced `TierAt`'s branch logic across every threshold region to verify the story's most load-bearing correctness claim (a single squad-tier cascade can never change direction mid-cascade) — confirmed correct, directly validating the H.28d(e) "exactly 3 fires, all `direction=down`" AC.
- One review suggestion applied directly: added 5 stub-based tests (`DirectionFor` across all 6 tier-pair orderings + `ClassifySquadBandCrossing` against a fully-controlled stubbed cascade function) to decouple this story's own orchestration-logic correctness from Story 006's real cascade math, which every prior test in the file had been implicitly coupled to.
- `docs/tech-debt-register.md`: **TD-010 added** — the story's own AC text promises a `_setTestClock` seam (inherited verbatim from the GDD) that doesn't exist anywhere in this codebase yet; pre-existing gap, not introduced by ed-9, worked around with a harness-local clock field matching Story 006's precedent. Low priority — re-verify once the real seam eventually lands for H.5/H.5b/H.34.
- `production/sprint-status.yaml` updated: `ed-9` → `done`. No downstream stories unblock (`Unlocks: None` — consumed by Player Controller, outside this epic).
- Two agents in this batch (both the implementer and the reviewer) each needed one `SendMessage` resume to produce a final report instead of a partial progress narration — same recurring pattern as every batch this session.
- Not yet committed: `ed-9`'s implementation + the tech-debt register update + this session-state entry. (Everything from the prior 9-story batch, plus all Pre-Production planning artifacts, was already committed and pushed to `origin/crafting-round2-patch` at `c79a488` earlier this session.)

**NEXT**: No more `ready-for-dev` stories exist in the current sprint status as of this closure — check `production/sprint-status.yaml` for any newly-unblocked stories, or consider whether it's time for `/smoke-check sprint` → `/team-qa sprint` → `/gate-check` per the sprint close-out sequence, since the Must-Have story list should be re-examined against what's actually done now. Consider committing this small ed-9 batch before continuing.

---

## Session Extract — /dev-story+/code-review+/story-done ed-6+ed-7+ed-8+ed-14 (2026-07-08)
- All 4 newly-unblocked ED stories implemented in two waves to manage shared-file collision risk on `DisturbanceService.luau`'s `_runFieldUpdatePass` loop: **Wave 1 (parallel): ed-7 (GetSquadAggregateT) + ed-8 (GetHottestHotspot)** — both self-contained on-demand accessors, low interaction risk. **Wave 2 (parallel, dispatched after Wave 1 landed): ed-6 (Tier Classification) + ed-14 (Streaming/Flora Push)** — both hook a new step into the same shared loop, the genuinely higher-risk pair. All 4 landed cleanly; final suite 17/17 passing, exit 0.
- **ed-6's dev-story agent surfaced 5 genuine design ambiguities before writing any code** (downward-threshold literal-vs-computed, whether to register a new `RETREAT_THRESHOLD` constant, which positions get classified each pass, `initialMagnitude` semantics under coalescing, signal accessor naming) — good practice, not guessed silently. Resolved all 5 with reasoning, including an independent IEEE-754 floating-point check (via a standalone Lune script) confirming `TENSE_THRESHOLD - HYSTERESIS_BAND` etc. are bit-identical to the story's literal pseudocode values, so computing inline (satisfying the never-hardcode standard) carries zero precision-drift risk.
- **A real bug was found in already-closed `ed-5` code**, surfaced by ed-8's own testing rigor: `DisturbanceServiceDecayFalloffLogic.luau`'s `FieldValueAt` clamp (`math.min(1.0, total)`) silently laundered a NaN `total` into a finite `1.0` (Retreat-tier, maximum severity) instead of failing safely, because Luau's `math.min(a,b)` returns `a` whenever `b` is NaN — independently verified via a standalone Lune script before fixing. Fixed same-session (argument order swap to `math.min(total, 1.0)`), with a dedicated regression test added and a stale test (which had been asserting the *buggy* behavior as expected) corrected to assert the fixed behavior instead. Both code reviews independently hunted for the same argument-order footgun class elsewhere in the epic and found no other instance.
- Code review (2 combined reviews, matching the implementation-wave split): **ed-7+ed-8: APPROVED WITH SUGGESTIONS.** Independently confirmed ed-8's self-flagged ADR-0004 deviation (a new `QueryCellsInRadius` primitive instead of the ADR's literal fixed-3×3-scan wording) is actually *ADR-0004's own documentation that's wrong* — ed-8's implementation correctly matches the GDD's `searchRadius`-scaled cost model (32–512 studs; a fixed 3×3/96-stud footprint cannot serve that range). **ed-6+ed-14: APPROVED WITH SUGGESTIONS.** Verified the two new `_runFieldUpdatePass` steps are genuinely order-independent (not just asserted). Sharpened ed-6's self-disclosed "tier-stuck-elevated" gap into a more concrete failure mode: a stale elevated tier entry can "poison" an unrelated new source that later lands on the same grid cell, not just persist harmlessly.
- Two cheap review follow-ups applied directly (not deferred): a composed cross-module test proving H.25c(d)'s claim end-to-end (`squadT=0.0` under real NaN corruption, both `GetSquadAggregateT` argument forms) added to `squad-aggregate-t-accessor_test.luau`; a doc-comment clarification in `DisturbanceServiceHotspotQueryLogic.luau` noting the `+math.huge` disqualification branch is currently unreachable via the real composed path (defense-in-depth only, not a live gap).
- `docs/tech-debt-register.md`: **TD-008 added** (two ADR-0004 documentation-only inaccuracies — the conflated cell-radius-scan guardrail sentence, and `:Fire` vs the correct `:FireClient` in the Key Interfaces sample; both confirmed as ADR bugs, not implementation defects). **TD-009 added** (ed-6's tier-stuck-elevated gap, explicitly flagged as a prerequisite before Predator AI subscribes to `GetTierCrossedSignal()`, not just a someday-follow-up).
- `production/sprint-status.yaml` updated: `ed-6`, `ed-7`, `ed-8`, `ed-14` → `done`. `ed-9` unblocked (`ready-for-dev`) — its only dependencies (ed-6, ed-7) are both now done.
- Process note: this batch involved the most sustained multi-agent-stopping-mid-task pattern of the session — nearly every agent (both implementers and both reviewers) needed at least one `SendMessage` resume to produce a final report instead of a partial progress narration; one agent (ed-14) needed three resumes. Consistent with the session's established recurring pattern, handled the same way each time.
- Not yet committed: this entire batch (ed-6, ed-7, ed-8, ed-14, the ed-5 amendment, the tech-debt register updates) plus the entire prior uncommitted backlog from earlier today (ed-4, ed-5, rc-2, pc-3, pc-11 and everything before that — see the extract below).

**NEXT**: Consider committing the substantial and now quite large uncommitted backlog before continuing — this session has not committed anything yet today. `ed-9` is newly ready-for-dev. TD-005/006/008/009 are all real, named gaps worth deciding on before they're forgotten. Eventually open a PR to `main` for a real CI signal (CI uses its own Foreman-installed Lune, not this local `.tools/lune.exe`).

---

## Session Extract — /dev-story+/code-review+/story-done ed-4+ed-5+rc-2+pc-3+pc-11 (2026-07-08)
- All 5 stories implemented in two conflict-free batches (Batch 1: ed-4/rc-2/pc-3 — independent files; Batch 2: ed-5/pc-11 — dispatched after Batch 1 landed, since ed-5 and pc-11 build on files ed-4/pc-3 had just touched). All 5 closed COMPLETE or COMPLETE WITH NOTES. Test suite grew 10→12 files, 12/12 passing, exit 0.
- Code review (3 combined reviews, since ed-4+ed-5 and pc-3+pc-11 each touch the same shared service file sequentially): **ED (ed-4+ed-5): CHANGES REQUIRED → fixed.** Found a real bug — `self._decayCache` (ed-5's addition) was never cleared on Story 003's pre-existing cap-eviction path in `DisturbanceService:Emit()`, an unbounded per-process leak. Fixed with a one-line nil-assignment, re-verified 12/12 passing. **RC (rc-2): APPROVED**, clean. **PC (pc-3+pc-11): APPROVED WITH SUGGESTIONS** — no blocking changes, but two findings worth tracking: `platform` hardcoded to `"pc"` is a live correctness gap for touch players (not just untested), and `OnSprintPulse` has no owning story anywhere in the PC epic despite two stories assuming it exists.
- Two cheap review suggestions applied same-session (not deferred): an axis-wiring regression test (Sprint/Light hooks feed the correct `emissionState` field, guards against a copy-paste axis swap) and a constant-drift test (pc-3's 8 locally-duplicated ED-registry constants vs. `DisturbanceConstants.luau`'s canonical values) — both added to `tests/unit/player-controller/disturbance-emission-publisher_test.luau`, both pass.
- `docs/tech-debt-register.md`: **TD-004 resolved** (ed-4's own test suite now proves what TD-004 asked for — archive survives live-source eviction, behaviorally not just structurally). **TD-005 added** (platform hardcode, touch-player correctness gap — flagged "do not trust mobile/touch playtesting until resolved"). **TD-006 added** (`OnSprintPulse` epic-level ownership gap). **TD-007 added** (pre-existing, unrelated `control-manifest.md:118` drift describing the old direct-Crafting→RunController model that ADR-0002 already corrected — surfaced by the rc-2 reviewer, not a defect in rc-2 itself).
- `production/sprint-status.yaml` updated: `ed-4`, `ed-5`, `rc-2`, `pc-3`, `pc-11` → `done`. Downstream ED stories unblocked by ed-5's completion: `ed-6`, `ed-7`, `ed-8`, `ed-14` → `ready-for-dev` (blockers cleared).
- Independent verification performed throughout (not just trusting subagent self-reports): re-ran the full test suite myself after every fix; verified the grid-neighborhood coverage math for ed-5's D.3 (cell size 32 ≥ `INFLUENCE_RADIUS` 24 satisfies the G.7 invariant, provably sufficient for a 3×3 query); confirmed "Craft" is a real `EmissionType` in the schema (not a hallucinated agent assumption); manually verified indentation/column-0 `end` boundaries before writing the two new structural regression tests, avoiding the same "naive first-match" class of bug that bit rc-2's and ed-4's own tests earlier this session.
- Process note: two of three code-review agents and one dev-story agent (ed-5) again stopped mid-task with only partial progress narration, not a final report — resumed via `SendMessage` each time, consistent with this session's established recurring pattern.
- Not yet committed: this entire batch (ed-4, ed-5, rc-2, pc-3, pc-11, the two new tests, the tech-debt register updates, the `DisturbanceConstants` harness mount addition).

**NEXT**: Consider committing the substantial uncommitted backlog before continuing. TD-005 (touch platform hardcode) and TD-006 (`OnSprintPulse` missing owner) are real, named gaps worth deciding on (new stories vs. continued tech debt) before mobile playtesting or before Story 010/011's assumptions are trusted. Ready-for-dev stories now include `ed-6`, `ed-7`, `ed-8`, `ed-14` (newly unblocked) alongside whatever was already ready. Eventually open a PR to `main` for a real CI signal (CI uses its own Foreman-installed Lune, not this local `.tools/lune.exe`).

---

## Session Extract — /story-done pc-2, /dev-story+/code-review+/story-done rc-1+pc-1 (2026-07-07)
- pc-2 closed COMPLETE WITH NOTES (4 advisory items carried from its earlier code review, logged as tech debt — `docs/tech-debt-register.md` created, TD-001 through TD-004).
- rc-1 (RunController Core Aggregator) and pc-1 (Locomotion & Sprint-State Authority) implemented IN PARALLEL (independent epics/files, no conflicts — both edited `tests/run_tests.lua` concurrently and merged cleanly). Both code-reviewed (engine specialist verdict CLEAN on both; QA verdict GAPS on both, all gaps closed same-session — rc-1 got a branch-scoped `warn()` check + a `squadState=nil` test; pc-1 got 6 new tests closing the biggest gap found this session: the voluntary "stop sprinting" domain path had never been directly tested, only inferred via the forced-revert path, plus zero coverage on `CheckSprintToggleRateLimit` despite this project's own exploit-resistance testing standard). Both closed COMPLETE.
- `production/sprint-status.yaml` updated: `rc-1`, `pc-1`, `pc-2` → `done`; `rc-2`, `pc-3`, `pc-11` all newly unblocked (`ready-for-dev`).
- Process note: a second subagent (rc-1's implementer) also flagged suspected prompt-injection content, including a new category — a "file modified, don't tell the user" note. Independently confirmed as legitimate: both rc-1 and pc-1's agents really were concurrently editing `tests/run_tests.lua` in parallel, so the harness correctly surfaced that to each of them. Not an attack — same false-positive pattern as two earlier incidents this session (stale multi-engine-template docs, the date-change reminder), now with a third confirmed-legitimate category added to the pattern.
- Not yet committed: ed-3 + fixes, pc-2's closure, rc-1, pc-1, `docs/tech-debt-register.md`.

**NEXT**: ed-4 (Attribution Archive), ed-5 (Decay Formula & fieldValue()), rc-2, pc-3, pc-11 are all ready-for-dev. Consider committing the substantial uncommitted backlog (ed-3, pc-2 closure, rc-1, pc-1, tech-debt register) before continuing, and eventually opening a PR to `main` for a real CI signal (CI uses its own Foreman-installed Lune, not this local `.tools/lune.exe`).

---

## Session Extract — /dev-story + /code-review, pc-2 and ed-1 (2026-07-07)

- **pc-2 (Lantern Axis, Light-to-Gather Gate & Tap-Hold Gather)**: implemented (`src/gameplay/services/PlayerControllerLanternLogic.luau` + `PlayerControllerService.luau`, test at `tests/unit/player-controller/lantern-gather-gate_test.luau`), code-reviewed (verdict CHANGES REQUIRED, solely on the then-missing test-execution infra — no architectural violations). Committed `ea3119a`, pushed to `origin/crafting-round2-patch`. This same commit also scaffolded the test toolchain (`default.project.json`, `foreman.toml`, `tests/run_tests.lua`, `.github/workflows/tests.yml`) around **Lune** (a real Luau runtime) after an initial Lua 5.1/Lemur attempt was caught and rejected as fundamentally incompatible with this project's `--!strict`/typed/`+=` Luau syntax. Toolchain has NOT been run for real anywhere yet (no local Lune/Foreman; CI push happened but branch push alone doesn't trigger the workflow — needs a PR to `main` or a direct push to `main` to actually fire it).
- **ed-1 (Constants Module, Service Bootstrap & 5Hz Field-Update Loop Driver)**: implemented (`DisturbanceConstants.luau`, `DisturbanceServiceBootstrapLogic.luau`, `DisturbanceServiceRegistrationHandshake.luau`, `DisturbanceService.luau`, `src/core/ServerBootstrap.luau`, `src/core/ServerInit.server.luau`, test at `tests/integration/ecological-disturbance/service-bootstrap-config-validation_test.luau`). Code review found and I directly fixed one real ARCHITECTURAL VIOLATION: `default.project.json` mapped `src/core` as a single leaf file instead of a folder, so `ServerBootstrap.luau` was never Rojo-synced and both `require()` call sites (production + test) would have failed at real boot — fixed by mapping `"core": {"$path": "src/core"}`. QA testability review found real, non-blocking coverage gaps (Heartbeat-wiring untested, `RegistrationHandshake` require()-ability untested and not even harness-mounted, `PlayerRemoving` cleanup untested and unflagged). Code-review verdict was CHANGES REQUIRED (same "tests not yet executed anywhere" BLOCKING-gate reasoning as pc-2) — **user explicitly overrode this verdict and approved the story as-is** (2026-07-07), consistent with this project's established precedent that user acceptance overrides panel/review critique. Not yet committed.
- **Process note**: a dev-story subagent (ed-1) flagged what it believed was a prompt-injection attempt in its tool-output stream (a fake system-reminder + fake CLAUDE.md content using Godot/GDScript examples). Independently verified as a false positive — the "fake" content was genuine, if stale, multi-engine-template artifacts already confirmed present in this repo (`.claude/rules/gameplay-code.md` really does contain GDScript examples; this project's own CLAUDE.md documents it ships Godot/Unity/Unreal specialists with no native Roblox one). Worth remembering: subagents lack the broader conversation context to distinguish this from a real attack, so a flagged "injection" should be independently verified against known repo content before treating it as confirmed, not dismissed outright either.

**RESOLVED (2026-07-07, same session)**: the "tests never actually executed" blocker is closed for real. Downloaded a genuine Lune v0.10.5 Windows binary (`.tools/lune.exe`, gitignored — CI installs its own via Foreman) and used it to actually run the suite, iterating against real error output (not guesses) across a long multi-round debugging session. Found and fixed 3 real bugs in the process: (1) `tests/run_tests.lua`'s Roblox-environment setup assumed `game`/`Instance` were ambient Lune globals — they're not; fixed to `require("@lune/roblox")` + `Instance.new("DataModel")` per Lune's real documented API. (2) Lune's `require()` doesn't support Roblox-style Instance-argument calls the way the harness assumed — pivoted to a `loadstring()` + module-cache approach. (3) The harness's hand-rolled TestEZ-mock `expect()` matcher used colon self-sugar (`function assertion:equal(other)`) but every real test file calls it via dot-chaining (`.to.equal(x)`), which silently discarded every real comparison argument — every single assertion in every test file was passing `nil` as `other` regardless of what was written. Fixed to plain closures + added real `.never` support. Also found and fixed one genuine ed-1 test bug: `test_no_datastoreservice_reference_anywhere_in_source` did a naive whole-word grep for "DataStoreService" that false-positived on `DisturbanceService.luau`'s own explanatory comment — narrowed to check for actual `GetService("DataStoreService")` usage. **Final verified result: `.tools/lune.exe run tests/run_tests.lua tests/unit tests/integration` → exit code 0, 3/3 test files pass, 0 failures.** Both story files' Test Evidence sections updated to reflect this. `/story-done` for ed-1 now proceeding with real, not structural-only, test evidence.

## Session Extract — /story-done ed-1 (2026-07-07)
- Verdict: COMPLETE
- Story: `production/epics/ecological-disturbance/story-001-constants-bootstrap-field-update-loop.md` — Constants Module, Service Bootstrap & 5Hz Field-Update Loop Driver
- Tech debt logged: None (no advisory deviations — the one blocking deviation found during code review was already fixed before this closure check)
- `production/sprint-status.yaml` updated: `ed-1` → `status: done`, `completed: "2026-07-07"`
- Next recommended: pc-2 (also has verified-passing test evidence now, still unclosed via `/story-done`) — or rc-1/pc-1 to keep opening new Must-Have stories

## Session Extract — /dev-story + /code-review + /story-done ed-2 (2026-07-07)
- ed-2 (Emission Schema & Server-Authoritative Emit() Intake) implemented, code-reviewed, and closed COMPLETE in one continuous session. `src/gameplay/services/DisturbanceServiceEmissionLogic.luau` (new, pure validator) + `DisturbanceService.luau` (added `Emit()`) + `tests/unit/ecological-disturbance/emission-schema-server-emit-intake_test.luau` (39 tests, genuinely executed and passing). Code review found and fixed 3 non-blocking issues: an ADR-0004 mis-citation, a `_liveSources` typing gap, and — most notably — a real (currently non-manifesting) `stripLuaComments` test-harness bug: its naive first-`--`-match would have silently truncated a `warn()` message that used `--` as a visual separator, hiding the checked-token region from static checks. Fixed by rewording the message and correcting the test file's own overconfident safety-claim comment. Full suite re-confirmed green (4/4, exit 0) after every fix.
- `production/sprint-status.yaml` updated: `ed-2` → `done`; `ed-3` unblocked (`ready-for-dev`, blocker cleared — both its dependencies, ed-1 and ed-2, are now done).

**COMMITTED (2026-07-07)**: ed-1 + ed-2 + Lune toolchain fixes committed as `3d119ef`. Not yet pushed.

## Session Extract — /dev-story + /code-review + /story-done ed-3 (2026-07-07)
- ed-3 (Spatial Hash Grid, Live-Source List & Cap Eviction) implemented, code-reviewed, and closed COMPLETE. `src/gameplay/services/DisturbanceServiceSpatialGridLogic.luau` (new, pure grid/eviction logic) + `DisturbanceService.luau` (Emit() now routes through it) + `tests/unit/ecological-disturbance/spatial-grid-live-source-cap-eviction_test.luau` (21 tests, genuinely executed passing). Code review: engine specialist verdict CLEAN (hand-traced cell math, eviction ordering, tie-break order-independence, grid hygiene — all correct); QA verdict GAPS, all 3 closed same-session (undocumented duplicate-emissionId precondition, untested ops-log warn() call site, unacknowledged liveCount scan cost — all fixed with doc-comments + one new test). Full suite re-confirmed green (5/5, exit 0) after every fix.
- `production/sprint-status.yaml` updated: `ed-3` → `done`; `ed-4` and `ed-5` both unblocked (`ready-for-dev`).
- Not yet committed.

## Session Extract — /story-done pc-2 (2026-07-07)
- Verdict: COMPLETE WITH NOTES. Story: `production/epics/player-controller/story-002-lantern-gather-gate.md` — Lantern Axis, Light-to-Gather Gate & Tap-Hold Gather. Test evidence re-confirmed genuinely passing as part of the current 5/5-file suite.
- 4 advisory items carried forward from the original code-review (never applied — Suggestions, not Required Changes) logged as tech debt: **`docs/tech-debt-register.md` created** (TD-001 through TD-004, the last being ed-3's own already-flagged Story-004 forward obligation, consolidated into the same register).
- `production/sprint-status.yaml` updated: `pc-2` → `done`.

**NEXT**: All 3 stories originally flagged as unclosed (ed-1, ed-2, ed-3, pc-2) are now Complete. Remaining ready-for-dev stories: ed-4 (Attribution Archive — flagged with its own forward-test obligation, TD-004), ed-5 (Decay Formula & fieldValue()), rc-1, pc-1. Uncommitted work: ed-3 + its code-review fixes, pc-2's story-file/sprint-status updates, and the new tech-debt register. Consider committing next, and eventually opening a PR to `main` for a real CI signal (CI uses its own Foreman-installed Lune, not this local `.tools/lune.exe`).

---

## Session Extract — Sprint 1 planning (2026-07-06) — moved into Pre-Production implementation

- **All 14 ADRs ratified → Foundation-layer epics broken into 34 stories → Sprint 1 planned.** User said "move into sprint 1"; scoped to Foundation layer only (Ecological Disturbance, Player Controller, RunController — Resource Management/Crafting deferred to Sprint 2 per user's own choice among 3 options).
- **Synced 3 Foundation epics' EPIC.md files first** — all three referenced conflicts/ADR-Proposed-status that the prior session's architecture-review fixes had already resolved; updated each epic's "Known conflict" callouts to reflect the fixes before generating stories from them (avoids propagating stale blocking language into new stories).
- **Ran 3 parallel background agents** (general-purpose, one per epic) to draft full story decomposition — RunController (2 stories), Player Controller (15 stories), Ecological Disturbance (17 stories) = 34 total. ED's agent response was truncated on delivery (only stories 010-017 arrived); resumed the same agent via SendMessage to get 001-009, which came back clean.
- **Presented full summary tables for all 34 stories, got explicit approval to write all of them**, then wrote all 34 story files (`production/epics/[epic]/story-NNN-*.md`) + updated all 3 EPIC.md Stories tables.
- **Built Sprint 1 plan** (`production/sprints/sprint-1.md` + `production/sprint-status.yaml`). No milestone doc or prior sprint existed — capacity question (team size/sprint length) timed out twice in a row; adopted the recommended default (solo dev, 2-week sprint, ~8 available days after buffer) per this project's established timeout precedent, explicitly flagged PENDING RATIFICATION in the sprint doc's own header.
- **Must-Have list (11.5 days) knowingly exceeds the 8-day budget by ~44%** — flagged explicitly rather than silently undercounted, since the PR-SPRINT producer feasibility gate is skipped in this project's `lean` review mode (no gate to catch this automatically). Must-Have = RunController (both stories) + ED's minimal event-bus spine (bootstrap, Emit() intake, spatial grid, Humanoid.Died fan-out — stories 001/002/003/010) + PC's core loop (locomotion/sprint/stamina, emission publisher, death-cost reconciliation core, death/respawn side-effects — stories 001/003/004/005). Should-Have/Nice-to-Have hold the rest, including everything that needs Predator AI/HUD/Crafting/Resource Management to exist to be meaningful.
- **No QA plan exists for this sprint** (`production/qa/qa-plan-sprint-1.md` — checked, not found). The interactive QA-plan-gate question also timed out; applied the skill's own documented fallback (a `⚠️ No QA Plan` warning block written directly into the sprint doc) rather than auto-triggering `/qa-plan sprint` unprompted.
- **Cross-epic mocking is load-bearing for Sprint 1**: PC-004 (death-cost core) needs Resource Management's `RequestSquadOxygenSpend` receiver, which doesn't exist yet (no RM epic/stories) — every affected story's own QA Test Cases already assume a mock, so this isn't a new gap, just a documented one.

**NEXT**: implementation can start on any `ready-for-dev` story in `production/sprint-status.yaml` (rc-1, ed-1, pc-1, pc-2 have no blockers). Recommended entry point: `/story-readiness production/epics/ecological-disturbance/story-001-constants-bootstrap-field-update-loop.md` → `/dev-story`, since ED-001 unblocks the most downstream work. Consider running `/qa-plan sprint` before implementation goes deep, and re-confirm the capacity assumption with the user at the next opportunity.

---

## Session Extract — /architecture-review 2026-07-06 (re-verification + same-session fixes + follow-up: all 6 carried-forward conflicts fixed)

- Verdict: **PASS** (CONCERNS → then user requested the 6 carried-forward conflicts be fixed too → all fixed → PASS). Every conflict found across both passes (3 new-ADR conflicts + 6 carried-forward) is now resolved. One non-blocking residual item flagged (PC/Crafting GDD prose tension on BC4-forwarding, not implementation-blocking) plus 2 small unfixed text corrections in ADR-0012 (non-blocking, from the original pass).
- **Follow-up fix pass (this turn) — all 6 carried-forward conflicts fixed directly in source**:
  1. ED cap-eviction: GDD's own Q3 open question was explicitly deferred to "the DisturbanceService ADR" — not actually a contradiction, just unclosed. Fixed: tuning-knob table text + Q3 marked RESOLVED, citing ADR-0004's evict-oldest decision.
  2. ADR-0004's `Humanoid.Died` registration moved from its diagram's `KnitStart` section to `KnitInit` (matches GDD C.1.11 + ADR-0001 Rule 1).
  3. `GetOnPlayerDiedSignal` type tension: kept as raw `BindableEvent` (ED's GDD froze this at round-11, closed before ADR-0001 existed — not silently overridden) + added an explicit `Workspace.SignalBehavior = Enum.SignalBehavior.Immediate` boot-time pin per ADR-0001's own stated fallback. ADR-0004 diagram text corrected from "Signal" to "BindableEvent"; ADR-0001 gained a carve-out note.
  4. ADR-0005's clock-seam violation fixed (`row.inFlightSince` now via `PlayerController.getServerTime()` seam) — 3rd and final instance of this bug class now closed (after ADR-0011, ADR-0013 earlier this session).
  5. ADR-0002 (RunController): stale `C.5.8`→`C.9` citation fixed; caller list corrected — Crafting's actual GDD model has it firing to PC, not the arbiter directly, so PC is now stated as sole current caller; added optional `defeatReason: ("wipe"|"scatter")?` field so PC's forwarding of Crafting's BC4 outcomes can carry the `"scatter"` distinction. One residual item flagged, not silently fixed: `player-controller.md` line 369 still says "raised to the arbiter" (implying direct Crafting→RunController), in tension with Crafting's own GDD — recommended as a small GDD-text-sync follow-up.
  6. RM's `RequestSquadOxygenSpend` GDD signature patched in 4 locations (`resource-management.md`: CR.4 definition, PC-interaction-table row, Boundary-3 table row, AC H.10) to match ADR-0005's ratified `(deadPlayerUserId, amount, reason, deathEventId)` form.
- Requirements: 269 total — **108 covered (40.1%), 70 partial (26.0%), 91 gap (33.8%)** (bumped +1 covered from the craft-005 clock-seam fix, after the initial re-verification tally of 107/71/91)
- New TR-IDs registered: none (0 new requirements; this pass re-verified existing TR-IDs against 6 newly-authored ADRs, no GDD text changed)
- ADRs verified this pass: ADR-0009..0014 (all Status: Proposed) — closed RM's 2 blocking Core-tier gaps (accessor + tick loop), PC's 3 self-flagged Foundation-tier gaps, 2 of Crafting's 4 self-flagged Core-tier gaps
- **2 new conflicts found and FIXED same session** (independently confirmed by a second engine-specialist pass, then patched directly in the ADR files): ADR-0011 and ADR-0013 both bypassed their own system's GDD-mandated clock-injection seam with an inline `workspace:GetServerTimeNow()` call — same bug class as the still-unfixed ADR-0005 violation (now the 3rd known instance of this pattern, 2 of 3 fixed). Fix: added `PlayerControllerService.getServerTime` (ADR-0011) and `CraftingService._clock` (ADR-0013) as production-default, test-overridable seam fields and routed all call sites through them.
- Also found and fixed: `architecture.md` line 139 had stale text ("ED's Beacon-Charge-Tier accessor") contradicting ADR-0009's correct Crafting-owned decision — corrected in place.
- GDD revision flags: one **closure** (not a new flag) — PC's GDD self-flagged `HasChunkLoaded()` as an unverified API (OQ.4); ADR-0012 confirmed it doesn't exist and substituted real APIs. Recommend syncing the GDD text to close OQ.4, not yet done.
- Remaining required ADRs (non-blocking for Sprint 1): `/architecture-decision crafting-test-harness-injection-seam` (Core, LOW); RM's oxygen HUD-facing signal contract (could be an ADR-0010 addendum).
- Files updated this turn (follow-up fix pass): `docs/architecture/architecture-review-2026-07-06.md` (verdict → PASS, "Carried-Forward Conflicts: RESOLVED" section added), `docs/architecture/architecture-traceability.md` (Review History row added), `design/gdd/ecological-disturbance.md` (Q3 resolved + tuning table), `docs/architecture/adr-0004-*.md` (diagram fix + new BindableEvent/SignalBehavior Decision subsection), `docs/architecture/adr-0001-*.md` (carve-out note), `docs/architecture/adr-0005-*.md` (clock-seam fix), `docs/architecture/adr-0002-*.md` (citation, caller list, `defeatReason` field, Open Question), `design/gdd/resource-management.md` (4 signature-drift fixes). Files updated earlier this session (re-verification + first fix pass): `docs/architecture/adr-0011-*.md`, `docs/architecture/adr-0013-*.md`, `docs/architecture/architecture.md` (line 139). tr-registry.yaml **not** modified in either pass — no GDD wording changed in the sense the registry tracks, and no new requirements were added.

**RATIFIED (this turn)**: user explicitly ratified ADR-0009 through ADR-0014. All 6 Status headers changed `Proposed` → `Accepted (ratified by the user 2026-07-06)`. `docs/registry/architecture.yaml`'s 6 associated entries (3 state_ownership, 2 forbidden_patterns, 1 interface) flipped from "PENDING RATIFICATION" to "RATIFIED by the user 2026-07-06." Also caught and fixed a stale registry entry while ratifying: the `run_end_condition_raised` interface contract still listed Crafting as a direct caller with the old 2-param-plus-squadState signature — synced to match the ADR-0002 fix from the prior turn (PC-only caller, `defeatReason` field added). **All 14 ADRs (0001-0014) are now Accepted.**

**NEXT**: no blocking work remains from architecture review. Optional cleanup: ADR-0012's 2 small text corrections (Conflict #10), the PC/Crafting GDD prose sync (ADR-0002's residual Open Question). Ready to proceed into Sprint 1 implementation.

---

## (superseded same-day) Session Extract — /architecture-review 2026-07-06 (initial pass)

- Verdict: FAIL (written on a 60s AskUserQuestion timeout — recommended option adopted, PENDING RATIFICATION)
- Requirements: 269 total — 93 covered, 72 partial, 104 gap
- New TR-IDs registered: 269 (TR-pc-001..053, TR-rm-001..028, TR-rn-001..031, TR-ed-001..072, TR-pa-001..032, TR-craft-001..025, TR-hud-001..028)
- GDD revision flags: None (the real cross-doc issues found are ADR-drafting defects against frozen GDD text, not GDD-assumes-wrong-engine-behavior — logged as Cross-Document Conflicts instead)
- Top ADR gaps: RM disturbance-band accessor + tick loop (Core, blocks RM), PC Sprint-State-Authority + Stamina (Foundation, self-flagged never authored), Crafting Survival-Window/Bench-Watcher/Placement-Validation/Test-Harness (Core, self-flagged never authored)
- Report: docs/architecture/architecture-review-2026-07-06.md (also wrote docs/architecture/architecture-traceability.md + populated docs/architecture/tr-registry.yaml)

Context: this review was a carried obligation from the 2026-07-06 gate-check (Technical Setup -> Pre-Production, mechanically FAIL, user-overridden). Also closed this session: `/test-setup` (tests/ + CI workflow scaffolded) and `/create-control-manifest` (docs/architecture/control-manifest.md written). Remaining carried obligations: design/accessibility-requirements.md + design/ux/ (interaction-patterns.md + HUD spec), a ruling on the art-bible #FFD060/predator-hue contradiction, then epic + vertical-slice scoping. See project memory `project_architecture_adrs.md` for full detail.

---

## ED ROUND-24 COMPLETE (2026-07-05): revision applied + narrow gates PASSED-after-same-session-fixes — NOT committed

**Gate results (all 3 delivered after SendMessage resumes — 8 resumes total this session):** Gate A (ai-programmer, accessor seam) FAIL→fixed: 12/12 PA consuming sites reconciled incl. H.57 solo + effectiveQuietThreshold(n); found the SIXTH stale zero-arg site (C.3.3 Disengage-driver line — fixed) + RETREAT_SUSTAIN 30s-vs-PA-20s note (row annotated, PA governs). Gate B1 (perf/CI) PASS-w/I→fixed: archive pre-seed not constructible as written → batch-and-decay procedure + count==4000 assertion pinned; h39d [ -f ] deletion-bypass removed; ≈7% headroom honesty note; 8.2/3720/4×/0.72ms re-derived exact. Gate B2 (qa+systems) PASS-w/I→fixed: B6 exploit UNRECONSTRUCTABLE; H.28d(a)/(e) non-contradictory; **SD-1 G.7 entry had reused 2s-interval constants for the 1s case → corrected (r₁≈0.9772, Hunt ≈6.7 s, S(9)≈0.75 — breach WORSE than stated, closure unaffected)**; H.25c read-seam named + dual-corruption + dual-idiom greps; H.22c single-player pin; H.39e→STATIC-LINT (6th mistag); receiver-table headers refreshed + F.2a-6 cell flipped. **Review-log "Review — 2026-07-05 (round 24)" entry = authoritative. Systems-index updated.** Carried: registry-grooming pass; H.25c(d) sub-lettering; DevOps paths-filter dry-run; RETREAT_SUSTAIN default alignment; round-23 cross-GDD batch (PC/PA/HUD). **Caveat recorded: the ~15 same-session gate fixes lack independent fresh eyes (all mechanical + gate-traceable).**

**NEXT:** commit (user approval pending) → `/review-all-gdds` (all 7 authored — fresh session recommended) → `/gate-check` toward architecture. The recurring found-a-sixth-stale-site pattern says keep the cited-AC + canonical-literal lints running at every future touch.

---

## (superseded same-day) ED round-23 verdict RECORDED + round-24 patch APPLIED (2026-07-04/05, user-directed "revise now"); round-24 NARROW GATES stage record

**Round-23 closure panel: NEEDS REVISION** (7 blocking + RT-2 ruling-request; tripwire NOT fired; NOT MAJOR; scope M) — user ruled: revise now / **RT-2 = social-layer accept** / index yes / log yes. Review-log round-23 verdict entry + systems-index row WRITTEN. **Round-24 patch APPLIED same session — all 7 blocking closed at rule level + RT-2 recorded + 12 IMPORTANT fold-ins** (authoritative itemization: review-log "Patch session — 2026-07-04/05 (round-24)" entry). Headlines: `GetSquadAggregateT(excludePlayer)` authored (C.3.3/D.5/D.6b/G.6 + new AC H.22c — closes PA R5/OQ.10, PA unchanged); MAX_ATTRIBUTION_ARCHIVE_ENTRIES 4000 + H.36a workload 11 + Variant-1 pre-seed; H.38b 3-beacon 1,500-eval fixture extension; FIELD_UPDATE_HZ=5 scope clause; SPRINT_PULSE_INTERVAL + grace×interval + beacon-lifetime×TTL G.7 joint constraints; H.25c(d) P_probe routing + falsifiability control + playerT-guard parity; h39d gate recursive filter + content-grep + per-step if-guards; H.28d(e) cascade parity; STATIC-LINT retags; H.37 → ≈8.2 KB/s (5-term); retired-framing sweep (4th site found at C.3.6 design-intent); both "C.11" sites fixed; Q1/Q8 → HUD next-touch; I13 precondition/RT-2/GD-4/interval bullets. GDD now 1,965 lines; fences balanced; zero live stale literals.

**Round-24 narrow gates SPAWNED (CD-prescribed, NOT a panel):** Gate A ai-programmer (accessor seam end-to-end vs frozen PA incl. H.57 solo semantics + effectiveQuietThreshold(n) counting + server-authority); Gate B1 performance-analyst (PF-1/2/3 closures re-derived + h39d YAML walk incl. content-grep regex vs the four declaration forms + BASE-sha validity); Gate B2 qa-lead-w/-systems-charge (H.25c re-attack, H.22c/H.28d(e) quality incl. the steady-state-vs-cascade wording tension, G.7 arithmetic re-derivation incl. the ~110 s TTL breakeven, final residue sweep). **If session dies: re-spawn the 3 gates from the review-log round-24 entry's NEXT paragraph. DO NOT predict APPROVED.** After gates: verdict entry + systems-index + memory + commit offer.

---

## ED round-22 — AC-extraction EXECUTED + narrow gates **PASSED (3/3, zero BLOCKING)** 2026-07-04 (this session, user-directed)

**GATE RESULT:** network PASS-w/NOTEs (cited-AC-existence lint CLEAN — every H.xx cite resolves; B1/B2 closures HOLD; all 7 relocation pointers verbatim-verified, fences balanced) · systems PASS (27/27 re-derivations MATCH incl. all 11 I13 figures; canonical-literal fanout CLEAN in all 3 files) · PA reconciliation PASS-w/IMPORTANTs (all 4 checks RECONCILED; PA unchanged/NOT reopened). **Tripwire NOT fired** (sole self-seeded item = I13 "Investigate-grade" wording, IMPORTANT-class, fixed same-session — not a ring). **Same-session micro-fixes:** I13 + F.2a row-6 retired-framing correction (fieldValue ≥ 0.65 = FSM-Hunt, attack melee/LOS-gated per PA's H.47; cap suppresses field-driven Retreat escalation only); H.5b "H.5c if present" hedge → D.7 Lantern example pointer; "PA's H.47" namespace clarifier; **N11 NEW obligation** (hotspot.id comparison-convention pin — lexicographic-vs-numeric unpinned on both sides, ADR-owned); forward-obligations header refresh (row 9 → RATIFIED ×2 sites; STILL-OPEN bullet → gate-status). **Carried:** N11; PC pulse-boundary convention (row 9); PA next-touch batch (OQ.11→RESOLVED-by-ED, naming note, I4); G.6 8.0s placeholder yields to PA 15s at ADR. **Authoritative record: review-log "Restructure — 2026-07-04" + "Review — 2026-07-04 (round 22)" entries; systems-index ED row updated. NOT committed.**

**ROUND-23 PANEL — ALL 7 SEATS DELIVERED (2026-07-04); CD SYNTHESIS IN FLIGHT. Seat tally: 5 NEEDS REVISION / 2 APPROVED.** Blocking candidates for CD adjudication: **AI-1** `GetSquadAggregateT(excludePlayer)` never authored (PA R5/OQ.10 + 4 P0 ACs demand it; ED zero-arg at 4 sites; NOT in ED's forward-obligations; COORDINATOR-VERIFIED both sides; Hunt→Disengage structurally unreachable without it — Pillar-1 inversion); **SD-1** SPRINT_PULSE_INTERVAL safe-range floor 1 s breaks canonical ~15/~28 s + row-9 auto-release window + I13 margin (Hunt at ~7.4 s < 8–10 s window; S(9)≈0.685); **PF-1** attribution archive unfenced/uncapped (~3,600 entries/20-min, no AC); **PF-2** 500-sources×3-beacons never fixture-tested (~0.7 ms ≈ half budget, workload-6 full-list scan); **PF-3** PERF ACs only at 5 Hz vs 2–10 Hz safe range; **RT-1/QA-2 convergent** H.25c(d) vacuous (player-position routing not required in GIVEN; SELF-SEEDED, same class as B11 — tripwire-relevant, 1 data point); **RT-2** solo-troll beacon-endgame sabotage (deliberate re-press → Retreat in ~28 s, zero squad counterplay; I13 never examined adversarial intent; ruling-request class); **QA-1** h39d gate path-filter never fires under single-file Knit layout (latent since r7a). IMPORTANT cluster: NP-1 3 more retired-framing sites (:610-611/:1624/fwd:215); GD-1 row-9 title still PROVISIONAL; GD-2 Q1/Q8 trigger fired unresolved; SD-2 D.3/D.4 NaN asymmetry; SD-3 H.28d cascade gap; SD-4/5 joint-range gaps; QA-3 touch 1–4 undefined; QA-4 seam row stale; QA-5 STATIC-LINT taxonomy; RT-3 sprint-planting-on-teammate; RT-4 PC self-contradiction :764 vs :768 (PC-owned); AI-2/AI-3 PA-precision (PA next-touch); PF-4 H.37 itemization. **INJECTION ANOMALY ×3 this round** (ai-programmer ×1, qa-lead ×2; same signature as r21) — all ignored correctly; FLAG TO USER. If session dies: seat reports live in this conversation only — the CD synthesis + verdict entry must be re-run from this checkpoint's list.

*(spawn record)* Round-23 panel spawned same session after commit `ea18b66`: `/design-review design/gdd/ecological-disturbance.md` full-depth — all 7 adversarial seats SPAWNED in parallel 2026-07-04 (game-designer w/ concept+PC+Crafting; systems-designer boundary/composition audit; ai-programmer FSM×field deep dive w/ PA; network-programmer exploit/privacy/post-extraction-enforcement; performance-analyst measurement-scope+memory/GC+mobile; gameplay-programmer RED TEAM on round-22-added text; qa-lead CROSS-SECTION gate w/ injection-anomaly warning) → creative-director synthesis = FINAL verdict. Prompts embed: B1 verify-against-artifact lesson, long-line grep caveat, protected user rulings, registered-obligations-not-new-findings rule, armed tripwire (fresh self-seeded ring → round-24 = hard subsystem re-decomposition). **If this session dies mid-panel: re-run the panel fresh; do NOT trust partial reports.** DO NOT predict APPROVED. After ED closes: `/review-all-gdds` (all 7) → `/gate-check` toward architecture.

---

## (superseded same-day) AC-extraction execution details

**AC-extraction (CD-MANDATED, the deferred half of the 2026-05-30 decomposition) — DONE.** Byte-exact script relocation (sentinels + round-trip assertions + fence-balance + line accounting; same method as 2026-05-30). Five blocks moved GDD → `ecological-disturbance-verification.md` § "Relocated from the GDD's Acceptance Criteria section": H.30c nesting diagram (13 ln), H.30c-i evidence gate YAML+template+clauses (136 ln), H.41m-g template+parse rules+MockSerializer (18 ln), H.39d gate YAML+template+clause (84 ln), H.39e reference impl (13 ln). Pointers at every original site preserve the binding "gate required / epic BLOCKED on gate presence" rules. **H.31b/H.31c regrouped to follow H.31** (ids unchanged; pointer left at Security block; B1-residual note rewritten). **Labeled non-relocation changes:** (1) B2 fanout completion — verification file's MockRemoteSignalRecorder row still said `:FireClient`/`:FireAllClients`, corrected to `:Fire`/`:FireAll` (live stale site in file 2, missed by the GDD-scoped round-22 grep); (2) **I13 CLOSED** — new D.7 worked example "Beacon validation — endgame hold-window × sprint-holding defender": auto-release bounds compliant defender at S(5–6) ≈ 0.42–0.49 Tense (≥0.16 below Hunt floor); cap absorbs bounded readiness noise (pins 0.65); escalation intent-gated on every input method (~1 re-press → Hunt ~15 s, ~2–3 → Retreat ~28 s); 4× co-located sprint-plant → transient sub-mode (ii) Retreat, squad-traceable, decays ≈48 s; H.38 ~40/cell fixture unchanged; **no rule change required.** GDD now **1,940 lines** (2,187 → 1,940; still >1,200 — subsystem split remains CD's escalation option only if size keeps producing defects). Review-log **Restructure — 2026-07-04** entry appended (authoritative itemization). NOT committed.

**Narrow round-22 gates SPAWNED (3 fresh agents, per the round-22 patch plan — NOT a full panel):** network-programmer (cited-AC-existence lint + B1/B2 security-test cluster + extraction integrity + network-surface sweep), systems-designer (B4–B9 re-derivation + canonical-literal fanout 0.28/(0.272,0.30)/0.09/~15s/~28s/~65.1s/~138/~207/~35-per-cell/7.9KB/4Hz + I13 arithmetic audit), ai-programmer (PA reconciliation: hotspotId/H.25d vs PA H.8 incl. "%d_%d" lexicographic-vs-numeric ordering question, 4 Hz sweep, ReleasePredatorLock seam, F.2a rows 1–9 coherence). **Results pending — if this session dies, re-spawn the three gates per the review-log round-22 patch entry's final paragraph.** Tripwire: fresh self-seeded ring → round-23 = hard subsystem re-decomposition. DO NOT predict APPROVED.

---

## (prior) ED round-22 patch — APPLIED 2026-07-03 (same session as the round-21 verdict; user-directed + RATIFIED)

**User returned and ruled:** (1) revise now, (2) systems-index update YES, (3) review-log append YES — all executed. The batched decision widget timed out twice; the three recommended options were adopted under the user's "resume" directive and then **explicitly RATIFIED ("approve")**: B12 = PC-owned stationary sprint auto-release (~8–10 s zero-movement window, HUD pre-warning, re-press restarts) + H.PB1 Q4 input-method split; B3 = `hotspotId` (= D.6 dedup cellKey) added to HotspotResult (PA unchanged; answers PA OQ.11); AC-extraction deferred to a dedicated session (still CD-MANDATORY before the gate).

**Outcome:** 11 of 12 round-21 blocking closed at the rule level. **B1 = verified FALSE POSITIVE** — H.31b/H.31c exist at the end of the Security block (both the network-programmer AND the CD were fooled by non-contiguous AC numbering + grep long-line omission; discoverability pointer added at H.31/H.32). **Durable process lesson: verify every panel blocker's factual claim against the artifact before authoring the fix.** Also closed: 13 IMPORTANT fold-ins (incl. all 9 live `1–4 Hz`→4 Hz sites, E.21's live `:FireClient` — a B2-class catch beyond the panel's list, D.5 finiteness guard, D.1 range scope note, status header refresh) + PC I19 status flip (`player-controller.md` — factual correction only) + new obligations N10 (simultaneous-death semantics, ADR-owned) and F.2a row 9 (RATIFIED). +3 ACs: H.25c (non-finite disqualification, D.6+D.5), H.25d (hotspotId stability/format), H.28d clause (d) (first-pass zero-fire). Full itemization → review-log round-22 patch entry (authoritative).

**Files modified this session:** ecological-disturbance.md, ecological-disturbance-forward-obligations.md, player-controller.md (1 row + 1 table cell), systems-index.md (ED row ×2), reviews/ecological-disturbance-review-log.md (round-21 verdict entry + round-22 patch entry), this file. **COMMITTED `81a9b0f`** (branch `crafting-round2-patch`; user-approved commit-then-stop).

**NEXT (fresh sessions):** (1) **AC-extraction pass** (dedicated; byte-exact relocation per the 2026-05-30 plan; fold in the I13 beacon-endgame × sprint-holding-defender worked example — CD: BLOCKING at the closure gate if unexamined); (2) **round-22 verdict via NARROW fresh-agent gates** — network (security-test cluster + cited-AC-existence lint) + systems (arithmetic/perf + canonical-literal fanout: 0.28/(0.272,0.30)/0.09/~15s/~28s/~35-per-cell/7.9 KB) + one cross-GDD PA reconciliation (hotspotId vs H.8, 4 Hz, ReleasePredatorLock) — NOT a full panel; one full panel reserved as the single closure gate. **Tripwire:** fresh self-seeded ring at the gates → round-23 = hard subsystem re-decomposition. **DO NOT predict APPROVED.**

---

## (record) ED round-21 verdict review — RAN 2026-07-03: **NEEDS REVISION** (superseded by the round-22 patch above; kept for the blocker record)

The round-21 `/design-review design/gdd/ecological-disturbance.md` ran full-depth in a fresh session: 7 adversarial specialists (game-designer, systems-designer, ai-programmer, network-programmer, performance-analyst, gameplay-programmer-as-red-team, qa-lead-as-cross-section-gate) + creative-director synthesis. **Unanimous 7/7 NEEDS REVISION; CD verdict NEEDS REVISION (explicitly NOT MAJOR — the "eighth consecutive failure" re-decomposition trigger does NOT fire).** All 7 round-19 BLOCKING closures HELD (2 partially — third-citation-site misses); full arithmetic re-derivation MATCHED every core figure (S_inf 1.993, ~15s/~28s, 65.1s, 0.272, ~109s, ~138/~207, 0.886) except two isolated cells. 7 of 8 Session-C reverse-cites HOLD (F8 overclaimed — wrong mechanism cited, right conclusion). Pillars UPGRADED vs round-19: P1 STRENGTHENED-with-fairness-caveat, P2 NEUTRAL-with-concern, P3 STRENGTHENED, P4 slightly strengthened. CD's read: this round = the DEFERRED red-team/cross-section gates finally running on the Session B/C deltas; defect class shifted again (arithmetic → citation-site/fixture-spec/cross-GDD-seam hygiene) = convergence, not escalation. **Tripwire: if round-22 fixes self-seed a fresh ring (this round's D.6 guard + S3/S4 errors were self-seeded by prior fixes), round-23 = hard subsystem re-decomposition trigger.**

**AWAITING USER (AskUserQuestion timed out 2×, user AFK — re-ask on return):** (1) revise now vs separate round-22 session (recommended) vs accept-as-is; (2) may I update systems-index ED row → round-21 NEEDS REVISION; (3) may I append the round-21 entry to reviews/ecological-disturbance-review-log.md. **NO project files written this session except this checkpoint. NOT committed.** Full Phase 4 output lives in the conversation; this checkpoint is the fallback if it's lost.

### 12 consolidated BLOCKING (CD-adjudicated, deduped)
1. **[network] H.31b/H.31c ACs DO NOT EXIST** but are cited 6–7× as verification of the round-4 P0 ReplicationFocus exploit closure + `_injectSyntheticEmission` invariant (numbering runs H.31→H.32). P0 security fix has zero specified test coverage. CD-generalized: run a **cited-AC-existence lint** — likely not the only dangling cites.
2. **[network] MockRemoteSignalRecorder fixture spec** (H.29/29b/29c/30/30b) wraps Knit `:FireClient`/`:FireAllClients` — methods C.3.4 L334 says DON'T EXIST on Knit RemoteSignal (`:Fire`/`:FireAll` correct; round-5 correction never propagated, 16 rounds stale) → false-negative privacy-leak net. B1+B2 = ONE compound security-test-completeness fix.
3. **[ai-programmer, CD-elevated] Interface-freeze blocker:** D.6 strict-`>` compare, no epsilon/tie-break, HotspotResult has NO stable id; APPROVED PA's Core Rules 3/4 + tested P0 AC H.8 need epsilon(0.005)+sq-distance+stable `hotspot.id` (= PA's OQ.11, never answered by Session C). Fix: ED adds `hotspotId` (= existing D.6 dedup cellKey) to HotspotResult; PA unchanged. Cannot APPROVE ED without this.
4. **[red-team] D.7 S3/S4 worked-example cells WRONG** (L1046–47: 0.3524/0.4157; correct ≈0.267/≈0.345 per the section's own pinned series). Isolated; introduced by the 2026-06-19 fanout's own table edit. Worked-example-trap class.
5. **[systems-designer + qa-lead, convergent 2×] Line 1927 H.PB1-BETA note stale:** still `SQUAD_T_FAIL_FLOOR_THRESHOLD = 0.20` + debunked "floor noise" rationale, contradicting G.5's 0.28/(0.272,0.30) — third citation site round-20 Session B missed → B-CD18-1/B-R19-SD-2 only PARTIALLY held.
6. **[systems-designer] H.37 bandwidth total mis-summed:** "≈8.9 KB/s" used server-wide 1.4 instead of per-client 0.35 from the SAME sentence; correct ≈7.9. PASS unaffected; BLOCKING per worked-example precedent (network-programmer's independent ~7.7–7.8 derivation confirms).
7. **[systems-designer] G.5 safe-range joint-constraint gap:** individually-"safe" knob values (LIGHT_PULSE 0.12 / STATIONARY_FACTOR 0.60 / HALF_LIFE 60s) each push stationary-light steady-state into Tense, breaking the D.7 "lantern settles in Calm" (Q9) invariant that also anchors SQUAD_T's (0.272,0.30) range; no G.7 warning. Fix: tighten ranges OR document joint constraint.
8. **[performance] "budget UNAFFECTED" proven only on the TOTAL-count axis;** workloads 2/3/6 are per-CELL-density-bound and one stationary hold-floor player now clusters ~34.5 sources/cell (was ~10.75), exceeding H.38's own "≤~30/cell" fixture assumption. Fix: re-derive clustered-cell cost vs 1ms/2.86ms budgets OR add stationary-cluster PERF-DEVICE fixture.
9. **[performance] H.36 Variant 2 self-contradictory:** "Same as Variant 1" (207 MOVING sources) + "all 4 players stationary" (forces ~138) cannot coexist — fixture unreproducible as written. State actual population OR mandate synthetic pre-seeding, and name which one PASS measures.
10. **[red-team + qa-lead, convergent 2×; CD RULED semantics] `previousSquadTier` first-pass unspecified** (C.3.3 L291 "initialised on first pass" — no value, no fire policy) → plausible spurious squad-wide "up" cue at every server boot (PC rebroadcasts). **CD ruling: init to first-pass classified tier, NO fire on pass 1** (mirrors D.4 First-call/H.17b-c; rejected qa-lead's default-Calm-fire-once alternative). + H.28d sub-clause asserting no pass-1 fire.
11. **[convergent 4× (ai-prog B / sys-des I / red-team I / qa-lead B-for-missing-AC); CD RULED BLOCKING] D.6 NaN/+Inf guard incomplete:** resets `bestScore` only — a +Inf candidate still WINS `bestPosition`/`hotspotCount`, returning non-nil result w/ fabricated 0.0 score < minimumTier (violates D.6's own output contract; predator mis-targets). Self-seeded by the round-21 close-out. Fix: move check IN-LOOP (disqualify candidate) + NEW AC H.25c (AUTO-UNIT via E.24 injection seams) so guard-deletion isn't test-invisible.
12. **[game-designer; CD-routed as USER RULING-REQUEST, does NOT reopen the 2026-06-19 ruling]** (a) On TOUCH (primary platform) sprint is a TOGGLE (PC C.1.3) → the "holding sprint IS a choice" fairness rationale for ~28s Retreat-reach is FALSE on mobile; H.PB1 Q4 doesn't split by input method (touch failure pooled away). (b) No AFK/inactivity safety net before squad-wide Retreat in 28s (only advisory first-zone level-design prose). Route: ONE batched ruling-request (touch parity + inactivity sprint-release policy) → PC-owned F.2a forward obligations → ED splits H.PB1 Q4 by input method.

### ~24 IMPORTANT (headline items; full list in conversation Phase 4)
D.5/D.6b lacks the parallel NaN guard; D.1 range column = defaults not envelope; G.7 "3×" false for Light (2.67×); PA never narrates GetOnPlayerDiedSignal acquisition (yield-free closure evidence thin); in-flight FindPathAsync not token-invalidated on target death (≤1 tick); F8 closure text cites wrong mechanism; G.6 "1–4 Hz" stale vs PA's fixed 4Hz; simultaneous squad-death attribution race (shared predator registry purged before 2nd victim's snapshot) needs AC/fallback; H.30c alias-bypass + BindableEvent :Fire honor-system (carried, ADR-owned); no cross-GDD bandwidth rollup doc (producer flag); **I13 beacon-endgame × sprint-holding defender UNEXAMINED at the sole win-con — CD: becomes BLOCKING at the round-22 closure gate** (intersects B8 density + B12 touch-fairness); H.PB1 thresholds calibrated for rare trigger now frequent; HUD H1 only partially closes edge (c), ED prose overclaims; C.3.3 check missing from H.36a instrumentation list; "+~8 gathers" underived; sub-mode-(ii)-is-representative note needed at the 0.10 anchor; **PC L472/482 still calls the OnDisturbanceBandCrossed producer "unauthored"** (cross-file staleness — flip to AUTHORED both sides); H.5b tests one max() branch; H.28d never asserts timestamp; stale GDD status header (round-15/2026-05-07); **AC-extraction pass CD-PROMOTED to mandatory-before-round-22** (proximate cause of the citation-drift class).

### CD's recommended round-22 path (if user picks separate session)
One sequenced authoring pass: (1) **mechanical sweep FIRST** (B4 B5 B6 B7 + I6 I7 I8 I19 I23); (2) **one batched user ruling-request** (B3 hotspotId — recommend add; B10 bootstrap — recommend red-team semantics; B11 guard — recommend in-loop; B12 touch/AFK policy); (3) **test-spec-first** for the security/AC cluster (B1 B2 B10-AC B11-AC/H.25c) — ACs and fixture specs authored FIRST, prose reconciled to them; (4) **fold in the AC-extraction pass (now mandatory)**. MANDATORY gates: all 5 protocol layers + NEW cited-AC-existence lint + NEW cross-GDD interface-freeze reconciliation vs PA + canonical-literal fanout for every changed constant + security-test-completeness template on the P0 cluster. Verdict routing: **narrow fresh-agent gates** (network + systems-designer + one cross-GDD pass), NOT a full 7-agent panel; reserve one full panel as the single closure gate. **DO NOT predict APPROVED for round-22.**

### Process notes (this session)
- 3 of 7 specialists stalled mid-run and were resumed via SendMessage to completion (known long-subagent pattern); all 7 reports genuine + complete.
- qa-lead reported + correctly ignored an injected mid-task message (fake /bug-report + /release-checklist payloads + fake MCP-instructions block) — possible prompt-injection artifact; flag to user.
- Scope signal: M for the round-22 patch; system remains XL (6 dependents, 8 formula clusters, DisturbanceService ADR required).

---

## (prior) HUD narrow-gate re-review — CLOSED 2026-07-03: accepted as-is by user, COMMITTED `cf89ef3`

User ruled accept-as-is over the narrow-gate NEEDS REVISION; systems-index + review-log round-2 entry written (the review-log entry is the AUTHORITATIVE 15-blocking itemization — implementers must read it pre-build). Item 15 (threat-edge loud-ground during Beacon-Window overlay) = unresolved micro-fork awaiting ruling; H.21 not TD-signed-off. Both user-ruled decisions (CR.8 figure-ground, CR.9 split-ownership) HELD under adversarial scrutiny.

---

## MVP GDD board (verified ground truth, 2026-07-03)
- **Crafting** — APPROVED (by acceptance, round-20, `620e67a`)
- **Resource Management** — APPROVED (round-4, `75cf2cf`)
- **Predator AI** — APPROVED (by acceptance, round-7, `8d70c3a`)
- **Player Controller** — APPROVED (round-31) + design-axis RECONCILED (`94ab840`)
- **Resource Node** — APPROVED (by acceptance, round-5, `937c783`)
- **HUD** — ACCEPTED AS-IS 2026-07-03 (`cf89ef3`; 15 blocking carried as open pre-impl obligations in review-log round-2 entry)
- **Ecological Disturbance** — **round-21 verdict = NEEDS REVISION (2026-07-03, this session)**; 12 blocking (list above); round-22 patch pending. NOT approved.

**6 of 7 accepted/approved. Remaining: ED round-22 patch → narrow-gate verdict.** After ED closes: `/review-all-gdds` (all 7) → `/gate-check` toward architecture. Note: systems-index Progress Tracker tally cells remain bloated/stale — separate cleanup.

## Session Extract — /review-all-gdds 2026-07-05
- Verdict: **FAIL** (7 blocking: C1 PC death-signal re-anchor; C2 Crafting same; C3 MAX_ACTIVE_BEACONS unauthored; C4 oxygen-grace-timer mutually disowned PC:123 vs RM:89; C5 HUD bearing on wrong PA channel (OnPredatorSense unwired); C6 registry stale/missing; D1 2-player BC4 collapse needs ruling)
- GDDs reviewed: 7 (4 parallel agents; every headline blocker coordinator-verified; coverage disclosures in report)
- Flagged for revision: player-controller, resource-management, crafting-and-items, hud (+ registry mechanical, PA/ED warning-level)
- Design-theory read: strongly positive ("unusually mature and internally coherent") — the FAIL is seam debt, not design incoherence; 2 items need USER DECISIONS (C4 owner+duration; D1 fallback-vs-accept)
- Recommended next: one cross-GDD seam-patch session (mechanical fixes C1/C2/C3/C5/C6 + the two rulings) → re-run /review-all-gdds (or a narrow re-verify) → /gate-check. Do NOT run /gate-check on this FAIL.
- Report: design/gdd/gdd-cross-review-2026-07-05.md (report write + index flags adopted on widget timeout per the 2026-07-03 precedent — PENDING USER RATIFICATION)

## Session Extract — cross-GDD seam-patch + re-verify CLOSED (2026-07-05/06)
- Rulings (originally widget-timeout-adopted; **explicitly RATIFIED by the user 2026-07-06 — no longer pending**): C4 = PC owns grace timer, OXYGEN_GRACE_DURATION 5 s (4–8), squad-wide, pool-recheck-at-expiry, CR.6 cancel; D1 = accept + BINDING kill-criterion (aliveCount ≤ 2 last-stand floor pre-committed if 2P post-BC4-death win rate ~zero at first playtest).
- All 7 blockers CLOSED + re-verified (6 LANDED-CLEAN first pass; re-verify caught HUD H.20/H.22 stale-channel ACs + a second bench-cap site — both fixed same-session per its prescriptions, standard no-further-pass caveat).
- Files: player-controller.md (C.5 block, G.6 knob, F.2 rows, F.4/F.5 cells, :371 attribution), resource-management.md (C4 note, D1 ruling ×2, RN tags ×3, CR.8 joint invariant), crafting-and-items.md (C.8 block, BCT3 cap + H.131, join pins ×2), hud.md (OnPredatorSense ×3 sites + H.20/H.22 rewrites), ecological-disturbance.md (G.7 mirror note), forward-obligations (3 tracker rows → LANDED), entities.yaml (squadAggregateT exclusion form + 2 new constants), systems-index (4 rows restored), gdd-cross-review-2026-07-05.md (addendum + re-verify close-out).
- Cross-review verdict: FAIL (2026-07-05) → **superseded: closes PASS-equivalent (2026-07-06)**. Next: /gate-check toward architecture.

## Session Extract — /gate-check systems-design→technical-setup 2026-07-06
- Verdict: CONCERNS (no blockers; CD/PR/AD concerns, TD ready). production/stage.txt written to "Technical Setup" (originally timeout-adopted — 3 consecutive widget timeouts this turn — **now explicitly RATIFIED by the user 2026-07-06**, same as the C4/D1 rulings).
- Panel: CD-CONCERNS (RunController climax, Pillar-2 gather-phase tension, pending rulings), TD-READY, PR-CONCERNS (RunController = near-term architecture blocker, ADR round-inflation guardrail), AD-CONCERNS (OQ.6 oxygen-color contradiction vs planned HSV CI gate).
- Day-1 Technical Setup tasks: ratify C4/D1; author RunController contract FIRST (gates PC architecture); resolve HUD OQ.6 before its ADR/CI-hook; port round-inflation guardrail into ADR process; decide Camera/OQ.3; gate PA ADR on prototype; front-load N1/N3/N5 empirical Roblox verifications.
- Gate-check's own chain-of-verification caught + fixed one more stale ref (HUD OQ.4, bearing channel).
- Report: production/gate-checks/systems-design-to-technical-setup-2026-07-06.md
- Next: `/create-architecture` (required before ADRs per the skill's own note) OR ratify the pending rulings first.

## Session Extract — /create-architecture 2026-07-06
- Master architecture document written: docs/architecture/architecture.md (v0.3)
- Technical Requirements Baseline: 218 items across ED(35)/PA(29)/PC(33)/RM(18)/RN(16)/Crafting(40)/HUD(31)/cross-cutting(16)
- System layer map: Foundation=ED+PC+RunController(unauth)+SaveLoad(unauth,no-GDD); Core=RM+Crafting; Feature=PA+RN+Camera(unauth); Presentation=HUD
- 16 required ADRs identified (8 must-have-before-coding, 4 should-have, 4 can-defer); zero ADRs written yet
- Engine Knowledge Gap: 7 HIGH RISK domains (locomotion driver, Input Action System, require()-singleton, BindableEvent sync, Knit RemoteSignal API, KnitStart ordering, AST-tooling), 7 MEDIUM RISK
- TD-ARCHITECTURE sign-off: CONCERNS -> APPROVED WITH CONDITIONS (4 findings fixed in-document same session: TR-coverage overclaim, RequestSquadOxygenSpend dedup-key signature, death-fan-out consumer-count/ReleasePredatorLock-caller contradiction [PA only, not PC], RunController read-direction mandated event-subscriber-only). LP-FEASIBILITY skipped (Lean review mode).
- Discovered + verified via git: a prior RunController arbiter migration was attempted and backed out at commit 3747c3c (round-24) - uncommitted, defeat-half-only, non-buildable. Recorded as a lesson (not salvageable code) in ADR 2's scope.
- Recommended next 3 ADRs, in order: (1) KnitInit/KnitStart Ordering Discipline (cheap, unblocks the others), (2) RunController Architecture (event-subscriber-mandated), (3) Locomotion Driver (gated on /prototype predator-ai).
- Next: /architecture-decision [title] for each ADR (Foundation-layer first) -> /create-control-manifest once ADRs land -> /gate-check pre-production once all must-have ADRs are written.

## Session Extract — ADR authoring 2026-07-06 (ADR-0001 + ADR-0002) — NOT committed, RATIFIED by the user 2026-07-06
- ADR-0001 written: docs/architecture/adr-0001-knit-lifecycle-ordering-discipline.md. Generalizes ED's RegistrationHandshake + HUD's CR.9 KnitInit-mounting rule into one project-wide rule: (1) KnitInit builds cross-service-visible objects, KnitStart only connects; (2) live cross-service registration (not just a signal read) uses a require()-cached RegistrationHandshake module, immune to undefined KnitStart ordering. Engine-specialist review (gameplay-programmer): MINOR NOTES ONLY, all incorporated — prefer Knit's Signal class over Instance.new("BindableEvent") (removes the Workspace.SignalBehavior/Deferred-dispatch risk entirely rather than mitigating it); flagged a real Actor/parallel-Luau require()-cache-identity gotcha (RegistrationHandshake modules must stay in the default serial script context); made the server-only scope explicit.
- ADR-0002 written: docs/architecture/adr-0002-runcontroller-architecture.md. Authors RunController as a thin, idempotent event-fan-in aggregator: one server-internal method RunEndConditionRaised(conditionType: "wipe"|"victory", squadState) callable by PC (T7/T8) and Crafting (C.5.8 BCT-DEFEAT); a one-shot _hasEnded latch; one outbound RunEnded(exitReason, squadState, timestamp) signal. Explicitly designed around the mined lesson from the backed-out commit 3747c3c (which migrated only the defeat half, left victory as a direct PC broadcast, and never declared the interface) and the TD sign-off's event-subscriber-only mandate — RunController never calls back into PC/Crafting/RN, callers always supply their own state in the payload. Engine-specialist review: MINOR NOTES ONLY, both incorporated — (1) terminology fix: RunEndConditionRaised must never be placed under Knit's .Client sub-table (would let any game client fake a wipe/victory and end the run — a server-authority breach); (2) timestamp field switched from workspace:GetServerTimeNow() (server-instance-relative, meaningless to analytics) to os.time() (wall-clock epoch).
- Registry (docs/registry/architecture.yaml) updated with 2 api_decisions, 2 forbidden_patterns, 1 state_ownership, 2 interfaces entries from these two ADRs — all now marked **RATIFIED by the user 2026-07-06** (no longer pending).
- **Process note**: 3 consecutive AskUserQuestion timeouts occurred this session (registry-update widget, next-ADR-choice widget, ADR-0002 assumptions widget). Per this project's established precedent, each was resolved by adopting the recommended option and flagging pending ratification; after the 3rd consecutive timeout, further interactive widgets were paused for the remainder of the session per the "stop offering further widgets once the pattern repeats 3x, state it explicitly" rule — the rest of ADR-0002's authoring (engine-specialist fixes, registry entries) proceeded autonomously using best judgment. **User explicitly ratified both ADRs and all registry entries in the next turn ("ratify both PRs")** — every pending-ratification marker in adr-0002.md and architecture.yaml updated accordingly.
- Nothing committed yet. Next: decide whether to continue authoring the remaining 6 must-have ADRs (Locomotion Driver, DisturbanceService Core Architecture, Death & Respawn Lifecycle, RemoteEvent Trust Boundary, Save/Load & Cosmetic Persistence, Character Replication Bandwidth) or pause here; offer to commit ADR-0001/ADR-0002 + registry once the user is ready. Remember: run /architecture-review only in a FRESH session, never the one that authored the ADRs.

## Session Extract — ADR authoring batch 2 2026-07-06 (ADR-0003..0008, all 6 remaining must-have ADRs) — NOT committed, PENDING RATIFICATION
- All 8 must-have-before-coding ADRs from architecture.md's Required ADRs table are now drafted (0001/0002 ratified prior session-turn; 0003-0008 this turn, user gave blanket "continue authoring all 6" instruction).
- ADR-0003 Locomotion Driver: ratifies Humanoid over Character Controller Library (predator-ai.md's own OQ.7 recommendation), explicit re-evaluation checkpoint at /prototype predator-ai spike. Engine review found + fixed a real bug (Path:ComputeAsync doesn't return ok/err, mutates path.Status in place) + added Path.Blocked repath handling + WaitForChild spawn-race note.
- ADR-0004 DisturbanceService Core Architecture: closes all 11 N1-N11 GDD-mandated obligations (plain table-of-tables spatial grid not Region3; server-computed position-based StreamingEnabled heuristic not ReplicationFocus; oldest-first eviction at 500-cap; N4 rate-limit/coalesce; N5 zero OnServerEvent registrations; N6 eager GetDeathAttributionPayload; N7/N8 ops-log monitoring; N9 atomic chunk-stream-in sequence; N10 pre-dispatch registry snapshot for simultaneous-death race; N11 lexicographic hotspot.id tie-break). Engine review fixed a GridCell type bug, an XZ-vs-3D distance bug, and added PlayerRemoving cleanup.
- ADR-0005 Death & Respawn Lifecycle: formalizes deathEventId minting, the (deadPlayerUserId, deathEventId) idempotency key, and ratifies PC's own Pending/InFlight/Committed/Abandoned reconciliation state machine as the binding project-wide async-charge pattern; restates C4 (5s grace, PC-owned) and D1 (aliveCount<=2 kill-criterion) as binding architecture; closes R7c-I4 (2.0s hard floor). Engine review found 2 BLOCKING issues, both fixed: (1) pcall doesn't await a Promise -- if RequestSquadOxygenSpend is ever made genuinely async, the post-yield re-check would be silently defeated unless the Promise is explicitly awaited inside the pcall; (2) error() at KnitInit doesn't halt startup under typical Knit bootstrap boilerplate (Knit.Start():andThen(print):catch(warn) only logs) -- bootstrap now mandated to call game:Shutdown() on any KnitInit rejection, project-wide.
- ADR-0006 RemoteEvent Trust Boundary: canonical 6-step validation order (alive-guard -> per-event rate limit -> NEW global 30/s per-player budget -> payload-shape -> server-side plausibility re-derivation -> deep validation) + reconciled registry of all 11 game-concept.md surfaces. Engine review: renamed "sliding window" to accurately-described tumbling window, added an explicit RemoteFunction carve-out (silent-drop is fire-and-forget-RemoteEvent-only; a future RemoteFunction must always return, never silently drop, or it leaves the caller's coroutine yielded forever), added PlayerRemoving cleanup for the budget counter.
- ADR-0007 Save/Load & Cosmetic Persistence: only persistence surface in the game (Architecture Principle #5) -- had NO GDD at all. ProfileStore schema + idempotent ProcessReceipt gate (PurchaseId-keyed ledger, durable-write-before-grant). Engine review: added pcall wrapping around the whole callback body, flagged ProfileStore's exact synchronous-save API as genuinely uncertain (not confidently guessable from training data -- explicit WebSearch/devforum-check required before implementation, this is now stated as an open verification item not a design decision), noted ProcessReceipt-singleton-ownership + autosave-collision + BindToClose edge-case risks.
- ADR-0008 Character Replication Bandwidth & NetworkOwnership: default automatic ownership for player characters (movement/visual-feedback client-prediction is explicitly permitted; gameplay outcomes stay RemoteEvent-validated), explicit SetNetworkOwner(nil) for the predator (ratifies PA's own existing design). Baseline bandwidth (~15-30 KB/s) explicitly left PROVISIONAL pending the /prototype predator-ai spike's empirical measurement -- this ADR formalizes the accounting method, not a false-precision number. Engine review: added Anchored-part gotcha (SetNetworkOwner throws), per-instance-not-per-template gotcha (must re-apply on every predator respawn), and a clarifying distinction between player-character ownership (stable) vs ownerless-prop proximity handoff (not applicable here).
- Registry (docs/registry/architecture.yaml) updated with ~15 new entries across all 4 sections (1 state_ownership, 1 interface, 2 performance_budgets, 6 api_decisions, 5 forbidden_patterns) from ADRs 0003-0008 -- all flagged PENDING RATIFICATION (2nd AskUserQuestion timeout this turn; adopted the recommended "update registry + commit" option per established precedent).
- All 6 ADR files + registry + this session-state update are about to be committed in one commit (recommended option, pending timeout).
- Next: user should ratify these 6 ADRs + ~15 registry entries when back. All 8 must-have ADRs are now written -- /create-control-manifest and /gate-check pre-production are the next pipeline steps once ratified. Remember: run /architecture-review only in a FRESH session, never the one that authored the ADRs.

## Session Extract — ADR ratification 2026-07-06 (user: "ratify")
- User explicitly ratified ADR-0003..0008 and all ~15 pending registry entries (docs/registry/architecture.yaml) -- all "PENDING RATIFICATION" markers replaced with "RATIFIED by the user 2026-07-06".
- ALL 8 must-have-before-coding ADRs (0001-0008) now have Status: Accepted (ratified by the user 2026-07-06) -- promoted from Proposed, since /create-control-manifest explicitly generates its output "from Accepted ADRs" per the Pre-Production gate-check requirements.
- Next: /create-control-manifest (all prerequisite ADRs now Accepted) -> /gate-check pre-production (run in a FRESH session, never the one that authored the ADRs, per /architecture-decision's own explicit instruction) -> /architecture-review also recommended in a fresh session to validate coverage.
